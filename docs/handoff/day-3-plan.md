# Day 3 â€” Plan

- **Timeline ref**: BE_TIMELINE_VU.md â†’ Day 3 (Jira: SCV-65) â€” "Identity Service: User + Auth foundation"
- **Prior checklist**: docs/handoff/day-2-checklist.md â†’ **not found** (new handoff process; per request the Day 1â€“2 baseline is verified green: all 6 .NET solutions build 0W/0E, identity + libs tests pass, gateway 11 tests pass). Carry-over preconditions confirmed below.
- **Plan status**: ✅ **v7.5 — ENVELOPE ROLLOUT ADDED (ADR 0004 Accepted, option A): Tasks 3.7–3.11 sequenced BEFORE Day-3 close.** **v7.5 patch (2026-06-01):** ADR 0004 (`docs/adr/0004-api-response-envelope.md`, Accepted 2026-06-01) mandates a single `ApiResponse<T>` envelope for every FE-facing HTTP response on BOTH stacks, rolled out under "option A" (implement + migrate Identity Day 1–3 BEFORE `/verify` + `/audit-day 3`, so Day 3 closes on the new standard — no double migration). v7.5 ADDS five tasks executing ADR 0004 follow-ups #1–#4: **Task 3.7** (SOT docs — rewrite BSOT §5.4 success / §5.5 error / §5.7 pagination / §5.8 filter to the envelope + `PagedResult<T>`/`QueryOptions` spec, bump BSOT 1.4.0 to **1.5.0** MINOR + §13 changelog; wrap every API-Contract response example), **Task 3.8** (.NET shared — `ApiResponse`/`ApiResponse<T>`/`PagedResult<T>`/`QueryOptions` types + a success result-filter (`IAlwaysRunResultFilter`) that auto-wraps `ObjectResult` + an `ApiResponseExceptionFilter` that **REPLACES** `ProblemDetailsExceptionFilter` + an `InvalidModelStateResponseFactory` override for model-binding 400s, all wired through `AddVietRideSharedWeb`), **Task 3.9** (TS — mirror `ApiResponse`/`PagedResult`/`QueryOptions` in `libs/shared/contracts` + a Nest response interceptor + exception filter in `nest-common` + a cross-stack contract test), **Task 3.10** (migrate Identity Day 1–3 tests to assert `.data`/`.error.code` + flip `[ProducesResponseType]` to the envelope type), **Task 3.11** (add the success-envelope invariant line to `AGENTS.md` + the 4 worker/reviewer agent files). **CORRECTION found on disk:** BSOT §5.6 is *Idempotency* (line 1206), NOT pagination — the query/sort/`QueryOptions` rewrite targets **§5.7 (Pagination, line 1235) + §5.8 (Filter conventions, line 1241)**, NOT §5.6 (§5.6 stays untouched). Tasks **3.0–3.6 are FROZEN-as-shipped** (history/acceptance/owned-files/progress rows unchanged) — v7.5 MUST NOT alter them. Open questions Q-v7.5.1–Q-v7.5.5 recorded below with recommended defaults. Dispatch: **3.7 → (3.8 ∥ 3.9) → 3.10 → 3.11**, then `/verify` + `/audit-day 3` (option A). Everything from v7.4 below is preserved unchanged. **v7.4 patch (2026-06-01):** the human resolved Q-v7.3.1 = **option (b)** — Task 3.6's `EfUnitOfWork` persistence proof is the no-new-package MINIMUM and is now the DEFINITIVE gating bar (the prior (a)/(b)/(c) ambiguity in A4 is removed): (1) the Identity composition root resolves `IUnitOfWork`->`EfUnitOfWork` from DI, AND (2) a unit test proves `EfUnitOfWork.CommitAsync`/`SaveChangesAsync` delegates to the underlying `DbContext.SaveChangesAsync` (substitute/mock or test DbContext) — NO new test-DB package (NO Testcontainers/Respawn/Sqlite/EFCore.InMemory), NO live Postgres; real end-to-end cross-scope persistence via live Postgres is explicitly OUT of the Day-3 bar (deferrable to a later approved test-DB harness). A4 now encodes (b) as fixed; A1–A3/A5–A6 unchanged; Q-v7.3.1 moved to "Resolved into plan body"; Open questions = None open. Dispatch unchanged: **3.6 → 3.4(re-impl) final-approve**. Everything else from v7.3 below is preserved unchanged. ✅ **v7.3 — IUnitOfWork SEAM TASK ADDED (Task 3.6); 3.4 now depends on 3.6**. **v7.3 patch (2026-06-01):** re-review of the (re-implemented, v7-acceptance-satisfied: B1/B2/B3/B4/SF1–SF4, build green, 59 identity + 39 kernel tests) Task 3.4 surfaced a Day-3-wide SEAM DEFECT — `IUnitOfWork` (`libs/dotnet/VietRide.Shared.Application/UnitOfWork/IUnitOfWork.cs`) has NO implementation anywhere in the repo, so `TransactionBehavior` (`.../Behaviors/TransactionBehavior.cs` lines 32–35) is a NO-OP whenever no `IUnitOfWork` is registered (VERIFIED on disk). Consequence: the Identity CQRS handlers (VerifyEmail/Login/Refresh/Logout) mutate tracked EF entities whose changes are NEVER flushed/committed across requests (register works only by accident — `EmailVerificationTokenRepository.TryAddAsync` calls its own `SaveChanges`). No Day-3 task wired the flush mechanism. **Human decision: fix as a SHARED-LIB primitive** (all 5 .NET services will need it). **v7.3 adds Task 3.6** — implement `EfUnitOfWork : IUnitOfWork` in `VietRide.Shared.Persistence` (VERIFIED: that project references `VietRide.Shared.Application`, so the impl lives in Persistence with NO layering violation) + register `IUnitOfWork`→`EfUnitOfWork` (scoped, backed by `VietRideDbContextBase`) inside the existing shared `AddVietRideDbContext<TContext>` (VERIFIED non-breaking — all 5 services call it but NONE inject `IUnitOfWork`; only Identity wires `TransactionBehavior`, so the sole behavioral change is Identity’s transaction stops being a no-op; zero per-service edits). **Task 3.4 now `depends on` 3.6** — 3.4’s FINAL reviewer-APPROVE is gated on persistence working end-to-end. Tasks 3.0–3.3 + 3.5 are FROZEN-as-shipped (reviewer-APPROVED on disk) — v7.3 MUST NOT alter their history, acceptance, or owned files. The `IUnitOfWork` interface + `TransactionBehavior` are CORRECT (only the implementation + registration are missing) — Task 3.6 MUST NOT alter either. One Open question added (integration-test DB strategy for the persistence proof — current harness has no real/in-memory DB). ✅ **v7.2 — Q-v7.1 RESOLVED (option (b)); READY for `/implement-task 3.5` then `/implement-task 3.4`**. **v7.2 patch (2026-06-01):** the human resolved Q-v7.1 = **option (b)** — on a WRONG OTP code, the verify-email handler increments `failed_attempts` on the **newest pending token for the user+purpose** via a dedicated repo method `FindLatestPendingAsync(Guid userId, EmailVerificationPurpose purpose, CancellationToken ct)` (returns the most-recent `used_at IS NULL` token for that user+purpose, NO code match — so a wrong-code attempt still has a row to increment), invalidating/disqualifying the row once `failed_attempts >= 5`. The decision is folded into Task 3.4 (owned files + acceptance), the **BLOCKED-until-resolved hard gate is LIFTED** from the Dispatch order (3.5 must still land BEFORE 3.4), and Q-v7.1 is moved from "Open questions" to "Resolved into plan body". Everything else from v7.1 below is preserved unchanged. **v7.1 rework (2026-06-01, applying the PLAN-REVIEW "REQUEST PLAN CHANGES" verdict on v7):** (B1) the OTP rate-limit 429 path now has a CONCRETE exception type + filter arm — `TooManyRequestsException` (new, in `ApplicationExceptions.cs`) + a `429` arm in `ProblemDetailsExceptionFilter` (both ratified by Task 3.5), replacing the vague "an exception mapped to 429"; (B2) **Q-v7.1 is now a HARD GATE** — Task 3.4’s wrong-code-increment target MUST be resolved by the human and folded into 3.4 acceptance BEFORE the 3.4 re-implement is dispatched; 3.4 dispatch is **BLOCKED-until-resolved** and the implementer’s (a)/(b)/(c) discretion is removed (now "per resolved Q-v7.1"); (B3 should-fix) the OTP rate-limit acceptance is aligned to the on-disk interface `Task<bool> TryIncrementAsync(string email, CancellationToken ct = default)` (handler checks the bool return, not the non-existent `EnsureWithinLimitAsync`). Everything else (3.0–3.3 frozen, the v7 seam fix, B1/B2/SF1–SF4) is preserved unchanged. Tasks 3.0–3.3 remain reviewer-APPROVED + implemented on disk — this revision MUST NOT alter their history, acceptance, or owned files except where 3.2’s repository contract is explicitly extended below (the `FindByCodeAsync` companion + the ownership move are the ONLY 3.2 deltas). Patch scope (the 3 inputs the human raised after Task 3.4 was blocked): (1) **fix the 3.2↔3.4 seam defect** — 3.2’s `FindActiveAsync` returns opaque-null on every miss, which cannot satisfy 3.4 DoD line 26 (INCREMENT `failed_attempts` on a wrong code; distinguish `AUTH_OTP_EXPIRED` 400 from `AUTH_OTP_INVALID` 400). Add a companion `FindByCodeAsync(userId,code,purpose,ct)` that returns the row WITHOUT the active filters so the 3.4 handler can inspect expiry + increment the counter, and **move ownership of `IEmailVerificationTokenRepository.cs` + `EmailVerificationTokenRepository.cs` (the method-add edit only) into Task 3.4** so a re-implement is not forced to breach scope; (2) human decision B2 — register a NEW error code `AUTH_OTP_RATE_LIMIT_EXCEEDED` (429) in BSOT §5.9 + bump the BSOT changelog = **new Task 3.5**; (3) reconcile the out-of-fence edits the blocked 3.4 attempt already made (decision: **KEEP all** — each is coherent, builds green, and is required by the contract — re-homed to its correct owning task below; no reverts). Plus the remaining 3.4 reviewer should-fixes: B1 (`AUTH_PHONE_INVALID_FORMAT` → 400 BadRequestException, not 409 ConflictException), `CreatedAtAction`→`StatusCode(201)`, one-class-per-file for request records, and do-not-echo user-supplied `purpose` into error detail. History: v6 APPROVED → 3.0–3.3 implemented + reviewer-APPROVED → 3.4 blocked (REQUEST CHANGES: seam defect + 4 BLOCKERs + out-of-fence edits) → **v7 seam patch (this revision)**.

## Carry-over preconditions (verified against current tree, do not re-do)
- `IdentityDbContext` exists (`apps/identity/src/VietRide.Identity.Infrastructure/IdentityDbContext.cs`, schema `vietride_identity`) and inherits `VietRideDbContextBase` which already maps `outbox_messages` + snake_case + auditing.
- `IDesignTimeDbContextFactory<IdentityDbContext>` exists (`.../Design/IdentityDbContextDesignFactory.cs`) â€” migrations run without booting the host.
- `Program.cs` already wires `AddVietRideSharedWeb` (Internal JWT HS256, ProblemDetails filter, Swagger, Health, `IClock`) + `AddVietRideDbContext<IdentityDbContext>`.
- Gateway routes for `/v1/auth`, `/v1/.well-known`, `/v1/users` already present in `apps/gateway/src/config/routes.ts` (`authRequired: 'none'` for auth + jwks). **No Gateway change needed Day 3.**
- Shared exceptions (ValidationException to 422, NotFoundException to 404, ConflictException to 409, ForbiddenException to 403) + `ProblemDetailsExceptionFilter` exist in `VietRide.Shared.Web`/`VietRide.Shared.Application`.
- `Result<T>`/`Error`, `BaseEntity<TId>`, `IAuditable`, `ISoftDeletable`, `Money`, `PhoneNumber`, `IClock`/`SystemClock` exist in `VietRide.Shared.Kernel`.
- Outbox plumbing (`OutboxMessage{Id,OccurredAt,Type,Payload,...}`, `IOutboxStore`, `OutboxStore`) exists; `Type` column = routing key, `Payload` = jsonb. Identity does NOT yet have an Outbox publisher hosted service (that is Day 10 per timeline) â€” Day 3 only INSERTs the row in-transaction.
- `Behaviors/` folder does NOT exist in `VietRide.Shared.Application` (confirmed). MediatR / FluentValidation / BCrypt / NetArchTest are NOT in `Directory.Packages.props` (confirmed).

## Objective
Stand up the Identity CQRS + auth foundation: the architecture baseline (MediatR v11 pipeline behaviors + FluentValidation + BCrypt via CPM, NetArchTest layering test), the 5 auth tables via EF migration, RS256 access-token signing + JWKS endpoint, opaque rotating refresh tokens, BCrypt cost-12 password hashing, and the email register to OTP verify to login to refresh to logout flow with a logging-only email provider stub (`LoggingEmailService` â€” no SendGrid SDK in Identity per Q4 decision). Delivers SCV-65 DoD: passenger can register, receive OTP, verify, login, get access+refresh token; JWKS serves the public key. Unblocks Day 4 (Google OAuth, complete-phone, admin bootstrap) which build on the same User/token machinery.

## Success criteria (DoD â€” binary, verifiable)
- [ ] `dotnet build apps/identity/VietRide.Identity.sln -c Release` and `libs/dotnet/VietRide.Libs.sln` build 0 warnings / 0 errors.
- [ ] `dotnet format --verify-no-changes` clean on both touched solutions.
- [ ] NetArchTest dependency-direction test in `VietRide.Identity.UnitTests` passes (Domain to nothing; Application to Domain; Infrastructure to Domain+Application; Api to Application+Infrastructure).
- [ ] `dotnet ef migrations add` produces a migration that, applied to an empty DB, creates `users`, `oauth_identities`, `refresh_tokens`, `email_verification_tokens`, `user_devices` matching `db-schema/identity-user/schema.sql`; `Down()` reverses it.
- [ ] `POST /v1/auth/register` returns 201 with body `{userId,email,status:"PENDING_EMAIL_VERIFICATION",otpTtlMinutes:5}`; persists a `REGISTRATION` OTP; duplicate email returns 409 `AUTH_EMAIL_ALREADY_REGISTERED`.
- [ ] `POST /v1/auth/verify-email` with correct code returns 200 `{userId,status:"ACTIVE"}`; wrong code returns 400 `AUTH_OTP_INVALID` (increments `failed_attempts`, invalidates after 5); expired returns 400 `AUTH_OTP_EXPIRED`.
- [ ] `POST /v1/auth/login` valid creds on ACTIVE user returns 200 token bundle (RS256 access ~15min, opaque refresh 30d, `user{...}`); bad creds returns 401 `AUTH_INVALID_CREDENTIALS`; unverified returns 403 `AUTH_EMAIL_NOT_VERIFIED`; locked returns 403 `AUTH_ACCOUNT_LOCKED`.
- [ ] `POST /v1/auth/refresh` rotates: old token revoked (`NORMAL_ROTATION`), new token same `family_id`, `parent_token_id` chained; reused revoked token revokes whole family + 401 `AUTH_TOKEN_INVALID`.
- [ ] `POST /v1/auth/logout` returns 204; refresh token revoked with reason `USER_LOGOUT`.
- [ ] `GET /v1/.well-known/jwks.json` returns 200 JWKS with `kty=RSA, alg=RS256, use=sig, kid, n, e`; the `kid` matches access-token header.
- [ ] Each new endpoint has at least 1 happy-path + 1 error-case unit/integration test and Swashbuckle `[ProducesResponseType]` annotations.

## Contract changes
- **Endpoints** (all under existing Gateway `/v1/auth` + `/v1/.well-known` route families â€” no routes.ts edit): `POST /v1/auth/register`, `POST /v1/auth/verify-email`, `POST /v1/auth/login`, `POST /v1/auth/refresh`, `POST /v1/auth/logout`, `GET /v1/.well-known/jwks.json`. Shapes per `VietRide_API_Contract_v1.md` Identity & User Service section, lines 18â€“136. Note: contract verb is `verify-email` with body `{email,code,purpose}`, NOT a generic `/verify`.
- **Error codes** (already in BSOT 5.9 registry â€” no new codes): `AUTH_EMAIL_ALREADY_REGISTERED` (409), `AUTH_PHONE_ALREADY_REGISTERED` (409), `AUTH_INVALID_CREDENTIALS` (401), `AUTH_EMAIL_NOT_VERIFIED` (403), `AUTH_ACCOUNT_LOCKED` (403), `AUTH_OTP_INVALID` (400), `AUTH_OTP_EXPIRED` (400), `AUTH_TOKEN_INVALID` (401), `AUTH_PHONE_INVALID_FORMAT` (**400** — v7 patch B1: maps to `BadRequestException`, NOT `ConflictException`/409; bad-format input is a 400, only a duplicate phone is 409 `AUTH_PHONE_ALREADY_REGISTERED`). **NEW CODE (v7, human decision B2)**: `AUTH_OTP_RATE_LIMIT_EXCEEDED` (**429**) — OTP request rate limit hit (Redis `identity:otp_rate:{email}` max 3/h, BSOT §6.9 line 1545). This code is NOT yet in the BSOT §5.9 registry; **Task 3.5 registers it + bumps the BSOT changelog BEFORE Task 3.4 references it**. The OTP rate-limit path MUST use this code (429), NOT reuse `AUTH_OTP_INVALID` (400). The matching `BadRequestException`/`UnauthorizedException` shared exceptions (400/401) used by the verify/login handlers already exist in `VietRide.Shared.Application/Exceptions/ApplicationExceptions.cs` and are mapped by `ProblemDetailsExceptionFilter` (added during the blocked 3.4 attempt — KEEP, re-homed to Task 3.4 / 3.5; see those tasks).
- **DB migration**: 5 auth tables (User, OAuthIdentity, RefreshToken, EmailVerificationToken, UserDevice) PLUS a minimal **`operators` stub table** (PK only) in schema `vietride_identity` per `db-schema/identity-user/schema.sql`. **Decision (post-review):** `users.operator_id` declares an intra-service FK to `operators(id)` (schema line 149) â€” `operators` lives in the same `vietride_identity` DB, NOT a different service DB. The Day-3 migration must create `operators(id UUID PK)` BEFORE `users` so the FK is satisfiable on an empty DB. This is a deliberate scoped deviation from schema.sql (which declares ~30 columns + the `operator_registration_status` enum): Day 6 will `ALTER TABLE operators ADD COLUMN ...` + create the enum + add the Domain behavior/endpoints. Subscription/ActivityLog tables remain out-of-scope (Day 5+/Day 6+).
- **Events**: register **does NOT** insert an Outbox row in Day 3 (decision, post-review). Timeline Day 10 explicitly wires Outbox into Identity (`user.created`, `operator.approved`, `staff.password_set`); Day 10 will both add the Outbox publisher hosted service AND retro-emit `identity.user.created` for existing users via a migration/seed if needed. Day 3 may still raise a domain event in-memory (no consumer yet) but does NOT touch `outbox_messages`.
- **Config/secrets** (decision, post-review): RS256 keypair env vars are `USER_JWT_PRIVATE_KEY` (PEM) + `USER_JWT_KID` (string, e.g. `"2026-05"`) per BSOT Â§11.3 lines 2261/2264. Worker adds a dev placeholder under `IdentityJwt:PrivateKey` / `IdentityJwt:Kid` in `appsettings.Development.json` (NOT a real production key); production reads from env. `.env` remains forbidden scope.
- **Phone (decision Q3)**: `POST /v1/auth/register` body field `phone` is **REQUIRED** for PASSENGER self-registration (v7 lines 1309 + 4720). Accepted input forms = local `^0[0-9]{9,10}$` OR E.164 `^\+84[0-9]{9,10}$`. Backend normalizes local â†’ E.164 (strip leading `0`, prepend `+84`) BEFORE persisting; DB only stores E.164 (schema line 161 CHECK). Other forms â†’ 400 `AUTH_PHONE_INVALID_FORMAT`. Duplicate phone (after normalize, `deleted_at IS NULL`) â†’ 409 `AUTH_PHONE_ALREADY_REGISTERED`. Google-OAuth path (Day 4) is the separate optional/complete-profile flow.
- **Email service (decision Q4)**: Day 3 ships an Identity-local `LoggingEmailService : IEmailService` that writes the OTP via Serilog. **No `SendGrid` NuGet in Identity csproj. No `@sendgrid/mail` anywhere.** Day 10 will (a) wire the Outbox publisher hosted service in Identity, (b) replace `LoggingEmailService` with `OutboxBackedEmailService` that inserts `identity.otp.requested`, (c) add a Notification Service consumer for that routing key (BullMQ job â†’ SendGrid). Per BSOT Â§1.2 line 93 + Â§3.5 line 419 the SendGrid SDK lives in Notification only.

## Tasks

### Task 3.0 â€” Architecture baseline: CPM deps + MediatR pipeline behaviors + NetArchTest layering (DO FIRST; blocks all below)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none) |
| owned files (write set) | `Directory.Packages.props`; `libs/dotnet/VietRide.Shared.Application/Behaviors/ValidationBehavior.cs`; `libs/dotnet/VietRide.Shared.Application/Behaviors/LoggingBehavior.cs`; `libs/dotnet/VietRide.Shared.Application/Behaviors/TransactionBehavior.cs`; `libs/dotnet/VietRide.Shared.Application/DependencyInjection/ApplicationServiceCollectionExtensions.cs` (new â€” `AddVietRideMediatRBehaviors`); `libs/dotnet/VietRide.Shared.Application/VietRide.Shared.Application.csproj`; `apps/identity/tests/VietRide.Identity.UnitTests/Architecture/LayeringTests.cs`; `apps/identity/tests/VietRide.Identity.UnitTests/VietRide.Identity.UnitTests.csproj` |
| forbidden scope | `.env`, any secrets, real package `Version=` on a `<PackageReference>`; any other service (`apps/trip|booking|payment|parcel|gateway|tracking|notification|rag`); git branch/commit/push; do NOT delete `SanityTests.cs`; do NOT touch other `libs/dotnet/*` projects |
| depends on | â€” |
| parallel-safe | no (every feature task depends on it) |
| invariant flags | CPM no `Version=` on `<PackageReference>` (add `<PackageVersion>` only); **MediatR pinned 11.x** (v12+ banned); FluentValidation + FluentValidation.DependencyInjectionExtensions 11.x; BCrypt.Net-Next latest; NetArchTest.Rules; CRLF on all `.cs`/`.csproj`/`.props`; no banned deps; async methods take `CancellationToken` |
| acceptance | `Directory.Packages.props` adds (as `<PackageVersion>` only â€” no `Version=` on `<PackageReference>`): **`MediatR` 11.* AND `MediatR.Extensions.Microsoft.DependencyInjection` 11.*** (both required â€” DI extension is a separate package in v11; only merged in v12 which is banned), `FluentValidation` 11.*, `FluentValidation.DependencyInjectionExtensions` 11.*, `BCrypt.Net-Next` (latest stable), `NetArchTest.Rules` (latest), **`NSubstitute` 5.*** (mock library for Task 3.3/3.4 unit tests â€” confirmed absent from CPM), **`StackExchange.Redis` (latest 2.x)** (OTP rate-limit Redis client per BSOT Â§6.9; confirmed no existing Redis abstraction in `libs/dotnet`; connection string via `REDIS_URL` env, BSOT Â§11.3); behaviors implement `IPipelineBehavior<TRequest,TResponse>` (Validation throws `ValidationException` with field errors; Transaction wraps DbContext `SaveChanges`+commit via `IUnitOfWork` if available, else `_db.Database.BeginTransactionAsync` directly; Logging logs request name + elapsed ms); `VietRide.Shared.Application.csproj` references MediatR + FluentValidation packageless; NetArchTest verifies all 4 layer rules and passes; `dotnet build` libs sln + identity sln 0W/0E; `dotnet format --verify-no-changes` clean |
| source citations | BE_TIMELINE_VU.md Day 3 "Pre-reqs" (lines 48â€“51); BSOT 3.2 "Pipeline: Controller-MediatR.Send-Handler" (lines 373â€“376), 3.2.1 Behaviors examples (lines 254â€“256), 3.2.2 NetArchTest example + layer rules (lines 497â€“519), 2 banned/pinned deps (lines 121â€“138); AGENTS.md Hard invariants (MediatR v11, CPM); `IUnitOfWork` at `libs/dotnet/VietRide.Shared.Application/UnitOfWork/IUnitOfWork.cs`; `ValidationException` at `.../Exceptions/ApplicationExceptions.cs` |

### Task 3.1 â€” Domain: User aggregate + sub-entities + enums + value objects
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | scaffold-aggregate (Domain layer portion) |
| owned files (write set) | `apps/identity/src/VietRide.Identity.Domain/Entities/User.cs`; `.../Entities/OAuthIdentity.cs`; `.../Entities/RefreshToken.cs`; `.../Entities/EmailVerificationToken.cs`; `.../Entities/UserDevice.cs`; **`.../Entities/Operator.cs` (Day-3 STUB â€” Id-only POCO inheriting `BaseEntity<Guid>`; no behavior, no other fields; explicit XML doc comment: "Stub for Day 3 FK satisfaction; Day 6 extends with full schema + behavior")**; `.../Enums/UserRole.cs`; `.../Enums/UserStatus.cs`; `.../Enums/OAuthProvider.cs`; `.../Enums/EmailVerificationPurpose.cs`; `.../Enums/RefreshTokenRevokeReason.cs`; `.../Enums/DevicePlatform.cs`; `.../Exceptions/IdentityDomainException.cs` (+ specific subclasses); `apps/identity/tests/VietRide.Identity.UnitTests/Domain/UserTests.cs` |
| forbidden scope | `.env`, secrets; Application/Infrastructure/Api layers (Domain stays pure POCO â€” NO EF Core, NO MediatR, NO data annotations, NO `[Key]`/`[Required]`); other services; git ops; do NOT add packages |
| depends on | 3.0 |
| parallel-safe | no (3.2/3.4 depend on these types; disjoint write set from 3.0) |
| invariant flags | Domain ZERO external refs (NetArchTest enforces); status transitions in entity methods (`User.VerifyEmail()`, `User.RecordFailedLogin()`, `User.Lock()`); 1 class/file; nullable enabled; CRLF |
| acceptance | Enums match schema exactly: `UserRole{PASSENGER,DRIVER,ASSISTANT,OPERATOR_STAFF,OPERATOR_ADMIN,SYSTEM_ADMIN}`, `UserStatus{PENDING_EMAIL_VERIFICATION,PENDING_INITIAL_PASSWORD,ACTIVE,LOCKED,DELETED}`, `OAuthProvider{GOOGLE}`, `EmailVerificationPurpose{REGISTRATION,PASSWORD_RESET,SET_INITIAL_PASSWORD}`, `RefreshTokenRevokeReason{NORMAL_ROTATION,REUSE_DETECTED,USER_LOGOUT,ADMIN_REVOKE,PASSWORD_RESET}`, `DevicePlatform{IOS,ANDROID,WEB}`; `User` inherits `BaseEntity<Guid>` + `ISoftDeletable` (per **ADR 0003** [2026-05-31], which supersedes the 2026-05-30 interim decision — `ISoftDeletable` is now getter-only `DeletedAt`, satisfied by `User`'s private-set property; `users` has no `is_active` column [schema.sql lines 140-168] and does NOT implement `IActivatable` (uses `status` for the activation axis); soft-delete is `deleted_at`-only; `User` declares `public DateTimeOffset? DeletedAt { get; private set; }` (private set protects the soft-delete invariant; EF Core 8 hydrates private setters), `SoftDelete()` sets `DeletedAt` + `Status=DELETED`, no `IsActive`; Task 3.2 EF filters `WHERE deleted_at IS NULL`, not on `IsActive`); fields cover schema columns ONLY (email, phone, passwordHash nullable, displayName, avatarUrl, role, status, operatorId, failedLoginAttempts, lastFailedLoginAt, lastLoginAt, deletedAt â€” schema.sql lines 140â€“168 has NO `locked_until` column; do NOT invent one); `RefreshToken` has familyId, parentTokenId self-ref, tokenHash, expiresAt, revokedAt, revokedReason; `EmailVerificationToken` has purpose, code, expiresAt, failedAttempts, usedAt; `Operator` is a stub with only `Id` (no other state, no methods â€” Day 6 extends); domain methods: `User.VerifyEmail()` enforces `PENDING_EMAIL_VERIFICATION â†’ ACTIVE`; **`User.RecordFailedLogin(IClock)` increments `failed_login_attempts` AND sets `last_failed_login_at`; when the counter reaches `>= 5` (the 15-minute window is the Redis counter TTL on `identity:login_lockout:{userId}` per BSOT Â§10 line 2064, NOT a `LockedUntil` field â€” that field does not exist in schema), the method transitions `Status â†’ LOCKED`. The lock is PERMANENT (no auto-unlock by timeout) â€” only System Admin unlocks manually per `SU26SE101_VIETRIDE_technical_context_v7.md` line 4683**; `User.ResetFailedLogins()` zeros the counter on successful login; **`User.RecordSuccessfulLogin(IClock)`** sets `last_login_at = clock.UtcNow` and zeros `failed_login_attempts` (called by the login handler on success â€” Task 3.4); **`User` factory takes phone as `PhoneNumber` value object (E.164) and throws if not normalized â€” domain enforces the invariant; Application layer normalizes the raw input via `PhoneNumber.Normalize(...)` before constructing the entity (Task 3.4)**; at least 2 domain unit tests (happy `RecordFailedLogin` increment + boundary at attempt 5 triggers LOCKED with no `LockedUntil` assignment); `PENDING_INITIAL_PASSWORD â†’ ACTIVE` transition deferred to Day 5; build clean |
| source citations | `db-schema/identity-user/schema.sql` enums (lines 21â€“49), `users` (140â€“184), `oauth_identities` (189â€“204), `refresh_tokens` (209â€“236), `email_verification_tokens` (241â€“260), `user_devices` (265â€“279); BSOT 3.2.1 Domain layout (lines 449â€“455), 3.2.3 (entity invariants in methods, pure POCO, lines 543â€“546); status machine BSOT (line 1843: PENDING_EMAIL_VERIFICATION to ACTIVE); refresh fields BSOT 6.3 (lines 1424â€“1430) |

### Task 3.2 â€” Infrastructure: EF configurations + DbContext DbSets + repositories + migration
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | ef-migration |
| owned files (write set) | `apps/identity/src/VietRide.Identity.Infrastructure/IdentityDbContext.cs` (add DbSets + apply configs); `.../Persistence/Configurations/UserConfiguration.cs`; `.../Configurations/OAuthIdentityConfiguration.cs`; `.../Configurations/RefreshTokenConfiguration.cs`; `.../Configurations/EmailVerificationTokenConfiguration.cs`; `.../Configurations/UserDeviceConfiguration.cs`; **`.../Configurations/OperatorConfiguration.cs` (Day-3 stub mapping â€” `operators` table with Id PK column only; XML doc comment: "Day-3 stub; Day 6 ALTER TABLE adds remaining columns + `operator_registration_status` enum")**; `.../Persistence/Repositories/UserRepository.cs`; `.../Repositories/RefreshTokenRepository.cs`; `.../Repositories/EmailVerificationTokenRepository.cs` (**v7 NOTE: shipped + approved; the `FindByCodeAsync` companion method-ADD is delegated to Task 3.4 — see 3.2 forbidden-scope + 3.4 owned-files**); `apps/identity/src/VietRide.Identity.Application/Abstractions/Repositories/IUserRepository.cs`; `.../IRefreshTokenRepository.cs`; `.../IEmailVerificationTokenRepository.cs` (**v7 NOTE: same — `FindByCodeAsync` method-ADD delegated to Task 3.4; 3.2 keeps the shipped `FindActiveAsync` only**); `.../Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs`; `apps/identity/src/VietRide.Identity.Infrastructure/Migrations/*` (generated) |
| forbidden scope | `.env`, secrets; Domain entity files (read-only ref); Api layer; other services; git ops; do NOT hand-edit a generated migration model snapshot beyond what `dotnet ef` writes; no cross-DB FK to OTHER services (Operator is in the SAME `vietride_identity` DB â€” its intra-service FK from `users.operator_id` IS declared in Day 3); do NOT seed any `operators` row in Day 3 (Day 6 seeds); no new packages (CPM entries land in Task 3.0); **v7 SEAM PATCH: Task 3.2 is frozen-as-shipped — do NOT re-open it. The companion `FindByCodeAsync(...)` method that the verify-email handler needs is added by Task 3.4, NOT here; 3.2 ships only `FindActiveAsync` as already approved.** |
| depends on | 3.0, 3.1 |
| parallel-safe | no |
| invariant flags | snake_case (base context already applies via `ApplySnakeCaseNaming`); enums mapped as Postgres native enums OR string â€” must match schema PG enum types; partial unique indexes (`uq_users_email` on `LOWER(email) WHERE deleted_at IS NULL`, `uq_users_phone`, `uq_refresh_tokens_token_hash`, `uq_oauth_identities_provider_subject`); migration reversible `Down()`; CRLF; `IDesignTimeDbContextFactory` already present â€” do not duplicate |
| acceptance | DbSets added for all 6 entities (5 auth + `Operator` stub); each `IEntityTypeConfiguration<T>` maps columns/indexes/constraints per schema (incl. phone CHECK regex E.164 VN +84 9â€“10 digits, `chk_users_operator_role`, partial unique indexes, `family_id`/`parent_token_id` self-FK); **`OperatorConfiguration` creates `operators` table with `id UUID PK DEFAULT gen_random_uuid()` ONLY (intentional Day-3 stub; Day 6 ALTER TABLE adds the remaining ~30 columns + creates `operator_registration_status` enum + adds NOT NULL/CHECK/UNIQUE constraints â€” the migration deviation is documented in plan body)**; users.operator_id maps to that stub via real intra-service FK (`ON DELETE RESTRICT`, schema line 149); `dotnet ef migrations add InitIdentityAuth -p apps/identity/src/VietRide.Identity.Infrastructure -s apps/identity/src/VietRide.Identity.Api -o Migrations` succeeds; applying to empty DB yields tables matching `schema.sql` **for the 5 auth tables in full + `operators(id)` stub**; `Down()` drops all in correct order (users before operators); repositories implement interfaces: `IUserRepository.GetByEmailAsync(string emailLower)` + `IUserRepository.GetByPhoneAsync(string e164)` (both filter `deleted_at IS NULL`); `IRefreshTokenRepository.GetByTokenHashAsync(string sha256Hex)` (deterministic SHA-256 hex lookup against the `token_hash` column â€” see Q2 / Task 3.3) + `RevokeFamilyAsync(Guid familyId, RefreshTokenRevokeReason reason)`; **`IEmailVerificationTokenRepository.FindActiveAsync(Guid userId, string code, EmailVerificationPurpose purpose, DateTimeOffset now)`** returns the row where `user_id=@userId AND code=@code AND purpose=@purpose AND used_at IS NULL AND expires_at > @now AND failed_attempts < 5` (Q1 decision â€” do NOT add a `FindByCodeAndPurpose` method that would allow cross-user matches). **v7 SEAM PATCH NOTE (Tasks 3.2↔3.4): `FindActiveAsync` returning opaque-null on every miss is correct for the happy path but is INSUFFICIENT for 3.4 DoD line 26 — verify-email must (a) INCREMENT `failed_attempts` on a wrong code and (b) distinguish `AUTH_OTP_EXPIRED` (400) from `AUTH_OTP_INVALID` (400), neither of which an opaque null can express. The fix is a COMPANION method `Task<EmailVerificationToken?> FindByCodeAsync(Guid userId, string code, EmailVerificationPurpose purpose, CancellationToken ct = default)` that returns the row matched ONLY by `user_id=@userId AND code=@code AND purpose=@purpose AND used_at IS NULL` (NO `expires_at`/`failed_attempts<5` filters) so the 3.4 handler can read `ExpiresAt`/`FailedAttempts` itself, then branch (expired→`AUTH_OTP_EXPIRED`; else→increment + `AUTH_OTP_INVALID`). It stays user-scoped, so it does NOT reintroduce the cross-user-match risk Q1 guards against. This method is ADDED by Task 3.4 (3.2 is frozen-as-shipped); `FindActiveAsync` is retained as-is for any future single-shot lookup. Owned-files/forbidden-scope fences above + on Task 3.4 are aligned so the re-implement does not breach scope.** `AddInfrastructure()` registers repos; build 0W/0E; `dotnet format` clean |
| source citations | `db-schema/identity-user/schema.sql` (tables + indexes + CHECKs, lines 140â€“279); BSOT 3.2.1 Infrastructure layout (lines 457â€“484), 3.2.4 repository pattern (lines 557+); AGENTS.md Cross-DB FK forbidden â€” logical FK only; `VietRideDbContextBase` already maps outbox + snake_case (`libs/dotnet/VietRide.Shared.Persistence/VietRideDbContextBase.cs`); ef-migration SKILL.md |

### Task 3.3 â€” Auth infrastructure: BCrypt hasher + RS256 token signer + JWKS provider + email-OTP provider stub
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none) |
| owned files (write set) | `apps/identity/src/VietRide.Identity.Application/Abstractions/IPasswordHasher.cs`; `.../Abstractions/IAccessTokenService.cs`; `.../Abstractions/IRefreshTokenFactory.cs`; `.../Abstractions/IJwksProvider.cs`; `.../Abstractions/ExternalClients/IEmailService.cs`; `apps/identity/src/VietRide.Identity.Infrastructure/Security/BCryptPasswordHasher.cs`; `.../Security/RsaAccessTokenService.cs`; `.../Security/JwksProvider.cs`; `.../Security/RefreshTokenFactory.cs`; `.../Security/JwtSigningOptions.cs`; `.../ExternalClients/LoggingEmailService.cs`; `apps/identity/src/VietRide.Identity.Api/appsettings.Development.json` (placeholder dev keypair config only); `apps/identity/src/VietRide.Identity.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs` (extend registration); `apps/identity/tests/VietRide.Identity.UnitTests/Security/RsaAccessTokenServiceTests.cs`; `.../Security/BCryptPasswordHasherTests.cs` |
| forbidden scope | `.env` and real secrets (use dev-only placeholder PEM in `appsettings.Development.json`, document the env var name in the plan output, NOT commit a production key); `appsettings.json` (prod file â€” leave keys empty / env-driven); Domain layer; other services; git ops; do NOT add a JWT/RSA package beyond CPM (`Microsoft.IdentityModel.Tokens` 7.7.1 + `System.IdentityModel.Tokens.Jwt` 7.7.1 already present â€” reuse); BCrypt.Net-Next added in 3.0; **do NOT add `SendGrid` NuGet to Identity csproj; do NOT add `@sendgrid/mail` anywhere â€” SendGrid lives in Notification only (BSOT Â§1.2 line 93, Â§3.5 line 419, line 471)** |
| depends on | 3.0 |
| parallel-safe | no (serial after 3.2; shares `InfrastructureServiceCollectionExtensions.cs` write set with 3.2) |
| invariant flags | **BCrypt cost 12** (hardcoded constant); RS256 only (HS256 is Internal JWT, separate concern); access-token claims include iss=vietride-identity, sub, role, operatorId, email, iat, exp, kid; TTL 15min; refresh token = opaque UUID, store **hash** not raw; JWKS exposes public key only (n,e,kid,kty=RSA,alg=RS256,use=sig); email provider is a stub (log/no-op, no real SendGrid SDK call); CRLF; CancellationToken on async |
| acceptance | `BCryptPasswordHasher.Hash` uses workFactor 12, `Verify` roundtrips (BCrypt is reserved for `users.password_hash` ONLY â€” not refresh tokens, not OTPs, not delivery tokens); `RsaAccessTokenService` issues RS256 JWT with required claims + `kid` header matching `JwksProvider` output; `IJwksProvider` returns one JWKS key; **`IRefreshTokenFactory.Create(Guid userId, Guid? parentTokenId, Guid? familyId)` returns `(string rawToken, RefreshToken entity)`: raw = 32 hex chars from `Guid.NewGuid().ToString("N")` (or 32-byte base64url from `RandomNumberGenerator.GetBytes`); entity.`token_hash` = `Convert.ToHexString(SHA256.HashData(Encoding.UTF8.GetBytes(rawToken))).ToLowerInvariant()` (64 chars). Raw token is returned to the caller ONCE and never persisted â€” see Q2 decision** (deterministic SHA-256 hex enables the `uq_refresh_tokens_token_hash` index lookup that BCrypt-salted hash cannot support); **`IEmailService` interface signature MUST match v7 lines 234â€“239 exactly: `Task SendOtpAsync(string to, string code, EmailOtpPurpose purpose, int ttlMinutes, CancellationToken ct = default)` AND `Task SendParcelDeliveryLinkAsync(string to, string deliveryToken, ParcelDeliveryEmailDto parcelInfo, CancellationToken ct = default)`. `EmailOtpPurpose` enum `{REGISTRATION, PASSWORD_RESET}` lives in `Application/Abstractions/ExternalClients/` (contract enum, not domain). `ParcelDeliveryEmailDto` may be declared as a forward-compat record OR omitted â€” but the Day-3 stub MUST `throw new NotImplementedException("Parcel delivery email â€” Day 26+")` from `SendParcelDeliveryLinkAsync` to avoid premature coupling**; **`LoggingEmailService : IEmailService`** in Infrastructure logs OTP via injected `ILogger<LoggingEmailService>` (`LogInformation` with code, email, purpose, ttl) and returns `Task.CompletedTask` â€” NO SendGrid SDK reference, NO HTTP call; DI registers `services.AddSingleton<IEmailService, LoggingEmailService>()`; reuses CPM `Microsoft.IdentityModel.Tokens`; unit tests cover sign+verify roundtrip + bcrypt verify + SHA-256 refresh-token roundtrip; **v7 RE-HOME (ComputeHash): the blocked 3.4 attempt added `string ComputeHash(string rawToken)` to `IRefreshTokenFactory` + `RefreshTokenFactory` (deterministic SHA-256 hex, same algo as storage) so the refresh handler can hash an incoming raw token for the `uq_refresh_tokens_token_hash` lookup + reuse-detection. This member belongs on this factory and both files are ALREADY in Task 3.3’s owned-files set — so it is IN-SCOPE for 3.3, KEEP. It is re-homed here (not 3.4): if the member is not present when 3.4 re-implements, Task 3.3 is the owner that adds it (3.4 only CALLS it). Verified on disk: `RefreshTokenFactory.cs:54` + `IRefreshTokenFactory.cs:41`.** build clean; `dotnet format` clean |
| source citations | BSOT 6.1 token table (lines 1390â€“1398), 6.2 access-token claims + JWKS (lines 1400â€“1422), 6.3 refresh rotation (lines 1424â€“1430); AGENTS.md BCrypt cost 12, User Access Token RS256 issuer vietride-identity audience vietride-api; schema `password_hash` comment bcrypt cost 12 (line 184); v7 `IEmailService` interface (lines 211â€“268, signature 234â€“239, business abstraction independent of SendGrid SDK 254â€“255); BSOT Â§1.2 line 93 + Â§3.5 lines 419/471 + line 2191 (SendGrid lives in Notification only); BSOT Â§6.1 line 1391 (refresh = opaque random UUID, DB lookup) + Â§6.3 lines 1424â€“1430 (rotate + reuse detection) + schema line 225 (`uq_refresh_tokens_token_hash` needs deterministic hash â†’ SHA-256 hex per Q2); API contract JWKS shape (lines 118â€“136); CPM `Microsoft.IdentityModel.Tokens` 7.7.1 (`Directory.Packages.props` lines 32â€“33) |

### Task 3.4 â€” Application CQRS features + Api controllers for auth endpoints
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | `apps/identity/src/VietRide.Identity.Application/Features/Auth/Register/*` (Command, Handler, Validator, Dto); `libs/dotnet/VietRide.Shared.Kernel/ValueObjects/PhoneNumber.cs` (widen regex to `^\+84\d{9,10}$` + add `Normalize(string)` factory per Q3 â€” verified current file is `+84\d{9}` exact, with no normalize); `tests/dotnet/VietRide.Shared.Kernel.UnitTests/PhoneNumberTests.cs` (lockstep test-suite update per Q3 widen â€” see acceptance); `.../Features/Auth/VerifyEmail/*`; `.../Features/Auth/Login/*`; `.../Features/Auth/Refresh/*`; `.../Features/Auth/Logout/*`; `.../Features/Auth/GetJwks/*`; `apps/identity/src/VietRide.Identity.Api/Controllers/AuthController.cs`; `.../Controllers/JwksController.cs`; `apps/identity/src/VietRide.Identity.Api/Program.cs` (register MediatR + behaviors + infra DI); `apps/identity/tests/VietRide.Identity.IntegrationTests/Api/AuthEndpointsTests.cs`; `apps/identity/tests/VietRide.Identity.UnitTests/Application/Auth/*`; **v7 RE-HOMED (out-of-fence edits from the blocked 3.4 attempt — KEEP, now explicitly owned by 3.4)**: `apps/identity/src/VietRide.Identity.Application/Abstractions/Repositories/IEmailVerificationTokenRepository.cs` + `apps/identity/src/VietRide.Identity.Infrastructure/Persistence/Repositories/EmailVerificationTokenRepository.cs` (**ADD the `FindByCodeAsync` companion method AND the `FindLatestPendingAsync(Guid userId, EmailVerificationPurpose purpose, CancellationToken ct)` method [v7.2 Q-v7.1 option (b) — returns the most-recent `used_at IS NULL` token for that user+purpose, NO code match] — do NOT alter the shipped `FindActiveAsync`; this is the delegated 3.2-seam edit, ADD-only**); `apps/identity/src/VietRide.Identity.Application/Abstractions/IOtpRateLimiter.cs` (NEW external-boundary abstraction for the OTP rate-limit, BSOT §6.9); `apps/identity/src/VietRide.Identity.Infrastructure/Security/RedisOtpRateLimiter.cs` (NEW — `StackExchange.Redis` impl of `IOtpRateLimiter`, key `identity:otp_rate:{email}` TTL 1h, throws `AUTH_OTP_RATE_LIMIT_EXCEEDED`/429 on >3); `apps/identity/src/VietRide.Identity.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs` (**register `IOtpRateLimiter`→`RedisOtpRateLimiter` + the Redis `IConnectionMultiplexer` singleton via `REDIS_URL`; this file is shared with 3.2/3.3 which are now frozen — append registrations only, do NOT touch existing lines**); `apps/identity/src/VietRide.Identity.Infrastructure/VietRide.Identity.Infrastructure.csproj` + `apps/identity/src/VietRide.Identity.Api/VietRide.Identity.Api.csproj` (`<PackageReference Include="StackExchange.Redis" />` — **packageless, version is the existing `<PackageVersion>` 2.8.24 added in Task 3.0; do NOT add a `Version=` attribute**); `apps/identity/tests/VietRide.Identity.IntegrationTests/HealthEndpointTests.cs` (update for the `AddInfrastructure(IConfiguration)` boot signature from Task 3.3 — test-harness adjustment only) |
| forbidden scope | `.env`, secrets; `apps/gateway/**` (routes already exist â€” DO NOT edit `routes.ts`); Domain entity internals (use methods); Infrastructure impl files (depend on interfaces from 3.2/3.3); other services; git ops; controllers stay thin (no business logic, no DbContext) â€” must call `MediatR.Send`; no NEW packages **(exception: `StackExchange.Redis` is already in CPM from Task 3.0 — adding a packageless `<PackageReference>` to the two identity csproj is in-scope; adding any OTHER package is forbidden)**. **v7 carve-outs to the "Infrastructure impl files / Domain internals" fence**: 3.4 MAY now edit the specific re-homed files listed in owned-files above (the `FindByCodeAsync` method-add on the EmailVerificationToken repo pair, the new `IOtpRateLimiter`/`RedisOtpRateLimiter`, the two csproj, the DI extension append, and `HealthEndpointTests.cs`). 3.4 MAY also edit the two SHARED-LIB files re-homed to **Task 3.5** ONLY IF Task 3.5 has not already landed them — see Task 3.5 (it owns `VietRide.Shared.Application/Exceptions/ApplicationExceptions.cs` + `VietRide.Shared.Web/Filters/ProblemDetailsExceptionFilter.cs`); 3.4 treats those two as READ-ONLY dependencies once 3.5 lands. Everything else in Infrastructure/Domain remains forbidden (do NOT re-open 3.1/3.2/3.3 files except the explicitly re-homed `FindByCodeAsync` edit). **`IRefreshTokenFactory.ComputeHash` (added during the blocked attempt) is re-homed to Task 3.3** — if 3.3 has not re-landed it, 3.4 STOPS and reports rather than editing the frozen 3.3 files; see Task 3.3 v7 note. |
| depends on | 3.0, 3.1, 3.2, 3.3, **3.6** (v7.3: 3.4 FINAL reviewer-APPROVE is gated on the `IUnitOfWork` flush working end-to-end — the persisted-across-scope behaviour Task 3.6 delivers; 3.4 may be RE-IMPLEMENTED before 3.6 lands but MUST NOT receive final APPROVE until 3.6 is reviewer-APPROVED) |
| parallel-safe | no |
| invariant flags | Controller thin to `MediatR.Send` only (BSOT 3.2 â€” no direct service/DbContext); ProblemDetails via shared filter (no hand-rolled error JSON); `errorCode` UPPER_SNAKE_CASE from BSOT 5.9; `[ProducesResponseType]` per documented status; register/verify/login/refresh/jwks are public (Internal JWT exempt â€” match Gateway `authRequired:'none'`); logout requires auth; `CancellationToken` threaded; CRLF; naming `<Verb><Aggregate>Command/Handler/Validator` |
| acceptance | Each feature has Command/Query + Handler + (Validator for mutations) + Dto; `AuthController` actions map to contract paths/shapes exactly (register 201, verify-email 200, login 200, refresh 200, logout 204); `JwksController` GET `/.well-known/jwks.json` 200; OTP rate-limit (max 3/h per email, Redis key `identity:otp_rate:{email}` TTL 1h, BSOT Â§6.9) + brute-force (OTP `failed_attempts` invalidated after 5) honored. **OTP generation (Q1 decision)**: codes are 6-digit numeric from `RandomNumberGenerator.GetInt32(0, 1_000_000).ToString("D6")` (cryptographic random â€” NEVER `System.Random`); INSERT wrapped in try/catch on `DbUpdateException` from `uq_email_verification_tokens_code_purpose` unique violation â†’ retry up to 3 times with a fresh code; after 3 collisions log and throw. **OTP verify (Q1 + v7 seam fix B3/B4)**: the verify-email handler MUST use the COMPANION lookup `IEmailVerificationTokenRepository.FindByCodeAsync(user.Id, code, purpose, ct)` (matched by `user_id+code+purpose AND used_at IS NULL` ONLY — still user-scoped to prevent cross-user match, NO expiry/attempts filter) so it can read the row and branch explicitly: (1) **no row found** → return 400 `AUTH_OTP_INVALID` via `BadRequestException` (cannot increment — there is no row to increment); (2) **row found but `expires_at <= _clock.UtcNow`** → return 400 `AUTH_OTP_EXPIRED` via `BadRequestException` (do NOT increment `failed_attempts` for an expired code — expiry is not a wrong-code attempt); (3) **row found, not expired, but `failed_attempts >= 5`** → the row is already burned → return 400 `AUTH_OTP_INVALID` (per BSOT §6.9 line 1546 "invalidate sau 5 lần"); (4) **row found, not expired, `failed_attempts < 5`, code correct** → success: mark `used_at`, call `User.VerifyEmail()`, return 200 `{userId,status:"ACTIVE"}`. Because `FindByCodeAsync` matches on `code` equality, a "wrong code" yields case (1) (no row) — to satisfy DoD line 26 "increments `failed_attempts`", the handler MUST, on a wrong-code miss, increment the failed counter of the user’s active outstanding token, then return `AUTH_OTP_INVALID`; and when that increment reaches 5 the row is burned. **v7.2 RESOLVED (Q-v7.1 = option (b)):** the increment target is the **newest pending token for the user+purpose**, obtained via the dedicated repo method `IEmailVerificationTokenRepository.FindLatestPendingAsync(user.Id, purpose, ct)` (returns the most-recent `used_at IS NULL` token for that `user_id`+`purpose`, with NO code match — so a wrong-code attempt still has a row to increment). Concrete behavior the re-implement MUST satisfy: verify-email does its success/expiry branching via exact-match `FindByCodeAsync` (cases (1)–(4) above); on a **wrong code** (case (1), no exact-match row) the handler calls `FindLatestPendingAsync(user.Id, purpose, ct)`, increments that row’s `failed_attempts`, and returns 400 `AUTH_OTP_INVALID` via `BadRequestException`; when that increment reaches `failed_attempts >= 5` the row is invalidated/disqualified (the burned row then yields case (3) on subsequent attempts). An **expired** exact-match (case (2)) returns 400 `AUTH_OTP_EXPIRED` and does NOT increment. (Schema `email_verification_tokens.failed_attempts` is a per-row INT, lines 241–260, which option (b) honors.) This is no longer implementer’s choice and is no longer BLOCKED — proceed once Task 3.5 has landed. **Do NOT echo the user-supplied `code` or `purpose` into the ProblemDetails `detail` (should-fix: avoid reflecting attacker input).** The retained `FindActiveAsync` is NOT used by verify-email anymore. **OTP rate-limit (v7 B2 — 429; v7.1 wording fix B1+B3)**: BEFORE generating/sending an OTP (register + any resend), the handler calls `IOtpRateLimiter.TryIncrementAsync(email, ct)` (on-disk interface signature `Task<bool> TryIncrementAsync(string email, CancellationToken ct = default)`; Redis `identity:otp_rate:{email}`, max 3/h TTL 1h, BSOT §6.9 line 1545). **If `TryIncrementAsync` returns `false` (rate limit exceeded), the handler throws `TooManyRequestsException("AUTH_OTP_RATE_LIMIT_EXCEEDED", ...)` → mapped to HTTP 429 by `ProblemDetailsExceptionFilter`’s 429 arm** (the NEW `TooManyRequestsException` + the new code, both ratified by Task 3.5 — NOT `ConflictException`/409, NOT `AUTH_OTP_INVALID`/400). The 429 path has a dedicated `[ProducesResponseType(429)]` annotation + at least one test asserting status 429 + `errorCode == "AUTH_OTP_RATE_LIMIT_EXCEEDED"`. **Phone validation + normalization (Q3)**: `RegisterCommandValidator` requires `Phone` `NotEmpty` and matches `^0[0-9]{9,10}$` OR `^\+84[0-9]{9,10}$`; other forms â†’ **400 `AUTH_PHONE_INVALID_FORMAT` (v7 B1 FIX: this MUST be raised as `BadRequestException("AUTH_PHONE_INVALID_FORMAT", ...)` → 400; the blocked attempt wrongly used `ConflictException` → 409. Bad format is a 400; only a duplicate phone is 409 `AUTH_PHONE_ALREADY_REGISTERED`. BSOT §5.9 line 1263 = 400)**. Handler normalizes: strip leading `0` if local, prepend `+84`. **Reuse the `PhoneNumber` VO at `libs/dotnet/VietRide.Shared.Kernel/ValueObjects/PhoneNumber.cs`** â€” current `Parse` accepts only `^\+84\d{9}$` (exactly 9 digits) and has no normalize factory, so the libs change is in-scope: widen the regex to `^\+84\d{9,10}$` (match schema CHECK line 161) AND add `public static PhoneNumber Normalize(string input)` that accepts local-or-E.164 and returns the E.164 VO (throw `ArgumentException` on garbage). The libs file is in Task 3.4 owned files. **Lockstep test-suite update (mandatory â€” Day 3 DoD "tests pass")**: the worker MUST update `tests/dotnet/VietRide.Shared.Kernel.UnitTests/PhoneNumberTests.cs` in the same change as the VO regex widen: (1) **Remove** the `[InlineData("+849012345678")]` line (10-digit-after-`+84`) from `Parse_Invalid_Throws_ArgumentException` â€” under the widened regex it is now valid; (2) **Extend** the EXISTING `Parse_Valid_E164_Vn_Phone_Succeeds` theory (already present at line 13 of `PhoneNumberTests.cs` covering 9-digit inputs like `+84901234567`) by adding `[InlineData("+849012345678")]` to its inline data list â€” do NOT create a new method with the same name (duplicate method = compile error). The existing method name is reused and gains 10-digit coverage; (3) **Shift** the existing "too long" invalid case to an 11-digit-after-`+84` input (e.g. `+8490123456789`) so the over-length failure expectation still has coverage; (4) **Add** new tests for the `Normalize(string)` factory: `Normalize("0901234567") == "+84901234567"` (local 9-digit â†’ E.164), `Normalize("09012345678")` returns a valid E.164 (local 10-digit), `Normalize("+84901234567")` is idempotent (already E.164 â†’ returned unchanged), and `Normalize(null)` / `Normalize("")` / `Normalize("garbage")` throw `ArgumentException`; (5) after the change, `dotnet test libs/dotnet/VietRide.Libs.sln` MUST be **green** including these new + updated cases (Day 3 DoD "build 0W/0E + tests pass"). **Phone unique check (Q3)**: before insert, `IUserRepository.GetByPhoneAsync(normalizedE164)` â€” if a row exists return 409 `AUTH_PHONE_ALREADY_REGISTERED` (BSOT Â§5.9 line 1261). DB only stores E.164. refresh rotation + reuse-detection produce documented codes; **login handler: on `User.Status == LOCKED` returns 403 Forbidden with `errorCode = AUTH_ACCOUNT_LOCKED` (BSOT Â§5.9 line 1257 â€” 403, NOT 423; matches DoD line 27 + Contract changes line 35); on wrong password calls `User.RecordFailedLogin(IClock)` (which auto-locks at attempt 5 per Task 3.1, lock is permanent until admin unlock); on success calls `User.ResetFailedLogins()` + `User.RecordSuccessfulLogin()`**; `Program.cs` MODIFIED (not created â€” file already exists per carry-over) to register MediatR + pipeline behaviors (via `AddVietRideMediatRBehaviors`) + `AddInfrastructure` â€” do NOT overwrite existing `AddVietRideSharedWeb`/`AddVietRideDbContext` wiring; at least 1 happy + 1 error test per endpoint; integration test runs full register-verify-login-refresh-logout; **v7 should-fixes (fold into acceptance — re-implement must satisfy all)**: (SF1) the register action returns 201 via `StatusCode(StatusCodes.Status201Created, dto)` (or `Created(string.Empty, dto)`) — **NOT `CreatedAtAction(...)`** (no GET-by-id route exists to name, and the blocked attempt’s `CreatedAtAction` produced a broken/empty Location); (SF2) **one class per file** for the request records — each endpoint’s request DTO/record lives in its own `.cs` file under that feature folder (BSOT §3.5 naming; do NOT co-locate multiple request records in a single controller or shared file); (SF3) ProblemDetails `detail` strings MUST NOT echo user-supplied `purpose`, `code`, `email`, or `phone` back to the caller (avoid reflecting attacker-controlled input); (SF4) all five auth error paths assert the EXACT `errorCode` + HTTP status from the v7 Contract-changes table, including the new 429 `AUTH_OTP_RATE_LIMIT_EXCEEDED` and the 400 `AUTH_PHONE_INVALID_FORMAT`/`AUTH_OTP_EXPIRED`/`AUTH_OTP_INVALID` distinctions; build 0W/0E; `dotnet format` clean |
| source citations | `VietRide_API_Contract_v1.md` Identity section (register 18â€“40, verify-email 42â€“61, login 63â€“90, refresh 92â€“103, logout 105â€“116, jwks 118â€“136); BSOT 3.2 controller-MediatR rule (lines 373â€“376, 547â€“548), 3.2.1 Features layout (lines 432â€“444), 5.9 error registry (lines 1253â€“1266), 6.9 OTP 2-layer protection (lines 1542â€“1545), 6.3 refresh reuse detection (lines 1424â€“1430), 6.4 exemptions incl. register/jwks (lines 1464â€“1470); v7 register/login/OTP flow (line 454), phone REQUIRED + UNIQUE rule (lines 1309 + 4720); BSOT Â§6.1 line 1398 (6-digit numeric OTP, 5 min TTL â€” Q1); schema line 161 (E.164 CHECK regex) + line 173 (partial unique on phone) + line 254 (`uq_email_verification_tokens_code_purpose` full unique â†’ mandates `RegisterHandler` retry-on-conflict per Q1); BSOT Â§5.9 line 1261 (`AUTH_PHONE_ALREADY_REGISTERED`); existing VO `libs/dotnet/VietRide.Shared.Kernel/ValueObjects/PhoneNumber.cs` (current state verified â€” regex `+84\d{9}` only, needs widen + `Normalize` per Q3); existing test suite `tests/dotnet/VietRide.Shared.Kernel.UnitTests/PhoneNumberTests.cs` (current state: strict `^\+84\d{9}$` regex, no `Normalize` tests â€” must be updated in lockstep with the VO change per Day 3 DoD "tests pass"); add-endpoint SKILL.md; Gateway routes already present (`apps/gateway/src/config/routes.ts` lines 28â€“35); **v7**: BSOT §6.9 line 1545 (OTP rate-limit Redis `identity:otp_rate:{email}` max 3/h → backs `IOtpRateLimiter`/`RedisOtpRateLimiter` + the 429 path); new code `AUTH_OTP_RATE_LIMIT_EXCEEDED` registered by Task 3.5 (BSOT §5.9); BSOT §6.9 line 1546 (OTP `failedAttempts` invalidate after 5 → verify-email branching B3/B4); BSOT §5.9 line 1263 (`AUTH_PHONE_INVALID_FORMAT` = 400 → B1); shared exceptions `UnauthorizedException`/`BadRequestException` at `libs/dotnet/VietRide.Shared.Application/Exceptions/ApplicationExceptions.cs` lines 53–73 (owned/ratified by Task 3.5) |

### Task 3.5 — (v7 NEW) Register `AUTH_OTP_RATE_LIMIT_EXCEEDED` (429) in BSOT §5.9 + changelog; re-home the shared-lib exception edits
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer (or /code-review) |
| skill | (none) |
| owned files (write set) | `BACKEND_SOURCE_OF_TRUTH.md` (§5.9 Auth group — add the one error-code row; §13 changelog — add ONE entry, bump version MINOR per §13 rule "thêm code mới → bump MINOR"); `libs/dotnet/VietRide.Shared.Application/Exceptions/ApplicationExceptions.cs` (**re-home the KEEP edit: confirm `UnauthorizedException` (401) + `BadRequestException` (400) classes are present + correct — the blocked 3.4 attempt already added them; this task formally OWNS + ratifies them. **v7.1 ADD (B1):** also DEFINE the new 429-capable exception `public sealed class TooManyRequestsException(string errorCode, string message) : Exception(message) { public string ErrorCode { get; } = errorCode; }` in this same file — no 429-mappable exception exists yet, so the OTP rate-limit path in Task 3.4 cannot reach 429 without it (the blocked attempt wrongly reused `ConflictException` → 409)**); `libs/dotnet/VietRide.Shared.Web/Filters/ProblemDetailsExceptionFilter.cs` (**re-home the KEEP edit: confirm both new exceptions map to 400/401 — already done in the blocked attempt; ratify here. **v7.1 ADD (B1):** also ADD the filter arm `TooManyRequestsException t => (429, t.ErrorCode, t.Message, null),` so `TooManyRequestsException` maps to HTTP 429 with `errorCode` propagated**) |
| forbidden scope | `.env`, secrets; any `apps/**` service code (Task 3.4 owns the Identity handler that THROWS the new code); do NOT add the Redis limiter here (that is 3.4); do NOT alter any OTHER §5.9 row or BSOT section beyond §5.9 Auth + §13; git ops; no new packages |
| depends on | — (independent of 3.0–3.3; MUST land BEFORE the 3.4 re-implement that references the code) |
| parallel-safe | yes (disjoint write set from all other Day-3 tasks — BSOT doc + two shared-lib files that no live task touches; 3.4 treats those two libs files as read-only deps once this lands) |
| invariant flags | error `errorCode` UPPER_SNAKE_CASE; new code MUST be in the registry BEFORE any handler uses it (BSOT §0 rule line 49 + §5.9 header line 1249); CRLF on `.cs`; LF on `.md`; BSOT changelog is mandatory for any registry change; do NOT introduce a duplicate code or change an existing HTTP mapping |
| acceptance | BSOT §5.9 Auth group gains exactly one row: `\| \| \`AUTH_OTP_RATE_LIMIT_EXCEEDED\` \| 429 \| OTP request rate limit (Redis `identity:otp_rate:{email}` max 3/h) exceeded \|` (placed in the Auth group, after the existing `AUTH_OTP_EXPIRED`/`AUTH_PHONE_*` rows); BSOT §13 changelog gains ONE new top row with today’s date (2026-06-01), author "BE lead (Vũ)", PATCH/MINOR bump per §13, text noting the new code + that it backs the Day-3 OTP rate-limit path (Task 3.4) + decision ref B2; `ApplicationExceptions.cs` contains `public sealed class UnauthorizedException` (ErrorCode, → 401) + `public sealed class BadRequestException` (ErrorCode, → 400), both `sealed`, ctor `(string errorCode, string message)`; `ProblemDetailsExceptionFilter` maps `UnauthorizedException`→401 + `BadRequestException`→400 with `errorCode` propagated; **v7.1 (B1):** `ApplicationExceptions.cs` ALSO contains `public sealed class TooManyRequestsException(string errorCode, string message) : Exception(message) { public string ErrorCode { get; } = errorCode; }` (sealed, primary-ctor `(string errorCode, string message)`, exposes `ErrorCode`), and `ProblemDetailsExceptionFilter` ALSO has the arm `TooManyRequestsException t => (429, t.ErrorCode, t.Message, null),` mapping it to HTTP 429 with `errorCode` propagated; `dotnet build libs/dotnet/VietRide.Libs.sln -c Release` 0W/0E; `dotnet format --verify-no-changes` clean on the libs sln; no other §5.9 row changed |
| source citations | BSOT §5.9 registry (lines 1247–1266, Auth group); BSOT §6.9 line 1545 (OTP rate-limit Redis `identity:otp_rate:{email}` max 3/h TTL 1h — the rule the code names); BSOT §0 line 49 + §5.9 header line 1249 ("thêm code mới → bump MINOR doc version + thêm vào registry"); BSOT §13 changelog table (lines 2601–2612, format of an entry); human decision B2 (this patch pass); existing shared exceptions verified on disk (`libs/dotnet/VietRide.Shared.Application/Exceptions/ApplicationExceptions.cs` lines 53–73 + `ProblemDetailsExceptionFilter.cs` lines 50–55 — `UnauthorizedException`/`BadRequestException` present + mapped) |

### Task 3.6 — (v7.3 NEW) Implement `EfUnitOfWork : IUnitOfWork` in shared persistence + wire it into `AddVietRideDbContext` (fixes the Day-3-wide TransactionBehavior no-op seam)
| Field | Value |
|---|---|
| stack/owner | dotnet (cross-cutting shared lib) |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none) |
| owned files (write set) | `libs/dotnet/VietRide.Shared.Persistence/UnitOfWork/EfUnitOfWork.cs` (**NEW** — thin wrapper over `VietRideDbContextBase`); `libs/dotnet/VietRide.Shared.Persistence/DependencyInjection/PersistenceServiceCollectionExtensions.cs` (**append ONE scoped `IUnitOfWork`->`EfUnitOfWork` registration inside the existing `AddVietRideDbContext<TContext>`** — do NOT alter the existing `AddDbContext`/`VietRideDbContextBase`/`IOutboxStore` lines); OPTIONAL persistence-proof test under `apps/identity/tests/VietRide.Identity.IntegrationTests/**` (append a test only — see acceptance A4 + Open question Q-v7.3.1). **NO per-service Identity src edit is needed** — Identity already calls `AddVietRideDbContext<IdentityDbContext>(builder.Configuration)` (`apps/identity/src/VietRide.Identity.Api/Program.cs:28`, VERIFIED), so the shared registration covers it; do NOT edit `Program.cs`/composition root. |
| forbidden scope | `.env`, secrets; **do NOT alter `IUnitOfWork` (`libs/dotnet/VietRide.Shared.Application/UnitOfWork/IUnitOfWork.cs`) or `TransactionBehavior` (`.../Behaviors/TransactionBehavior.cs`) — they are CORRECT; only the IMPLEMENTATION + REGISTRATION are missing**; do NOT re-open or re-touch Tasks 3.0–3.3 / 3.5 acceptance or owned files (frozen-as-shipped); do NOT edit `VietRideDbContextBase.cs` (the wrapper composes over it, does NOT modify it); do NOT add ANY package (CPM frozen for this task — no Testcontainers/Respawn/EFCore.InMemory/Sqlite); do NOT edit any of the 4 other services (`apps/trip|booking|payment|parcel`) — the shared registration reaches them automatically and is additive-only; do NOT edit `apps/identity/src/**` (only `apps/identity/tests/VietRide.Identity.IntegrationTests/**` IF the proof test lands there); `apps/gateway|tracking|notification|rag`; git branch/commit/push |
| depends on | 3.0 (the `IUnitOfWork` interface + `TransactionBehavior` + CPM EF packages already shipped under 3.0; 3.2 shipped `IdentityDbContext : VietRideDbContextBase`). Independent of 3.1/3.3/3.5. |
| parallel-safe | no (3.4 final approve depends on it; its write set — the new `EfUnitOfWork.cs` + the persistence DI extension — is disjoint from the live 3.4 re-implement files, so it MAY be implemented before/alongside the 3.4 re-impl and MUST land before 3.4 final reviewer-APPROVE) |
| invariant flags | CRLF on `.cs`; CPM no `Version=` (add NO package); `EfUnitOfWork` ctor takes `VietRideDbContextBase` (the already-registered shared base context, `PersistenceServiceCollectionExtensions.cs:34`), NOT a service-specific `TContext` (keeps it service-agnostic); the four members delegate to the context + `Database.BeginTransactionAsync`/current `IDbContextTransaction`; scoped lifetime (matches DbContext scope); `CancellationToken` threaded on every async method; nullable enabled; one class per file |
| acceptance | (A1) `EfUnitOfWork.cs` exists: `public sealed class EfUnitOfWork(VietRideDbContextBase db) : IUnitOfWork` (or classic ctor) implementing all four members — `SaveChangesAsync(ct)` -> `db.SaveChangesAsync(ct)`; `BeginTransactionAsync(ct)` -> store `await db.Database.BeginTransactionAsync(ct)`; `CommitAsync(ct)` -> `SaveChangesAsync(ct)` then commit the stored transaction (a Begin->Commit cycle flushes tracked changes) then dispose/null it; `RollbackAsync(ct)` -> rollback + dispose the stored transaction; Commit/Rollback with no active transaction is a safe no-op or throws a clear `InvalidOperationException` (implementer judgment, documented). (A2) `AddVietRideDbContext<TContext>` registers `services.AddScoped<IUnitOfWork>(sp => new EfUnitOfWork(sp.GetRequiredService<VietRideDbContextBase>()))` (reuses the `VietRideDbContextBase` scoped registration already at line 34) — appended, existing lines unchanged. (A3) After this lands, `TransactionBehavior<,>` is NO LONGER a no-op for Identity: `_unitOfWork` resolves, so the Begin/Commit path (lines 41–47) executes and flushes the handler tracked mutations. Identity composition root resolves `IUnitOfWork` (boot the Identity host or a `WebApplicationFactory` scope and `GetRequiredService<IUnitOfWork>()` succeeds, returning `EfUnitOfWork`). (A4) **Persistence proof (DEFINITIVE gating bar — Q-v7.3.1 RESOLVED = option (b), fixed; no live-Postgres round-trip is required for reviewer-APPROVE).** The proof is the no-new-package MINIMUM and consists of exactly TWO mandatory assertions: (1) **DI resolution** — the Identity composition root resolves `IUnitOfWork` to `EfUnitOfWork` (boot the Identity host or a `WebApplicationFactory` scope and assert `GetRequiredService<IUnitOfWork>()` succeeds and returns an `EfUnitOfWork`); AND (2) **delegation** — a unit test proves `EfUnitOfWork.CommitAsync` (and/or `SaveChangesAsync`) delegates to the underlying `DbContext`'s `SaveChangesAsync` (Begin->SaveChanges->Commit order), using a substitute/mock or a test `DbContext` seam. **NO new test-DB package (NO Testcontainers / Respawn / Sqlite / EFCore.InMemory) and NO reliance on a live Postgres.** Rationale (VERIFIED on disk): the current Identity integration harness (`apps/identity/tests/VietRide.Identity.IntegrationTests/Api/AuthEndpointsTests.cs`) does NOT touch a real DB (asserts routing/validation only; conn-string is localhost:5432), EFCore.InMemory throws on `BeginTransactionAsync` (no transaction support), and adding a package is forbidden Day-3 scope. Real end-to-end cross-scope persistence via live Postgres is **explicitly OUT** of the Day-3 gating bar — it MAY be added later if/when a test-DB harness is approved (its own future scope), but it is NOT required for Task 3.6 reviewer-APPROVE. Both assertions above MUST pass for APPROVE. (Q-v7.3.1 resolved into plan body.) (A5) `dotnet build libs/dotnet/VietRide.Libs.sln -c Release` AND `dotnet build apps/identity/VietRide.Identity.sln -c Release` 0W/0E; `dotnet format --verify-no-changes` clean on both. (A6) Regression: existing **59 identity + 39 kernel** tests stay green; the 4 other services (trip/booking/payment/parcel) still build 0W/0E (the additive registration must not break them — they do not inject `IUnitOfWork`, VERIFIED). |
| source citations | `IUnitOfWork` interface (`libs/dotnet/VietRide.Shared.Application/UnitOfWork/IUnitOfWork.cs` lines 5–11, 4 members — VERIFIED); `TransactionBehavior` no-op seam (`libs/dotnet/VietRide.Shared.Application/Behaviors/TransactionBehavior.cs` lines 16–35, `IUnitOfWork? _unitOfWork` + early-return when null at lines 32–34 — VERIFIED); dependency direction OK (`libs/dotnet/VietRide.Shared.Persistence/VietRide.Shared.Persistence.csproj` ProjectReference to `VietRide.Shared.Application` — VERIFIED, impl in Persistence is legal); wiring point (`libs/dotnet/VietRide.Shared.Persistence/DependencyInjection/PersistenceServiceCollectionExtensions.cs` lines 12–38, esp. line 34 `AddScoped<VietRideDbContextBase>` — VERIFIED); shared base context (`libs/dotnet/VietRide.Shared.Persistence/VietRideDbContextBase.cs` lines 13–17, 48–58 — `SaveChangesAsync`/`SaveChanges` override + `IClock` auditing — VERIFIED); behaviors registration (`libs/dotnet/VietRide.Shared.Application/DependencyInjection/ApplicationServiceCollectionExtensions.cs:45` registers `TransactionBehavior<,>` transient — VERIFIED; only Identity calls `AddVietRideMediatRBehaviors`); Identity wiring (`apps/identity/src/VietRide.Identity.Api/Program.cs:28` — VERIFIED, no per-service edit); the 4 other services call `AddVietRideDbContext` + derive `VietRideDbContextBase` but inject NO `IUnitOfWork` (VERIFIED — additive/non-breaking); Day-3 re-review BLOCKER on Task 3.4 (Progress tracker row 3.4, this plan); AGENTS.md Persistence convention; Task 3.0 acceptance line 56. |

### Task 3.7 — (v7.5 NEW) SOT docs: rewrite BSOT §5.4/§5.5/§5.7/§5.8 to the `ApiResponse<T>` envelope + bump 1.4.0→1.5.0 + §13 changelog; wrap every API-Contract response example
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer (or /code-review) |
| skill | (none) |
| owned files (write set) | `BACKEND_SOURCE_OF_TRUTH.md` (§5.4 success-shape rewrite to envelope; §5.5 error-shape rewrite to `error{code,message,fields[]}` envelope, drop `application/problem+json`; §5.7 Pagination rewrite to `PagedResult<T>` `{items,page,pageSize,totalItems,totalPages,hasNextPage,hasPreviousPage}` + `QueryOptions` `page`/`pageSize`-clamped-1..100/`search`/`searchIn`/`sortBy`/`sortDir`/`includeDeleted`; §5.8 Filter conventions rewrite to `sortBy`+`sortDir` whitelist-validated SUPERSEDES the `?sort=-field` convention; §3 version line `1.4.0` to `1.5.0`; §13 changelog +ONE top row); `VietRide_API_Contract_v1.md` (wrap EVERY response example — success + error — in the `{success,statusCode,data\|error,meta}` envelope per ADR 0004 Rule 1) |
| forbidden scope | `.env`, secrets; **do NOT touch BSOT §5.6 (Idempotency — out of scope, on-disk line 1206)**; do NOT edit §5.9 error-code REGISTRY rows (codes unchanged — only their TRANSPORT moves from RFC7807 `errorCode` into `error.code`, described in §5.5 prose, not by editing §5.9 rows); do NOT alter `docs/adr/0004-...` (already Accepted); any `apps/**` / `libs/**` source code (Tasks 3.8–3.11 own code); git ops; no new packages |
| depends on | — (doc-only; SHOULD land first so 3.8/3.9 implement against the rewritten spec) |
| parallel-safe | yes (disjoint write set: two SOT docs only; no source-code overlap with any other task) |
| invariant flags | LF on `.md`; BSOT §13 changelog mandatory for any convention change; SemVer bump = **MINOR** (§0 line 50 "MINOR cho thêm section/registry entry"); UPPER_SNAKE_CASE error codes unchanged; camelCase JSON; `statusCode` mirrors HTTP status line (ADR 0004 Rule 2) |
| acceptance | BSOT §5.4 documents the success envelope `{success:true,statusCode,message?,data,meta{traceId,timestamp}}` (single-resource `data`=DTO; list `data`=`PagedResult<T>`); §5.5 documents the error envelope `{success:false,statusCode,error{code,message,fields?[]},meta}` and states `application/problem+json` is DROPPED + `error.code` carries the §5.9 registry code + `error.fields[]` replaces ProblemDetails `errors[]`; §5.7 documents `PagedResult<T>` (7 fields) + `QueryOptions` (page>=1 default 1, pageSize default 20 clamped max 100, search, searchIn, sortBy, sortDir in {asc,desc} default desc, includeDeleted default false); §5.8 documents `sortBy`+`sortDir` whitelist-per-aggregate (security requirement — reject non-whitelisted field) SUPERSEDING `?sort=-field`; BSOT §3 version line reads `1.5.0`; §13 changelog gains ONE top row dated 2026-06-01, author "BE lead (Vũ)", **MINOR**, text referencing ADR 0004 + the envelope rollout; `VietRide_API_Contract_v1.md` every documented response example is wrapped (success to `data`, error to `error`, both with `meta`); §5.6 (Idempotency) UNCHANGED; no §5.9 registry row edited; manual table-render check clean |
| source citations | ADR 0004 §Decision Rules 1–6 (`docs/adr/0004-api-response-envelope.md` lines 27–94), §Follow-ups #1–#2 (lines 122–123), §Decisions-resolved-at-acceptance (lines 130–133); BSOT §5.4 (line 1169), §5.5 (line 1179), §5.6 Idempotency=UNTOUCHED (line 1206), §5.7 Pagination (line 1235), §5.8 Filter conventions (line 1241), §5.9 registry (line 1247, codes unchanged), §0 SemVer rule (line 50), §13 changelog (line ~2604); reference pattern `FPT-EXE-201` `DTOs/Common` (ADR 0004 lines 7/21) |

### Task 3.8 — (v7.5 NEW) .NET shared: `ApiResponse`/`PagedResult`/`QueryOptions` types + success result-filter + `ApiResponseExceptionFilter` REPLACING `ProblemDetailsExceptionFilter` + `InvalidModelStateResponseFactory` + wire `AddVietRideSharedWeb`
| Field | Value |
|---|---|
| stack/owner | dotnet (cross-cutting shared libs) |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none) |
| owned files (write set) | **types**: `libs/dotnet/VietRide.Shared.Kernel/Primitives/ApiResponse.cs` (`ApiResponse` + `ApiResponse<T>` + `ApiError` + `ApiFieldError` + `ApiMeta` — related envelope records; per BSOT §3.2.3 balance co-locate where it aids clarity, else split); `libs/dotnet/VietRide.Shared.Kernel/Primitives/PagedResult.cs` (`PagedResult<T>` `items,page,pageSize,totalItems,totalPages,hasNextPage,hasPreviousPage`); `libs/dotnet/VietRide.Shared.Kernel/Primitives/QueryOptions.cs` (`QueryOptions` — page/pageSize-clamped-1..100/search/searchIn/sortBy/sortDir/includeDeleted). **filters**: `libs/dotnet/VietRide.Shared.Web/Filters/ApiResponseResultFilter.cs` (**NEW** — `IAlwaysRunResultFilter` auto-wrapping any `ObjectResult` success value into `ApiResponse<T>`; 204 stays empty; do NOT double-wrap an already-`ApiResponse`); `libs/dotnet/VietRide.Shared.Web/Filters/ApiResponseExceptionFilter.cs` (**NEW** — `IExceptionFilter` REPLACING `ProblemDetailsExceptionFilter`; maps each exception arm to the error envelope preserving HTTP status + `error.code`; keeps `ValidationException` to 422 (NOT 400), `TooManyRequestsException` to 429, `DomainException` to 422, `NotFoundException` to 404, `ConflictException` to 409, `ForbiddenException` to 403, `UnauthorizedException` to 401, `BadRequestException` to 400; logs structured userId(`sub`)/path/method/traceId via Serilog); `libs/dotnet/VietRide.Shared.Web/DependencyInjection/SharedWebServiceCollectionExtensions.cs` (swap `o.Filters.Add<ProblemDetailsExceptionFilter>()` to `ApiResponseExceptionFilter` + add `ApiResponseResultFilter`; add `ApiBehaviorOptions.InvalidModelStateResponseFactory` override routing model-binding 400s through the SAME error envelope); **retire**: `libs/dotnet/VietRide.Shared.Web/Filters/ProblemDetailsExceptionFilter.cs` (REMOVE — replaced; ADR 0004 Rule 5); unit tests: `tests/dotnet/VietRide.Shared.Web.UnitTests/VietRide.Shared.Web.UnitTests.csproj` **(NEW — scaffold the project; add it to `libs/dotnet/VietRide.Libs.sln`; references `VietRide.Shared.Web` + xUnit + FluentAssertions + NSubstitute from existing CPM — NO new package)** plus its test `.cs` files under `tests/dotnet/VietRide.Shared.Web.UnitTests/**` (assert wrap + each error arm + model-binding 400 shape) |
| forbidden scope | `.env`, secrets; `apps/**` service code (Task 3.10 migrates Identity tests/annotations; 3.8 does NOT edit controllers); TS / `libs/shared/**` (Task 3.9); `docs/**` (Task 3.7); other `libs/dotnet/*` projects beyond `.Kernel`/`.Web`; git ops; no new packages (use existing CPM); do NOT alter §5.9 error codes or the exception classes' `ErrorCode` contract (only their MAPPING transport changes) |
| depends on | 3.7 (SHOULD follow the rewritten BSOT §5.4/§5.5/§5.7 spec; may proceed in parallel with 3.9) |
| parallel-safe | yes vs 3.9 (disjoint: .NET libs vs TS libs). NO vs 3.7 (implements its spec) and NO vs 3.10 (3.10 asserts 3.8's output) |
| invariant flags | CRLF on `.cs`; CPM no `Version=`; ASP.NET camelCase JSON (`JsonSerializerDefaults.Web` — do NOT override); `ValidationException` to **422** (NOT 400) preserved; `statusCode` body mirrors HTTP status line (Rule 2 — never 200+`success:false`); 204 = empty body (no envelope); `errorCode` UPPER_SNAKE_CASE from §5.9; `IAlwaysRunResultFilter` for the success wrap; `InvalidModelStateResponseFactory` (preferred over a ModelState action filter, ADR 0004 Rule 5); one class/file where it aids clarity (BSOT §3.2.3 balance) |
| acceptance | `ApiResponse<T>` carries `success,statusCode,message?,data,meta{traceId,timestamp}`; `ApiResponse` (error) carries `success:false,statusCode,error{code,message,fields?},meta`; `PagedResult<T>` exposes all 7 fields with `totalPages`/`hasNextPage`/`hasPreviousPage` computed; `QueryOptions` clamps `pageSize` to 1..100 + defaults (page 1, pageSize 20, sortDir desc, includeDeleted false); `ApiResponseResultFilter` (IAlwaysRunResultFilter) wraps any `ObjectResult` success into `ApiResponse<T>` (200/201), leaves 204 empty, no double-wrap; `ApiResponseExceptionFilter` replaces `ProblemDetailsExceptionFilter` (old file REMOVED) and every prior arm maps to the error envelope with correct status + `error.code` (incl. `ValidationException` to 422 with `error.fields[]`, `TooManyRequestsException` to 429); `InvalidModelStateResponseFactory` emits the SAME error envelope for malformed-JSON/missing-field/type-mismatch (400); `meta.traceId` from gateway `X-Request-Id` (ADR 0002), `meta.timestamp` = UTC ISO-8601; `AddVietRideSharedWeb` wires the result-filter + new exception filter + the `InvalidModelStateResponseFactory`; **both filters read the correlation id for `meta.traceId` from the `X-Request-Id` header — the key set by `RequestLoggingMiddleware` (TS: `correlation-id.middleware`) — via the shared constant `RequestLoggingMiddleware.RequestIdHeader` (`libs/dotnet/VietRide.Shared.Web/Middleware/RequestLoggingMiddleware.cs:12`, also exposed on `ctx.Items[RequestIdHeader]`); do NOT hard-code the literal string a second time**; **ALL 6 .NET solutions build 0W/0E — `libs/dotnet/VietRide.Libs.sln` AND `apps/{identity,trip,booking,payment,parcel}/VietRide.{Svc}.sln` — confirming the filter REPLACEMENT (removing `ProblemDetailsExceptionFilter.cs` + changing `AddVietRideSharedWeb`, which all 5 services call) does not break the other 4 services**; `dotnet format --verify-no-changes` clean (on the libs sln + identity sln); the NEW `tests/dotnet/VietRide.Shared.Web.UnitTests` project builds, is registered in `libs/dotnet/VietRide.Libs.sln`, and its tests are green covering wrap + each error arm + model-binding 400; existing identity tests still COMPILE (their assertion migration is Task 3.10 — coordinate: 3.8 lands filters, 3.10 flips assertions). **(Q-v7.5.2: `.Kernel` vs `.Application` — recommended `.Kernel/Primitives`.)** |
| source citations | ADR 0004 §Decision Rule 5 (.NET bullet, `docs/adr/0004-api-response-envelope.md` lines 78–81), Rule 1 envelope (lines 27–62), Rule 2 status authoritative (lines 64–66), Rule 6 QueryOptions (lines 86–94), §Follow-up #3 (line 124); existing `ProblemDetailsExceptionFilter.cs` (to be REPLACED); `SharedWebServiceCollectionExtensions.cs` wiring point (`libs/dotnet/VietRide.Shared.Web/DependencyInjection/SharedWebServiceCollectionExtensions.cs` lines 16/33–35 — VERIFIED); shared exceptions (`libs/dotnet/VietRide.Shared.Application/Exceptions/ApplicationExceptions.cs`); BSOT §5.4/§5.5/§5.7 (rewritten by Task 3.7); gateway `X-Request-Id` (ADR 0002) |

### Task 3.9 — (v7.5 NEW) TS: mirror `ApiResponse`/`PagedResult`/`QueryOptions` in `libs/shared/contracts` + Nest interceptor + exception filter in `nest-common` + cross-stack contract test
| Field | Value |
|---|---|
| stack/owner | nest |
| implement agent | nest-worker |
| review agent | nest-reviewer |
| skill | (none) |
| owned files (write set) | **contracts**: `libs/shared/contracts/src/dtos/api-response.ts` (**NEW** — `ApiResponse`/`ApiResponse<T>` + `ApiError`/`ApiFieldError`/`ApiMeta` types + Zod schemas mirroring the .NET envelope); `libs/shared/contracts/src/dtos/page-result.ts` (**EXTEND in place** — current `PageResult<T>` is `{items,page,pageSize,total}`; align to the ADR `PagedResult<T>` 7-field shape `{items,page,pageSize,totalItems,totalPages,hasNextPage,hasPreviousPage}`, keep `PageResult` as alias — see Q-v7.5.3); `libs/shared/contracts/src/dtos/query-options.ts` (**NEW** — `QueryOptions` type + schema); `libs/shared/contracts/src/dtos/error-envelope.ts` (**EDIT** — current `ProblemDetailsSchema` is OLD RFC7807; retire/re-point to the `error` envelope — Q-v7.5.3); `libs/shared/contracts/src/index.ts` (export new modules); **nest-common**: `libs/shared/nest-common/src/interceptors/api-response.interceptor.ts` (**NEW**); `libs/shared/nest-common/src/filters/api-response-exception.filter.ts` (**NEW**); `libs/shared/nest-common/src/filters/problem-details-exception.filter.ts` (**RETIRE — re-point to re-export `ApiResponseExceptionFilter`, or delete if no importer**); `libs/shared/nest-common/src/index.ts` (export); `libs/shared/nest-common/src/lib/nest-common.module.ts` (wire interceptor + filter as `APP_INTERCEPTOR`/`APP_FILTER`); **contract test**: `libs/shared/contracts/src/dtos/__tests__/envelope-contract.spec.ts` (**NEW** — TS Zod schema validates a committed .NET-envelope fixture — Q-v7.5.5) |
| forbidden scope | `.env`, secrets; `apps/**` (gateway stays pass-through — ADR 0002; tracking/notification/rag not edited here); `libs/dotnet/**` (Task 3.8); `docs/**` (Task 3.7); the existing `correlation-id.middleware.ts`/`zod-validation.pipe.ts` (do NOT delete these — they are unrelated and stay protected); the RFC7807 `problem-details-exception.filter.ts` is the ONE filter this task MAY retire/re-point (it is in owned-files above); git ops; no new npm deps (zod present) |
| depends on | 3.7 (mirrors the BSOT-spec'd envelope; SHOULD align with 3.8's .NET types). Parallel-safe vs 3.8. |
| parallel-safe | yes vs 3.8 (disjoint: TS libs vs .NET libs). NO vs 3.7. The contract test asserts against a STATIC .NET fixture so it is robust to 3.8 landing in parallel. |
| invariant flags | LF on `.ts`/`.json`; camelCase JSON; envelope shape byte-identical to .NET (`{success,statusCode,message?,data\|error,meta{traceId,timestamp}}`); `statusCode` mirrors HTTP status (never 200+`success:false`); 204 empty; `error.code` UPPER_SNAKE_CASE; gateway pass-through (do NOT wrap at gateway); zod for runtime schemas (existing nest-common convention); banned deps unchanged |
| acceptance | `libs/shared/contracts` exports `ApiResponse`/`ApiResponse<T>`/`ApiError`/`ApiFieldError`/`ApiMeta` + `PagedResult<T>` (7-field) + `QueryOptions`, each with a Zod schema; `nest-common` `ApiResponseInterceptor` wraps any FE-facing controller success into the envelope (skips 204) + `ApiResponseExceptionFilter` maps Nest `HttpException`/domain errors to the error envelope with correct HTTP status + `error.code`; both wired in `nest-common.module.ts`; the cross-stack contract test passes (TS schema validates a .NET-shaped envelope fixture, field names/types match); the OLD RFC7807 `ProblemDetailsSchema`/`problem-details-exception.filter.ts` are retired/re-pointed (no dual envelope shipped — Q-v7.5.3); `npx nx run-many -t build --all --exclude="VietRide.*"` green; `npx nx run-many -t lint test --all --exclude="VietRide.*" --ci --passWithNoTests` green; prettier clean |
| source citations | ADR 0004 §Decision Rule 5 (TS bullet, `docs/adr/0004-api-response-envelope.md` line 82), §Consequences "cross-stack contract test" (line 116), Rule 1 envelope (lines 27–62), Rule 6 QueryOptions (lines 86–94); existing `libs/shared/contracts/src/dtos/error-envelope.ts` (OLD RFC7807 — to retire/re-point) + `page-result.ts` (4-field — to align to 7-field) + `index.ts` (VERIFIED); existing `libs/shared/nest-common/src/filters/problem-details-exception.filter.ts` + `interceptors/logging.interceptor.ts` + `lib/nest-common.module.ts` (VERIFIED — wiring point); gateway pass-through (ADR 0002) |

### Task 3.10 — (v7.5 NEW) Migrate Identity Day 1–3 tests to `.data`/`.error.code` + flip `[ProducesResponseType]` to the envelope type
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none) |
| owned files (write set) | `apps/identity/tests/VietRide.Identity.IntegrationTests/Api/AuthEndpointsTests.cs` (assert `.data.*` on success + `.error.code`/`statusCode` on error instead of bare-DTO / RFC7807); `apps/identity/tests/VietRide.Identity.IntegrationTests/HealthEndpointTests.cs` (keep asserting the UNWRAPPED health shape — `/health*` is EXEMPT from the envelope per resolved Q-v7.5.4); `apps/identity/tests/VietRide.Identity.UnitTests/Application/Auth/**` (handler tests asserting cross-controller response shape — adjust to envelope; pure handler-return assertions unchanged); `apps/identity/src/VietRide.Identity.Api/Controllers/AuthController.cs` (flip `[ProducesResponseType(typeof(XxxDto),200)]` to `typeof(ApiResponse<XxxDto>)` and error annotations to `typeof(ApiResponse)`; controller BODIES unchanged); `apps/identity/src/VietRide.Identity.Api/Controllers/JwksController.cs` (`/.well-known/jwks.json` is EXEMPT from the envelope per resolved Q-v7.5.1, so its `[ProducesResponseType]` stays the raw JWKS `{keys:[...]}` shape — do NOT flip it to `ApiResponse<T>`) |
| forbidden scope | `.env`, secrets; controller LOGIC/`MediatR.Send` calls (only `[ProducesResponseType]` type changes — no body edits); `libs/**` (Task 3.8/3.9 own filters+types — 3.10 only CONSUMES); `docs/**`; other services; the shared filters (read-only deps from 3.8); git ops; no new packages; do NOT change routes/status codes |
| depends on | 3.8 (envelope types + filters MUST exist before tests assert them + before `[ProducesResponseType(typeof(ApiResponse<...>))]` compiles) |
| parallel-safe | no (depends on 3.8; touches Identity test + controller files) |
| invariant flags | CRLF on `.cs`; CPM no `Version=`; controllers stay thin (attribute-only change); error assertions read `error.code` (UPPER_SNAKE_CASE) not RFC7807 `errorCode`; success assertions read `.data`; 204 logout asserts empty body; `[ProducesResponseType]` generic matches the wrapped type for Swagger accuracy |
| acceptance | every success assertion in `AuthEndpointsTests` reads from `.data` (e.g. `body.data.userId`, `.data.status`); every error assertion reads `.error.code` (== §5.9 code) + `statusCode` mirroring HTTP status; the 429 test asserts `.error.code == "AUTH_OTP_RATE_LIMIT_EXCEEDED"` + status 429; the 204 logout test asserts empty body; `AuthController`/`JwksController` `[ProducesResponseType]` reference `ApiResponse<T>` (success) / `ApiResponse` (error); `dotnet build apps/identity/VietRide.Identity.sln -c Release` 0W/0E; `dotnet test apps/identity/VietRide.Identity.sln` ALL green; `dotnet format --verify-no-changes` clean; NetArchTest layering still passes |
| source citations | ADR 0004 §Consequences "Migration touches every shipped endpoint's tests (assert `.data`/`.error.code`)" (`docs/adr/0004-api-response-envelope.md` line 118), §Follow-up #3 "migration of Day 1–3 Identity endpoints + their tests" (line 124); existing `AuthEndpointsTests.cs` + `HealthEndpointTests.cs` (current bare-DTO/RFC7807 assertions — VERIFIED via Task 3.4/3.6 progress rows); `AuthController.cs`/`JwksController.cs` `[ProducesResponseType]` (shipped under Task 3.4); the envelope types + filters from Task 3.8 |

### Task 3.11 — (v7.5 NEW) Add the success-envelope invariant line to `AGENTS.md` + the 4 worker/reviewer agent files
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer (or /code-review) |
| skill | (none) |
| owned files (write set) | `AGENTS.md` (update the "Domain conventions" `Errors:` line — responses now use the `ApiResponse<T>` envelope `{success,statusCode,data\|error,meta}`, error carries `error.code` from §5.9, NOT bare `application/problem+json`; cite ADR 0004); `.claude/agents/dotnet-worker.md`; `.claude/agents/dotnet-reviewer.md`; `.claude/agents/nest-worker.md`; `.claude/agents/nest-reviewer.md` (add the same one-line envelope reminder — ADR 0004 follow-up #4) |
| forbidden scope | `.env`, secrets; any source code / `libs/**` / `apps/**`; `docs/adr/0004-...` (already Accepted); `BACKEND_SOURCE_OF_TRUTH.md` (Task 3.7 owns the §5.x rewrite); `CLAUDE.md`; `manager.md`/`worker.md`/`reviewer.md` (only the 4 stack worker/reviewer files in scope); git ops; no new packages |
| depends on | 3.7 (the AGENTS line should reference the rewritten BSOT §5.4/§5.5; conventionally last so the convention is already real) |
| parallel-safe | yes (disjoint write set: `AGENTS.md` + 4 agent md files; conventionally dispatched last) |
| invariant flags | LF on `.md`; AGENTS.md hard-invariants format preserved; the envelope line must NOT contradict the rewritten BSOT §5.4/§5.5; keep RFC-7807 mention only as "superseded by ADR 0004"; do NOT touch the `Co-Authored-By`/CPM/banned-deps lines |
| acceptance | `AGENTS.md` "Domain conventions" `Errors:` entry states responses use the `ApiResponse<T>` envelope (success `data`, error `error{code,message,fields?}` with `error.code` from §5.9, `meta.traceId`), `application/problem+json` superseded by ADR 0004; each of the 4 agent files gains a one-line envelope reminder pointing at ADR 0004 + BSOT §5.4/§5.5; no other invariant line altered; markdown renders clean; prettier clean |
| source citations | ADR 0004 §Follow-up #4 "Add an explicit success-envelope invariant line to `AGENTS.md` + `dotnet-worker.md`/`nest-worker.md`" (`docs/adr/0004-api-response-envelope.md` line 125 — v7.5 EXTENDS to the 2 reviewer files per the patch mandate so review enforces it); AGENTS.md "Domain conventions" `Errors:` RFC-7807 line (current — to update); BSOT §5.4/§5.5 (rewritten by Task 3.7) |


## Dispatch order

1. **Task 3.0** (baseline) â€” must land + reviewer-approve first; blocks everything.
2. **Task 3.1** (Domain) â€” after 3.0.
3. **Task 3.2** (Infra config + migration) â€” after 3.1.
4. **Task 3.3** (auth infra) â€” after 3.2 (shares `InfrastructureServiceCollectionExtensions.cs` write).
5. **Task 3.5** (v7 NEW — BSOT error-code registration + shared-lib ratify) â€” parallel-safe; **MUST land BEFORE the 3.4 re-implement** (3.4 references `AUTH_OTP_RATE_LIMIT_EXCEEDED` + treats the two libs exception files as read-only deps once 3.5 lands).
6. **Task 3.6** (v7.3 NEW — `EfUnitOfWork` shared-lib primitive + wire into `AddVietRideDbContext`) — parallel-safe write set vs the live 3.4 files; MUST land + reviewer-APPROVE BEFORE Task 3.4 receives its FINAL approve (3.4 persistence DoD depends on it).
7. **Task 3.4** (CQRS + controllers + tests — **RE-IMPLEMENT under v7**) â€” last; after 3.0â€“3.3 + 3.5. **v7.2: Q-v7.1 is RESOLVED (option (b)) and folded into Task 3.4 acceptance — the prior BLOCKED-until-resolved gate is LIFTED.** Task 3.4 may be dispatched once Task 3.5 has landed (3.5 still MUST land BEFORE 3.4, which references `AUTH_OTP_RATE_LIMIT_EXCEEDED` + treats the two libs exception files as read-only deps once 3.5 lands).

**v7 dispatch chain (3.0â€“3.3 are DONE — do NOT re-run them; only 3.5 then a 3.4 re-implement remain):** land **Task 3.5** (registers the new code + ratifies the shared exceptions), then **RE-IMPLEMENT Task 3.4** against the v7 acceptance (seam fix B3/B4 via `FindByCodeAsync`; B1 phone→400; B2 429 rate-limit; KEEP + own the re-homed files; SF1–SF4). The 3.4 re-implement starts from the current on-disk state of the blocked attempt (build-green) and corrects it per v7 — it is NOT a from-scratch rewrite. `ComputeHash` (3.3-owned) is already on disk; if absent, Task 3.3 owns adding it. Net: **3.5 → 3.6 → 3.4(re-impl) final-approve**. **v7.3:** Task 3.4 was re-implemented and satisfies its v7 acceptance (B1/B2/B3/B4/SF1–SF4, build green, 59 identity + 39 kernel tests) but is BLOCKED on a Day-3-wide seam defect — `IUnitOfWork` has no implementation, so `TransactionBehavior` no-ops and handler mutations are never flushed across requests. **Task 3.6 fixes this as a shared-lib primitive** (`EfUnitOfWork` in `VietRide.Shared.Persistence` + registration in the shared `AddVietRideDbContext`). Once 3.6 lands + reviewer-APPROVES, re-review Task 3.4 against its persistence DoD (the mutation-persists-across-scope proof) for FINAL approve. 3.6 does NOT touch the live 3.4 files (disjoint write set), so it may be dispatched immediately.

**v7.5 dispatch chain (3.0–3.6 are DONE — do NOT re-run them; the ADR 0004 envelope rollout adds 3.7–3.11, sequenced BEFORE Day-3 close per option A):** **8.** **Task 3.7** (SOT docs — rewrite BSOT §5.4/§5.5/§5.7/§5.8 to the envelope + bump 1.4.0 to 1.5.0 + wrap API-Contract examples) — doc-only, parallel-safe, land FIRST so the code tasks implement against the written spec. **9.** **Task 3.8** ∥ **Task 3.9** — disjoint stacks (.NET shared libs vs TS libs), dispatch IN PARALLEL after 3.7: 3.8 adds the `ApiResponse<T>`/`PagedResult<T>`/`QueryOptions` types + success result-filter + `ApiResponseExceptionFilter` (REPLACES `ProblemDetailsExceptionFilter`) + `InvalidModelStateResponseFactory` wired through `AddVietRideSharedWeb`; 3.9 mirrors the TS types in `libs/shared/contracts` + the Nest interceptor/exception-filter in `nest-common` + the cross-stack contract test. **10.** **Task 3.10** — after 3.8: migrate the Identity Day 1–3 tests to `.data`/`.error.code` + flip `[ProducesResponseType]` to the envelope type (must follow 3.8 so the new filters/types exist + compile). **11.** **Task 3.11** — after 3.7 (conventionally LAST): the `AGENTS.md` + 4 worker/reviewer agent-file invariant line. Net v7.5 order: **3.7 → (3.8 ∥ 3.9) → 3.10 → 3.11**, THEN run `/verify` + `/audit-day 3` on the new standard (option A — the envelope ships + Identity migrates BEFORE Day-3 close, so there is no double migration). 3.7 is the only hard precondition for the rest; 3.8∥3.9 are write-set-disjoint; 3.10 gates on 3.8; 3.11 is cosmetic/last.

## Progress tracker
> Orchestrator bookkeeping — updated after each `/implement-task` (Step 3). **Informational only — NOT audit evidence.** `/audit-day 3` re-verifies every task independently against the SOT; it must not trust this table.

| Task | Status | Review verdict | Date | Notes |
|---|---|---|---|---|
| 3.0 | ✅ done | APPROVED | 2026-05-31 | Arch baseline (CPM + MediatR v11 behaviors + NetArchTest). Reviewer's 2 EOL findings verified false-positive (i/lf index = repo-wide norm). `/verify` = build+test green (no runtime surface). |
| 3.1 | ✅ done | APPROVED (3 patch rounds) | 2026-05-31 | Domain (User aggregate + enums + exceptions). BLOCKER `ISoftDeletable` vs schema → human decision → spawned **ADR 0003** (split `ISoftDeletable`/`IActivatable`, BSOT→1.3.5). `/verify` = build+test green (pure POCO). |
| 3.2 | ✅ done (await human /verify) | APPROVED (2 patch rounds) | 2026-05-31 | Infra: EF configs + 6 DbSets + 3 repos + migration `InitIdentityAuth`. R1 fixed: `uq_users_email` as `LOWER(email)` expr index via raw `migrationBuilder.Sql`; `RevokeFamilyAsync` 2-param + `IClock`; Operator Day-6 warning. R2 (human-authorized) fixed: `GetByPhoneAsync` reverted to direct string compare (R1 regression — `PhoneNumber.Parse` threw on 10-digit before 3.4 widens regex); `GetByEmailAsync` `.ToLower()` hits expr index. Build 0W/0E, format clean, tests 16/16. Pre-existing PhoneNumber-regex gap in ValueConverter is Task 3.4's owned scope. |
| 3.3 | ✅ done (await human /verify) | APPROVED (1 patch round) | 2026-05-31 | Auth infra: BCrypt(cost12) + RS256 `RsaAccessTokenService` + `JwksProvider` + `RefreshTokenFactory`(SHA-256 hex) + `LoggingEmailService` (no SendGrid). R1 fixed BLOCKER: `operatorId` claim now OMITTED (not `""`) when null + absence test; +`IClock` injected (deterministic exp/iat); dead test var removed. Build 0W/0E, format clean, tests 33/33. Note: `AddInfrastructure()` signature changed to `(IConfiguration)` → Task 3.4 must call `AddInfrastructure(builder.Configuration)` in Program.cs. NIT carry: `RefreshTokenFactory.ComputeSha256Hex` is public static (test-only; could be internal+InternalsVisibleTo). |
| 3.4 | ✅ done (await human /verify) | APPROVED (3 patch rounds total across re-impl) | 2026-06-01 | **FINAL APPROVE.** Re-impl under v7.2: B1 phone→400 `BadRequestException`; B2 rate-limit→429 `TooManyRequestsException`; B3/B4 `VerifyEmailCommandHandler` (`FindByCodeAsync`+`FindLatestPendingAsync`, case (1)–(4)); SF1 `StatusCode(201)`; SF2 request records own files; SF3 no input echo; SF4 `[ProducesResponseType(429)]`. R1 fixed Redis DI→`AddInfrastructure` + Application EF-free via repo `TryAddAsync`. IUnitOfWork seam BLOCKER → fixed by Task 3.6 (`EfUnitOfWork`); 3.4 final pass then root-caused + fixed the JWKS-500 integration failure (`TransactionBehavior` now opens a tx for every request incl. no-DB `GetJwksQuery` → test factory uses `NoOpUnitOfWork`). R2 (final review) added missing `AUTH_PHONE_INVALID_FORMAT` 400 handler test (SF4 B1) + removed dead `Revoke()` on expired-refresh path + added expired-token 401 test. Build 0W/0E, format clean, all identity + 39 kernel tests green, 4 other services 0E. (Carry-over: `VietRide.Identity.UnitTests.csproj` Infra+Api refs = Task-3.0-owned, reviewer-confirmed non-blocking.) |
| 3.5 | ✅ done (await human /verify) | APPROVED (0 patch rounds) | 2026-06-01 | (v7 NEW) BSOT bumped 1.3.6→**1.4.0**: §5.9 +row `AUTH_OTP_RATE_LIMIT_EXCEEDED` (429) after `AUTH_OTP_EXPIRED`; §13 changelog +entry. `TooManyRequestsException` added + filter arm →429; `UnauthorizedException`/`BadRequestException` (400/401) ratified. Build libs 0W/0E, format clean, kernel tests 39/39. SF/NIT only (switch-arm ordering; primary-ctor style) — non-blocking. **Reviewer forwarded 2 items for the 3.4 re-impl review: (i) `Program.cs` registers `IConnectionMultiplexer`/`IOtpRateLimiter` directly — plan says these belong in `InfrastructureServiceCollectionExtensions.cs` (avoid duplication); (ii) `VietRide.Identity.UnitTests.csproj` references Infrastructure+Api — may break NetArchTest layering assumption.** |
| 3.6 | ✅ done (await human /verify) | APPROVED (0 patch rounds) | 2026-06-01 | (v7.3 NEW) `EfUnitOfWork : IUnitOfWork` added in `VietRide.Shared.Persistence/UnitOfWork/` (thin wrapper over `VietRideDbContextBase`; Begin→SaveChanges→Commit flushes; Rollback safe no-op; Commit w/o tx → `InvalidOperationException`). Scoped `IUnitOfWork`→`EfUnitOfWork` appended to shared `AddVietRideDbContext<TContext>` (existing lines untouched). **Fixes the Day-3-wide `TransactionBehavior` no-op seam** → Identity handler mutations now flush. A4 proof (option b, no new package): DI-resolution integration test (`GetRequiredService<IUnitOfWork>()`→`EfUnitOfWork`) + delegation unit test (`SpyDbContext` SaveChanges count). Build libs+identity 0W/0E + 4 other services 0E (additive/non-breaking, VERIFIED); format clean; 55 identity unit + 39 kernel + DI test green. csproj refs to Shared.Persistence added to UnitTests+IntegrationTests (reviewer-verified necessary, non-blocking). NITs only. **Carry-over for 3.4 FINAL review: pre-existing integration failure `GetJwks_Returns200_WithRsaKeyShape` (500) — unrelated to 3.6, likely 3.4 WebApplicationFactory boot (Redis/JWKS DI) — must be resolved in 3.4 final.** |
| 3.7 | ✅ done (await human /verify) | APPROVED on merits (1 patch round) | 2026-06-01 | (v7.5) SOT docs: rewrote BSOT §5.4/§5.5/§5.7/§5.8 → envelope, bumped 1.4.0→**1.5.0** + §13, wrapped API-Contract examples (JWKS/204 exempt). Patch fixed S1 (registered `INVALID_SORT_FIELD` 400 in §5.9), S2 (§3.1 PagedResult 7-field), S3 (§3.1/§3.6 `ProblemDetailsExceptionFilter`→`ApiResponseExceptionFilter`/`ApiResponseResultFilter`). prettier clean. Reviewer's B1/B2 were FALSE-POSITIVES from the uncommitted v6 baseline (prior-task 3.0–3.6/3.1/3.5 work showing in the diff) — **resolved by committing the Day-3 baseline** (commits `dffff5a` impl + `97eceb0` envelope SOT), NOT a worker patch. Substantive §5.4/5.5/5.7/5.8 content was reviewer-confirmed. Clean baseline now established → tasks 3.8–3.11 review in isolation. |
| 3.8 | ⬜ todo | — | — | (v7.5 NEW) .NET shared: `ApiResponse`/`PagedResult`/`QueryOptions` (.Kernel) + success result-filter + `ApiResponseExceptionFilter` REPLACING `ProblemDetailsExceptionFilter` + `InvalidModelStateResponseFactory`, wired via `AddVietRideSharedWeb`. ADR 0004 follow-up #3. |
| 3.9 | ⬜ todo | — | — | (v7.5 NEW) TS: mirror `ApiResponse`/`PagedResult`/`QueryOptions` in `libs/shared/contracts` + Nest interceptor/exception-filter in `nest-common` + cross-stack contract test. Parallel-safe vs 3.8. ADR 0004 follow-up #3. |
| 3.10 | ⬜ todo | — | — | (v7.5 NEW) Migrate Identity Day 1–3 tests to `.data`/`.error.code` + flip `[ProducesResponseType]` to `ApiResponse<T>`. Depends on 3.8. ADR 0004 follow-up #3. |
| 3.11 | ⬜ todo | — | — | (v7.5 NEW) Add success-envelope invariant line to `AGENTS.md` + 4 worker/reviewer agent files. Depends on 3.7 (cosmetically last). ADR 0004 follow-up #4. |

Legend: ⬜ todo · 🔄 in progress · ✅ done (reviewer APPROVED + human `/verify`) · ⚠️ done-with-carryover · ❌ blocked

## Open questions
**None open.** (Q-v7.5.1 … Q-v7.5.5 resolved into plan body below — defaults already baked into the Task 3.7–3.11 acceptance.)

### Day-3 v6 questions — all resolved into plan body (no longer open)
(Q1–Q4 below resolved as recorded.)

### Resolved into plan body (no longer open)
- ~~Q-v7.5.1 (success result-filter wrap JWKS?)~~ → **EXEMPT `/v1/.well-known/jwks.json` from the envelope.** JWKS is an external standard (RFC 7517) parsed by the gateway's JWT/JWKS client — wrapping it in `{success,data,...}` would break standard consumers. Task 3.8's `ApiResponseResultFilter` SKIPS `/.well-known/*`; Task 3.10 keeps the JWKS test asserting the raw `{keys:[...]}` shape. Folded into Task 3.8 + Task 3.10 acceptance.
- ~~Q-v7.5.4 (is `/health*` wrapped?)~~ → **EXEMPT `/health*` from the envelope** (ASP.NET health-check format, polled by UptimeRobot/Docker — same rationale as JWKS). Task 3.8's result-filter skips it; Task 3.10 keeps `HealthEndpointTests` asserting the unwrapped shape. Folded into Task 3.8 + Task 3.10 acceptance.
- ~~Q-v7.5.2 (`ApiResponse<T>`/`PagedResult<T>`/`QueryOptions` in `.Kernel` vs `.Application`)~~ → **`VietRide.Shared.Kernel/Primitives`** (envelope is a transport primitive with no app-service dependency; Application query handlers already reference Kernel — no layering issue). Folded into Task 3.8 owned files + acceptance.
- ~~Q-v7.5.3 (TS migration of `error-envelope.ts`/`page-result.ts`)~~ → **re-point in place:** replace `ProblemDetailsSchema` (RFC7807) with the new error-envelope schema and keep `PageResult<T>` as a transition alias to the 7-field `PagedResult<T>`, so no dangling RFC7807 shape ships. Folded into Task 3.9 owned files + acceptance.
- ~~Q-v7.5.5 (cross-stack contract-test mechanism)~~ → **static committed fixture** (from ADR 0004 Rule 1 examples) that the TS Zod schema validates; regenerated when the envelope shape changes — NO live cross-process test in Day 3 (keeps 3.9 parallel-safe vs 3.8). Folded into Task 3.9 owned files + acceptance.
- ~~Q-v7.3.1 (Task 3.6 acceptance A4 — persistence-proof strategy without a new package)~~ â†’ **option (b): the no-new-package MINIMUM is the definitive gating bar.** Task 3.6's `EfUnitOfWork` persistence proof = (1) the Identity composition root resolves `IUnitOfWork` -> `EfUnitOfWork` from DI (a DI-resolution assertion), AND (2) a unit test proves `EfUnitOfWork.CommitAsync` (and/or `SaveChangesAsync`) delegates to the underlying `DbContext`'s `SaveChangesAsync` (via a substitute/mock or a test `DbContext`). **NO new test-DB package (NO Testcontainers/Respawn/Sqlite/EFCore.InMemory), NO reliance on a live Postgres.** Real end-to-end cross-scope persistence via live Postgres is explicitly OUT of the Day-3 gating bar (may be added later if/when a test-DB harness is approved — its own future scope — but NOT required for Task 3.6 reviewer-APPROVE). Folded into Task 3.6 acceptance A4 (definitive, no longer (a)/(b)/(c) ambiguous). (Verified Day-3 fences: CPM has no test-DB package + adding one is forbidden Day-3 scope; the current integration harness does not touch a real DB; EFCore.InMemory throws on `BeginTransactionAsync`.)
- ~~Q-v7.1 (wrong-code `failed_attempts` increment target)~~ â†’ **option (b): a dedicated repo method `FindLatestPendingAsync(Guid userId, EmailVerificationPurpose purpose, CancellationToken ct)`** that returns the most-recent `used_at IS NULL` token for that `user_id`+`purpose` (NO code match), so a wrong-code attempt still has a row whose `failed_attempts` to increment; after `failed_attempts >= 5` the row is invalidated/disqualified. Verify-email keeps exact-match `FindByCodeAsync` for the success/expiry branch (expired exact-match â†’ 400 `AUTH_OTP_EXPIRED`, no increment); on a wrong code (no exact-match row) â†’ `FindLatestPendingAsync` â†’ increment `failed_attempts` â†’ 400 `AUTH_OTP_INVALID`. Folded into Task 3.4 owned files (`FindLatestPendingAsync` ADDed alongside `FindByCodeAsync` on the EmailVerificationToken repo pair) + Task 3.4 acceptance; the prior BLOCKED-until-resolved hard gate is LIFTED (3.5 still lands before 3.4). (Schema `email_verification_tokens.failed_attempts` per-row INT, lines 241â€“260, supports (b).)
- ~~Q3 (RS256 env vars)~~ â†’ `USER_JWT_PRIVATE_KEY` + `USER_JWT_KID` per BSOT Â§11.3 lines 2261/2264. See Contract changes Â§Config/secrets.
- ~~Q4 (Outbox INSERT now vs Day 10)~~ â†’ defer entirely to Day 10. See Contract changes Â§Events.
- ~~Q7 (sln filename)~~ â†’ demoted to worker note: run `ls apps/identity/` once before `dotnet build` / `dotnet ef` to confirm `VietRide.Identity.sln`.
- ~~Q1 (OTP storage vs uniqueness)~~ â†’ **6-digit numeric kept (BSOT Â§6.1 line 1398).** Schema unique index `uq_email_verification_tokens_code_purpose` stays full unique (schema line 254 â€” no WHERE clause). Reconciliation: (a) verify lookup uses `(userId, code, purpose)`, NOT `(code, purpose)` â€” see `IEmailVerificationTokenRepository.FindActiveAsync` in Task 3.2; (b) code generation via `RandomNumberGenerator.GetInt32(0, 1_000_000).ToString("D6")` with INSERT retry-on-conflict (â‰¤3 attempts) â€” see Task 3.4. Collision probability with retries is negligible at expected OTP volume.
- ~~Q2 (refresh-token hash algorithm)~~ â†’ **SHA-256 hex** (BSOT Â§6.1 line 1391 + Â§6.3 lines 1424â€“1430 + schema line 225 `uq_refresh_tokens_token_hash`). Raw refresh = 32 hex chars from `Guid.NewGuid().ToString("N")` (or 32-byte base64url from cryptographic RNG); `token_hash` = lower-case hex of `SHA256.HashData(UTF8.GetBytes(raw))` (64 chars). Raw returned to client once at issue, never persisted. BCrypt is reserved for `users.password_hash` only. See Task 3.3.
- ~~Q3 (phone at email registration)~~ â†’ **REQUIRED** for PASSENGER self-register (v7 lines 1309 + 4720). Accept local `^0[0-9]{9,10}$` OR E.164 `^\+84[0-9]{9,10}$`; normalize local â†’ E.164 before persist; DB only stores E.164 (schema line 161 CHECK + line 173 partial unique). Existing `PhoneNumber` VO needs widen (current `+84\d{9}` â†’ `+84\d{9,10}`) + new `Normalize(string)` factory â€” added to Task 3.4 owned files. Errors: 400 `AUTH_PHONE_INVALID_FORMAT`, 409 `AUTH_PHONE_ALREADY_REGISTERED`. Google-OAuth complete-profile is the separate Day-4 path.
- ~~Q4 (SendGrid stub placement)~~ â†’ Day 3 ships `LoggingEmailService : IEmailService` in Identity Infrastructure (Serilog log only) â€” **NO SendGrid SDK in Identity, ever**. Day 10 swaps to `OutboxBackedEmailService` that inserts `identity.otp.requested` (Outbox publisher hosted service also lands Day 10); Notification Service adds the consumer + BullMQ job â†’ SendGrid (BSOT Â§1.2 line 93, Â§3.5 lines 419/471, line 2191). Class rename `SendGridEmailServiceStub` â†’ `LoggingEmailService` applied across Task 3.3.
