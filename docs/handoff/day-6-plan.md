# Day 6 — Plan

- **Timeline ref**: BE_TIMELINE_VU.md -> Day 6 (Mon 2026-06-01) — Operator Service within Identity DB (Jira: SCV-72)
- **Prior checklist**: docs/handoff/day-5-checklist.md (found — carry-over: Operator entity is a Day-3 stub PK-only; runtime operators table has only id; OperatorConfiguration has 3 Ignore(...) calls flagged "remove in Day 6"; Day 6 MUST also close the Day-5 carry-over for operator-created Driver/Assistant/OperatorStaff onboarding via SET_INITIAL_PASSWORD)
- **Plan status**: ✅ APPROVED (2026-06-07 review fixes folded; BE lead confirmed expanded Day-6 scope; Task 6.0a contract baseline completed in `VietRide_API_Contract_v1.md` + `BACKEND_SOURCE_OF_TRUTH.md`, so Day-6 implementation can proceed sequentially from Task 6.0)

## Objective
Day 6 turns the stub Operator aggregate into the full Operator + Subscription domain inside the Identity DB: self-registration, System-Admin manual create, approve/reject/suspend lifecycle, auto-assigned Starter Free-Trial subscription, operator-scoped user creation, OPERATOR_ADMIN/OPERATOR_STAFF profile read, OPERATOR_ADMIN profile/policy update, and internal operator/subscription lookup + usage-counter endpoints for Day 7+. The expanded scope beyond the terse timeline is intentional and BE-lead approved on 2026-06-07: it closes the Day-5 carry-over by wiring operator-created Driver/Assistant/OperatorStaff to the reusable SET_INITIAL_PASSWORD email-link flow, and it provides the internal operator/subscription lookup + usage-counter prerequisites needed by Day 7+ services. It also closes the Day-3 Operator stub carry-over by replacing the PK-only EF mapping with the full schema migration and default SubscriptionPlan seed. Outbox emission for identity.operator.approved/suspended remains deferred to Day 10 per timeline.

## Success criteria (DoD — binary, verifiable)
- [x] **Contract baseline is updated before feature code**: `VietRide_API_Contract_v1.md` documents exact request/response/error shapes for Day-6 FE-facing endpoints and internal endpoints before any add-endpoint worker implements DTOs/controllers; BSOT registry/changelog is synced for new endpoint registry notes and no undocumented error code is invented.
- [ ] Operator self-register (`POST /v1/operators/register`, public) accepts the v7 registration body **plus `password`** (human decision 2026-06-06), hashes with BCrypt cost 12, and creates `Operator{registrationStatus=PENDING}` + `User{role=OPERATOR_ADMIN, status=PENDING_EMAIL_VERIFICATION, passwordHash=set}` + `OperatorSubscription{status=PENDING_APPROVAL, startedAt/expiresAt=NULL, currentOperatorUsers=1}` in one transaction; duplicate businessRegistrationNumber/taxCode returns 409 `OPERATOR_DUPLICATE_REGISTRATION` / `OPERATOR_DUPLICATE_TAX_CODE`; duplicate OPERATOR_ADMIN email/phone returns 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED`; after verify-email + admin approve, the operator admin can login.
- [ ] System Admin manual create (`POST /v1/admin/operators`, SYSTEM_ADMIN) creates `Operator{registrationStatus=APPROVED}` + `OPERATOR_ADMIN` user **passwordless** with `status=PENDING_INITIAL_PASSWORD` + a `SET_INITIAL_PASSWORD` token/email link (human decision 2026-06-06) + `OperatorSubscription{status=ACTIVE, startedAt=now, expiresAt=now+30d, currentOperatorUsers=1}` using the Starter Free-Trial default; duplicate businessRegistrationNumber/taxCode returns 409 `OPERATOR_DUPLICATE_REGISTRATION` / `OPERATOR_DUPLICATE_TAX_CODE`; duplicate OPERATOR_ADMIN email/phone returns 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED`; any paid-plan/`planId` field is rejected with HTTP 422 `VALIDATION_ERROR`; after set-initial-password, the OPERATOR_ADMIN can login.
- [ ] Approve (`POST /v1/admin/operators/{operatorId}/approve`) sets registrationStatus=APPROVED + atomically `OperatorSubscription{status=ACTIVE, startedAt=approvedAt, expiresAt=approvedAt+30d}`; reject (`POST /v1/admin/operators/{operatorId}/reject`, body `{reason}`) sets registrationStatus=REJECTED + rejectedAt/rejectedByUserId/rejectReason AND sets the PENDING_APPROVAL subscription to status=CANCELLED (NO deletedAt — operator_subscriptions is not soft-delete, schema.sql:333-368), response `{operatorId, registrationStatus:"REJECTED"}`; suspend (`.../suspend`) sets SUSPENDED. Day 6 ships approve/reject/suspend ONLY — the SUSPENDED->APPROVED reactivation path (state machine BACKEND_SOURCE_OF_TRUTH.md:1923, technical_context_v7.md:4490) is deferred (Q5). Invalid lifecycle transitions return HTTP 422 with canonical `VALIDATION_ERROR`; do not add `OPERATOR_INVALID_STATUS_TRANSITION`.
- [ ] Login is blocked with HTTP 403 `FORBIDDEN` for an OPERATOR_ADMIN/OPERATOR_STAFF whose `Operator.registrationStatus != APPROVED` (covers PENDING + SUSPENDED + REJECTED after email/password checks pass) — enforced in the Identity login handler (Task 6.2b), no Gateway->Identity status hop and no new operator-specific error code. Tokens issued before a later suspend/reject are handled by Day-6 application-layer guards: operator write/action endpoints (`POST /v1/operator/users`, `POST /v1/operator/users/{userId}/resend-initial-password`, `PATCH /v1/operator/profile`) re-check the caller's Operator status and require APPROVED; `GET /v1/operator/profile` remains readable for OPERATOR_ADMIN/OPERATOR_STAFF so the UI can display the current status/policies.
- [ ] Operator-created users carry-over is shipped: OPERATOR_ADMIN can create DRIVER, ASSISTANT, and OPERATOR_STAFF under their own APPROVED operator via `POST /v1/operator/users`; users are `PENDING_INITIAL_PASSWORD`, receive a SET_INITIAL_PASSWORD email link, set password through Day-5 endpoint, then login; duplicate target email/phone returns 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED`; subscription limits `maxDrivers`, `maxAssistants`, and `maxOperatorUsers` are enforced and usage counters increment atomically without concurrent overshoot.
- [ ] OPERATOR_ADMIN and OPERATOR_STAFF can read own operator profile (`GET /v1/operator/profile`, operatorId from JWT claim) even if the operator has since become non-APPROVED; only OPERATOR_ADMIN of an APPROVED operator can update it (`PATCH /v1/operator/profile`) incl. cancellationPolicy/parcelNoShowPolicy/luggagePolicy JSONB; OPERATOR_STAFF and non-APPROVED operators get 403 on update. Gateway allows both roles for the prefix, and application-level authorization blocks PATCH for staff/status.
- [ ] Internal endpoints are implemented for Day 7+: `GET /internal/v1/operators/{operatorId}`, `GET /internal/v1/operators/{operatorId}/subscription`, and `POST /internal/v1/operators/{operatorId}/usage/increment` with body `{resource, delta}`; success payloads are raw internal DTOs, errors use ADR 0004 envelope, Internal JWT is required, and subscription limit overflow returns `SUBSCRIPTION_LIMIT_EXCEEDED`.
- [ ] EF migration applies clean from an existing Day-5 DB: operators gains the full columns + `operator_registration_status` enum; subscription_plans + operator_subscriptions tables + enums are created; default Starter plan is seeded at fixed UUID `00000000-0000-0000-0000-000000000001`; existing PK-only operator rows, if present in local/test DBs, are backfilled with deterministic LEGACY-* placeholder values before NOT NULL constraints are enforced (human decision: Hướng A, 2026-06-06).
- [ ] `dotnet build` + `dotnet format --verify-no-changes` clean; Identity unit + integration tests green incl. new tests; Gateway lint/build/test green for route changes; NetArchTest layering still green; Postman collection parses and covers register -> verify-email -> approve -> login -> profile update plus admin-create -> set-initial-password -> login and operator-user create -> set-initial-password -> login.

## Contract changes
- **Contract-first rule (Task 6.0a)** — The exact endpoint blocks below MUST be added to `VietRide_API_Contract_v1.md` before DTO/controller implementation. Workers must implement to that contract; if a shape is still ambiguous after Task 6.0a review, STOP and ask the human before code.
- **Endpoints (Identity, via Gateway)** — Gateway routes ALREADY exist for `/v1/operators` mixed public-register, `/v1/admin/operators` SYSTEM_ADMIN, and `/v1/operator/users` OPERATOR_ADMIN (apps/gateway/src/config/routes.ts:52-75). Task 6.6 ADDS a `/v1/operator/profile` prefix with roles `[OPERATOR_ADMIN, OPERATOR_STAFF]` so STAFF can GET while PATCH is app-blocked. Confirm longest-prefix-wins still routes `/v1/operator/users` and `/v1/operator/profile` distinctly; do not introduce a bare `/v1/operator` prefix.
  - `POST /v1/operators/register` — public; body = technical_context_v7.md:585 fields + `password`; response `{ operatorId, message }` (technical_context_v7.md:599); no Idempotency-Key; hashes password with BCrypt cost 12 and stores OPERATOR_ADMIN as `PENDING_EMAIL_VERIFICATION`; duplicate email/phone returns 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED`.
  - `POST /v1/admin/operators` — SYSTEM_ADMIN; no password in body; Day 6 supports ONLY Starter Free-Trial default (subscription ACTIVE, 30-day); creates OPERATOR_ADMIN as `PENDING_INITIAL_PASSWORD`, generates SET_INITIAL_PASSWORD token/email link, and returns contract-defined operator/admin/subscription summary. Paid-plan pick / PENDING_PAYMENT is deferred to Sprint 5 / Day 37 (Q4); any paid-plan/`planId` field is rejected with HTTP 422 `VALIDATION_ERROR` and never produces PENDING_PAYMENT; duplicate businessRegistrationNumber/taxCode returns 409 `OPERATOR_DUPLICATE_REGISTRATION` / `OPERATOR_DUPLICATE_TAX_CODE`; duplicate email/phone returns 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED`.
  - `POST /v1/admin/operators/{operatorId}/approve` — SYSTEM_ADMIN; response `{operatorId, registrationStatus:"APPROVED"}` (VietRide_API_Contract_v1.md §Operator/Admin Management). This intentionally follows the current API Contract's POST action-endpoint shape; the older `BE_TIMELINE_VU.md:83` PATCH wording is superseded for Day 6 by the API contract/action-endpoint convention.
  - `POST /v1/admin/operators/{operatorId}/reject` — SYSTEM_ADMIN; body `{reason}`, response `{operatorId, registrationStatus:"REJECTED"}` (Q1; mirrors approve/suspend pattern in VietRide_API_Contract_v1.md §Operator/Admin Management; reject behaviour technical_context_v7.md:587-595, with schema/BSOT conflict resolved to no deletedAt).
  - `POST /v1/admin/operators/{operatorId}/suspend` — SYSTEM_ADMIN; body `{reason}`, response `{operatorId, registrationStatus:"SUSPENDED"}` (VietRide_API_Contract_v1.md §Operator/Admin Management).
  - `GET /v1/admin/operators` — SYSTEM_ADMIN; paged via ADR 0004 `PagedResult<T>` (VietRide_API_Contract_v1.md §Operator/Admin Management; BSOT §5.7).
  - `POST /v1/operator/users` — OPERATOR_ADMIN; body minimum `{ email, phone, displayName, role }` where role is `DRIVER | ASSISTANT | OPERATOR_STAFF`; operatorId is always from caller JWT claim; handler re-checks caller Operator status and requires APPROVED (tokens issued before suspend/reject must fail with 403 `FORBIDDEN`); creates `PENDING_INITIAL_PASSWORD` user, sends SET_INITIAL_PASSWORD link, enforces subscription limits/counters; duplicate email/phone returns 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED`; no Idempotency-Key. Exact response is fixed by Task 6.0a before implementation.
  - Existing `POST /v1/operator/users/{userId}/resend-initial-password` keeps its Day-5 request/response shape (VietRide_API_Contract_v1.md §Identity & User Service, resend-initial-password) but Day 6 adds the same current-Operator APPROVED app-layer guard as other operator write/actions; a pre-suspend token must fail with 403 `FORBIDDEN` and produce no token/email/ActivityLog side effects. Task 6.3 must preserve its successful behavior while adding create and status guard coverage.
  - `GET /v1/operator/profile` + `PATCH /v1/operator/profile` — Gateway allows OPERATOR_ADMIN + OPERATOR_STAFF; read allowed for both even when the operator is non-APPROVED; update requires OPERATOR_ADMIN and a currently APPROVED operator, so tokens issued before suspend/reject cannot modify policies; operatorId from JWT claim, NO `{id}` in path (Q2/Q8/Q18); documented in API contract by Task 6.0a.
- **Internal endpoints (Identity, not via Gateway)** — registered in BSOT §7.2 already (BACKEND_SOURCE_OF_TRUTH.md:1660-1662); Task 6.5 implements and Task 6.0a documents exact raw success DTOs:
  - `GET /internal/v1/operators/{operatorId}` — Internal JWT; raw operator lookup DTO for logical FK validation.
  - `GET /internal/v1/operators/{operatorId}/subscription` — Internal JWT; raw plan + status + limits + usage counters + module flags.
  - `POST /internal/v1/operators/{operatorId}/usage/increment` — Internal JWT; body `{ resource: "VEHICLES"|"DRIVERS"|"ASSISTANTS"|"OPERATOR_USERS"|"ROUTES"|"TRIPS_THIS_MONTH", delta: positive int }` (human decision 2026-06-06); checks plan limit and atomically increments the matching counter; raw updated usage/subscription DTO on success.
- **DB migration**: full operators schema, `operator_registration_status` enum, subscription_plans, operator_subscriptions, `subscription_status`, `subscription_payment_method` enums + Starter plan seed (db-schema/identity-user/schema.sql:34-37,70-74,83-137,301-367). Migration must be safe for existing PK-only operator rows by using deterministic backfill placeholders before NOT NULL enforcement (human decision: Hướng A, 2026-06-06): `name = 'Legacy Operator ' || shortId`, `businessRegistrationNumber = 'LEGACY-BRN-' || shortId`, `taxCode = 'LEGACY-TAX-' || shortId`, `contactEmail = 'legacy-' || shortId || '@vietride.local'`, `contactPhone = '+84000000000'`, address/representative fields NULL, `registrationStatus = 'APPROVED'`, `isActive = TRUE`, timestamps = `now()`. `shortId` must be derived deterministically from the UUID (for example first 8 hex chars) and must keep businessRegistrationNumber/taxCode unique.
- **Error codes**: `OPERATOR_DUPLICATE_REGISTRATION` (409), `OPERATOR_DUPLICATE_TAX_CODE` (409), `AUTH_EMAIL_ALREADY_REGISTERED` (409), `AUTH_PHONE_ALREADY_REGISTERED` (409), `SUBSCRIPTION_LIMIT_EXCEEDED` (422), `SUBSCRIPTION_MODULE_DISABLED` (403), `SUBSCRIPTION_EXPIRED` (402), `SUBSCRIPTION_PAYMENT_PENDING` (409), and generic `RESOURCE_NOT_FOUND`/`FORBIDDEN`/`VALIDATION_ERROR` are already in BSOT §5.9. Do NOT invent `OPERATOR_INVALID_STATUS_TRANSITION` or `OPERATOR_NOT_APPROVED`; lifecycle guard failures use HTTP 422 with canonical `VALIDATION_ERROR` (BE-lead decision 2026-06-07), and non-APPROVED operator login/write-action guards use HTTP 403 `FORBIDDEN`.
- **ActivityLog**: `activity_logs.user_id` is the actor user id for Day 6 actions (human decision 2026-06-07). Authenticated actions use the caller's user id; public self-register uses the newly created OPERATOR_ADMIN user id as the self actor. Metadata MUST be built with `System.Text.Json.JsonSerializer.Serialize(new { ... })` (not string interpolation) and include `operatorId`, `actorUserId`, `targetUserId` when different from actor, and `source` (for example `SELF_REGISTER`, `SYSTEM_ADMIN_CREATE_OPERATOR`, `OPERATOR_USER_CREATE`). Tests must assert persisted metadata is valid JSONB and contains required keys. `CREATE_OPERATOR` is written for self-register and admin-create; `APPROVE_OPERATOR` and `REJECT_OPERATOR` are written for approve/reject; suspend writes NO ActivityLog because schema has no `SUSPEND_OPERATOR` value (db-schema/identity-user/schema.sql:51-68).
- **Events**: `identity.operator.approved {operatorId, approvedAt}` + `identity.operator.suspended {operatorId, suspendedAt}` are registered (BACKEND_SOURCE_OF_TRUTH.md:1723-1724) but emission is Day 10 (BE_TIMELINE_VU.md:119) — DO NOT wire Outbox in Day 6.

## Tasks

### Global implementation invariants for Day 6
- Task 6.0a contract baseline is complete in this session; if a future edit changes `VietRide_API_Contract_v1.md`/BSOT Day-6 shapes, feature code MUST pause until that contract drift is reviewer-approved again.
- No `.csproj` or `Directory.Packages.props` changes in Day-6 implementation tasks; no new NuGet/TS dependency unless the human explicitly approves; MediatR remains v11.x; no AutoMapper or banned observability dependencies.
- Operator-status enforcement is split intentionally: Identity login blocks non-APPROVED operator users, and Day-6 application handlers re-check current Operator status for operator write/action endpoints so pre-suspend tokens cannot keep mutating state. No Gateway -> Identity synchronous status hop is added in Day 6.
- `.../` path abbreviations in write-set tables are plan shorthand only; implementation dispatch prompts MUST expand them to full repo-relative paths before assigning a worker, especially for shared repository interfaces/implementations touched by multiple serialized tasks.

### Task 6.0a — Contract baseline: API contract + BSOT registry before code
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer |
| skill | (none) |
| owned files (write set) | VietRide_API_Contract_v1.md (add/complete register/admin-create/reject/operator-users-create/profile/internal endpoint blocks before implementation) - BACKEND_SOURCE_OF_TRUTH.md (endpoint registry notes/changelog only; no new error code unless human-approved) |
| forbidden scope | .env, secrets, apps/** code, libs/** code, db-schema/**, docs/api/postman/** (final Postman owned by 6.7), git ops |
| depends on | none |
| invariant flags | LF on .md - ADR 0004 envelope for FE-facing responses/errors - internal success raw DTOs, internal errors ADR 0004 - no undocumented error codes - no Idempotency-Key for Day-6 FE endpoints unless BSOT §5.6 is changed first - contract shapes are canonical for later DTO/controller work |
| acceptance | API contract contains exact request/response/error blocks for all Day-6 FE-facing endpoints and the three internal endpoints listed above; register body includes password; register/admin-create/operator-users-create document duplicate email/phone as 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED`; register/admin-create document duplicate businessRegistrationNumber/taxCode as 409 `OPERATOR_DUPLICATE_REGISTRATION` / `OPERATOR_DUPLICATE_TAX_CODE`; admin-create documents SET_INITIAL_PASSWORD link flow and rejects any paid-plan/`planId` field with HTTP 422 `VALIDATION_ERROR`; lifecycle endpoints explicitly document POST action paths as the API-contract shape superseding the older timeline PATCH wording, and invalid lifecycle transitions as HTTP 422 `VALIDATION_ERROR`; non-APPROVED operator login is documented as HTTP 403 `FORBIDDEN`; operator-users-create and resend-initial-password document current-Operator APPROVED guard, operator-users-create documents allowed roles and subscription limit behavior; profile documents read roles OPERATOR_ADMIN+OPERATOR_STAFF, read allowed for non-APPROVED operators, and update requiring OPERATOR_ADMIN plus current Operator APPROVED; internal usage increment body is `{resource, delta}`; BSOT changelog row added including the Day-6 reject-subscription no-`deletedAt` conflict resolution, ActivityLog actor-user convention, and app-layer status guard decision; reviewer approves contract baseline before 6.0+ implementation starts |
| source citations | technical_context_v7.md:535-541,585-599,700-715,791-819,1310-1311; BE_TIMELINE_VU.md:69-87; BACKEND_SOURCE_OF_TRUTH.md §5.6/§5.9/§6.6-6.7/§7.2/§13; VietRide_API_Contract_v1.md §Identity & User Service resend-initial-password + §Operator/Admin Management; docs/handoff/day-5-checklist.md:127-136 |

### Task 6.0 — Architecture baseline: full Operator + Subscription domain, EF config, migration, default-plan seed
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | ef-migration only (do NOT run/use the full scaffold-aggregate workflow; Task 6.0 is Domain entities/enums + EF configuration + DbContext enum mappings + migration/snapshot only) |
| owned files (write set) | apps/identity/src/VietRide.Identity.Domain/Entities/Operator.cs (replace stub) - .../Entities/SubscriptionPlan.cs - .../Entities/OperatorSubscription.cs - .../Enums/OperatorRegistrationStatus.cs - .../Enums/SubscriptionStatus.cs - .../Enums/SubscriptionPaymentMethod.cs - .../Enums/SubscriptionUsageResource.cs (or Application enum if kept out of Domain) - apps/identity/src/VietRide.Identity.Infrastructure/Persistence/Configurations/OperatorConfiguration.cs (replace stub; remove ONLY Ignore(o => o.CreatedAt) + Ignore(o => o.UpdatedAt); KEEP Ignore(o => o.RowVersion)) - .../Configurations/SubscriptionPlanConfiguration.cs - .../Configurations/OperatorSubscriptionConfiguration.cs - apps/identity/src/VietRide.Identity.Infrastructure/IdentityDbContext.cs (add DbSets + enum mappings) - apps/identity/src/VietRide.Identity.Infrastructure/Migrations/* (new migration + snapshot) - db-schema/identity-user/schema.sql (only if drift found) |
| forbidden scope | .env, secrets, any other service (apps/trip|booking|payment|parcel|gateway), apps/identity/.../Features/**, git branch/commit/push, User.cs (factories owned by 6.1/6.3), shared libs |
| depends on | 6.0a |
| invariant flags | CRLF on .cs - CPM no Version= - soft-delete deleted_at + partial unique WHERE deleted_at IS NULL (ADR 0003) on operators ONLY; operator_subscriptions and subscription_plans are NOT soft-delete (no deleted_at column — schema.sql:333-368,304-326) so DO NOT add one and DO NOT apply a soft-delete query filter to them - BaseEntity<TId> exposes RowVersion but schema.sql has no row_version column — keep Ignore(o => o.RowVersion) on Operator AND ignore RowVersion on SubscriptionPlan + OperatorSubscription configs - is_active separate activation flag on operators (not soft-delete) - Money BIGINT VND (plan prices) - no cross-DB FK - Domain depends on nothing |
| acceptance | dotnet build apps/identity/VietRide.Identity.sln -c Release 0 warn/err - dotnet format --verify-no-changes clean - dotnet ef database update applies clean on existing Day-5 DB via design-time factory; migration safely handles existing PK-only operator rows by adding required columns nullable/defaulted, backfilling deterministic LEGACY-* values from UUID shortId, then enforcing NOT NULL/unique constraints - backfilled rows keep `business_registration_number` and `tax_code` unique and satisfy partial unique indexes - migration emits NO row_version column and NO deleted_at on operator_subscriptions/subscription_plans - `OperatorConfiguration` XML/comment is updated so keeping `Ignore(o => o.RowVersion)` is explicitly intentional because the canonical schema has no `row_version` column - Down() reverses new tables/columns/indexes/types as far as PostgreSQL enum limitations allow - default Starter plan row present at 00000000-0000-0000-0000-000000000001 with limits from technical_context_v7.md:796-808 - model snapshot regenerated - existing tests/factories updated later as needed, NetArchTest layering still green |
| source citations | Operator columns: db-schema/identity-user/schema.sql:83-137; operator_registration_status enum: schema.sql:34-37 + BACKEND_SOURCE_OF_TRUTH.md:1597-1602; SubscriptionPlan/OperatorSubscription cols + enums: schema.sql:70-74,301-367 + technical_context_v7.md:752-819; Starter seed + fixed UUID: technical_context_v7.md:796-808 + db-schema README.md:34; policy JSONB shapes/defaults: schema.sql:105-137 + technical_context_v7.md:4726-4728; carry-over Ignore() removal: OperatorConfiguration.cs:30-37; initial PK-only migration: 20260531103145_InitIdentityAuth.cs:33-43 |

### Task 6.1 — Operator self-register + System-Admin manual create
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | apps/identity/src/VietRide.Identity.Application/Features/Operators/RegisterOperator/** - .../Features/Admin/CreateOperator/** - .../Abstractions/Repositories/IOperatorRepository.cs - .../Abstractions/Repositories/IOperatorSubscriptionRepository.cs - .../Abstractions/Repositories/ISubscriptionPlanRepository.cs - apps/identity/src/VietRide.Identity.Infrastructure/Persistence/Repositories/OperatorRepository.cs - .../Repositories/OperatorSubscriptionRepository.cs - .../Repositories/SubscriptionPlanRepository.cs - apps/identity/src/VietRide.Identity.Infrastructure/DependencyInjection* - apps/identity/src/VietRide.Identity.Domain/Entities/User.cs (add operator-admin factories only) - apps/identity/src/VietRide.Identity.Api/Controllers/OperatorsController.cs (new) - .../Controllers/AdminOperatorsController.cs (new) - .../Controllers/Requests/ (request DTOs) - apps/identity/tests/VietRide.Identity.UnitTests/Application/Operators/** - apps/identity/tests/VietRide.Identity.UnitTests/Domain/UserTests.cs (operator-admin factory/status cases) - apps/identity/tests/VietRide.Identity.IntegrationTests/Api/Operators* |
| forbidden scope | .env, secrets, other services, Gateway, Operator.cs/migrations (owned by 6.0), Features/Admin/CreateAdminUser/** except as pattern reference, git ops |
| depends on | 6.0a, 6.0 |
| invariant flags | CRLF - MediatR v11 - BCrypt 12 for self-register password - register transaction spans Operator+User+OperatorSubscription+EmailVerificationToken+ActivityLog (one UoW/TransactionBehavior) - self-register creates OPERATOR_ADMIN `PENDING_EMAIL_VERIFICATION` with PasswordHash set and sends OTP verify email - admin-create creates OPERATOR_ADMIN `PENDING_INITIAL_PASSWORD`, PasswordHash null, generates SET_INITIAL_PASSWORD token (48h) and sends account-created link using Day-5 services - initial OPERATOR_ADMIN counts toward subscription usage: both self-register and admin-create set `OperatorSubscription.currentOperatorUsers = 1` because `maxOperatorUsers` covers OPERATOR_ADMIN + OPERATOR_STAFF - soft-delete + partial-unique duplicate detection for operator BRN/tax and user email/phone; user duplicate email/phone maps to 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED` before DB constraint leakage - ApiResponse envelope - no Idempotency-Key (BSOT §5.6) - tenant: register public, admin-create requires SYSTEM_ADMIN claim - Day-6 admin-create = Starter Free-Trial default ONLY, no paid-plan / PENDING_PAYMENT path; reject any paid-plan/`planId` field with HTTP 422 `VALIDATION_ERROR` |
| acceptance | build + format clean - register creates Operator PENDING + OPERATOR_ADMIN PENDING_EMAIL_VERIFICATION with BCrypt cost-12 hash + OperatorSubscription PENDING_APPROVAL/currentOperatorUsers=1 + OTP token/email + ActivityLog CREATE_OPERATOR in one tx; response `{operatorId, message}` - after verify-email, login remains 403 `FORBIDDEN` until approve because operator is not APPROVED; after approve, login succeeds - register/admin-create duplicate biz-reg returns 409 OPERATOR_DUPLICATE_REGISTRATION, duplicate tax returns 409 OPERATOR_DUPLICATE_TAX_CODE, duplicate OPERATOR_ADMIN email returns 409 AUTH_EMAIL_ALREADY_REGISTERED, duplicate OPERATOR_ADMIN phone returns 409 AUTH_PHONE_ALREADY_REGISTERED (caught before DB constraint leakage) - admin-create returns contract-defined operator/admin/subscription summary, creates registrationStatus=APPROVED + subscription ACTIVE startedAt=now expiresAt=now+30d/currentOperatorUsers=1 using Starter plan + OPERATOR_ADMIN PENDING_INITIAL_PASSWORD + SET_INITIAL_PASSWORD token/email + ActivityLog CREATE_OPERATOR; after set-initial-password, login succeeds - paid-plan pick / `planId` / PENDING_PAYMENT request field is rejected with HTTP 422 `VALIDATION_ERROR` and never produces PENDING_PAYMENT - ActivityLog metadata is valid JSONB with required keys - >=1 happy + >=1 error test each - Swagger ProducesResponseType annotations |
| source citations | Path A flow/body/response/auto-subscription: technical_context_v7.md:585,599,811; password decision: human 2026-06-06 + technical_context_v7.md:1311; Path B manual create: technical_context_v7.md:597,819 + human decision initial-password link 2026-06-06; Day-5 SET_INITIAL_PASSWORD flow: technical_context_v7.md:535-541 + docs/handoff/day-5-checklist.md:127-136; duplicate error codes: BACKEND_SOURCE_OF_TRUTH.md:1327-1328,1385-1386; Gateway routes present: apps/gateway/src/config/routes.ts:52-69; mirror passwordless handler: Features/Admin/CreateAdminUser/CreateAdminUserCommandHandler.cs |

### Task 6.2 — Operator lifecycle: approve / reject / suspend
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | apps/identity/src/VietRide.Identity.Application/Features/Admin/ApproveOperator/** - .../Admin/SuspendOperator/** - .../Admin/RejectOperator/** - apps/identity/src/VietRide.Identity.Api/Controllers/AdminOperatorsController.cs (ADD lifecycle actions — shared with 6.1; serial) - .../Controllers/Requests/ (reason DTOs) - .../Abstractions/Repositories/IOperatorRepository.cs (add lifecycle lookups if missing) - apps/identity/tests/VietRide.Identity.UnitTests/Application/Operators/Lifecycle* - apps/identity/tests/VietRide.Identity.IntegrationTests/Api/AdminOperatorsLifecycle* |
| forbidden scope | .env, secrets, other services, Gateway, Operator.cs/migrations, repo IMPL file beyond coordinated methods, git ops |
| depends on | 6.0a, 6.0, 6.1 |
| invariant flags | CRLF - MediatR v11 - implement POST action lifecycle routes only; do NOT add PATCH lifecycle routes from the older timeline wording - approve/reject mutate Operator + OperatorSubscription atomically (one tx) - reject sets registrationStatus=REJECTED + rejectedAt/rejectedByUserId/rejectReason AND sets ONLY OperatorSubscription.status=CANCELLED on the PENDING_APPROVAL row — DO NOT set deletedAt - approve does PENDING->APPROVED ONLY in Day 6; SUSPENDED->APPROVED reactivation is NOT implemented (Q5) - status transitions via domain guard methods - lifecycle guard failures return HTTP 422 `VALIDATION_ERROR` (do not invent OPERATOR_INVALID_STATUS_TRANSITION) - ApiResponse envelope - SYSTEM_ADMIN-only - Outbox emission DEFERRED to Day 10 |
| acceptance | build + format clean - approve: PENDING->APPROVED + subscription ACTIVE startedAt=approvedAt expiresAt=approvedAt+30d, response `{operatorId, registrationStatus:"APPROVED"}`, ActivityLog APPROVE_OPERATOR with valid JSONB metadata - reject: body `{reason}` -> registrationStatus=REJECTED + rejectedAt/rejectedByUserId/rejectReason + PENDING_APPROVAL subscription set to status=CANCELLED with NO deletedAt, response `{operatorId, registrationStatus:"REJECTED"}`, ActivityLog REJECT_OPERATOR with valid JSONB metadata - suspend: ->SUSPENDED + suspendedAt/suspendReason, response `{operatorId, registrationStatus:"SUSPENDED"}`, NO ActivityLog because enum lacks SUSPEND_OPERATOR - approving/rejecting/suspending from an invalid state returns HTTP 422 `VALIDATION_ERROR` in ADR 0004 envelope - NO Outbox event written - >=1 happy + >=1 error per action |
| source citations | approve atomic transaction + 30-day trial auto-assign: technical_context_v7.md:585,811; reject atomic transaction: technical_context_v7.md:587-595; approve/reject/suspend response shapes: VietRide_API_Contract_v1.md §Operator/Admin Management; reject soft-cancel conflict resolved by schema.sql:333-368 + BACKEND_SOURCE_OF_TRUTH.md §6.6 Day-6 reject subscription rule; status enum: BACKEND_SOURCE_OF_TRUTH.md §6.7; ActivityLog action enum: schema.sql:51-68; events deferred: BE_TIMELINE_VU.md:119 |

### Task 6.2b — Identity login handler: block non-APPROVED operator login
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none) |
| owned files (write set) | apps/identity/src/VietRide.Identity.Application/Features/Auth/Login/LoginCommandHandler.cs - apps/identity/src/VietRide.Identity.Application/Abstractions/Repositories/IOperatorRepository.cs (add a by-id status read if not already added; coordinate, serial) - apps/identity/tests/VietRide.Identity.UnitTests/Application/Auth/Login* - apps/identity/tests/VietRide.Identity.IntegrationTests/Api/Auth* |
| forbidden scope | .env, secrets, other services, Gateway, Operator.cs/migrations (6.0), refresh/lockout logic beyond required ordering, git ops |
| depends on | 6.0a, 6.0, 6.1 |
| invariant flags | CRLF - MediatR v11 - login block enforced in Identity login handler, NO Gateway->Identity synchronous status hop - check applies to roles OPERATOR_ADMIN + OPERATOR_STAFF (UserRole.cs:8-9) when user.OperatorId is set; SYSTEM_ADMIN / PASSENGER / DRIVER unaffected - reject with 403 ForbiddenException using canonical code `FORBIDDEN` when operator is not APPROVED (PENDING/SUSPENDED/REJECTED); do not add `OPERATOR_NOT_APPROVED` - status check runs after credential success but before `RecordSuccessfulLogin`, lockout reset, refresh-token persistence, and access-token issuance - do NOT regress LOCKED / PENDING_EMAIL_VERIFICATION / PENDING_INITIAL_PASSWORD checks; preserve credential-check ordering as much as possible |
| acceptance | build + format clean - OPERATOR_ADMIN/OPERATOR_STAFF whose Operator.registrationStatus = PENDING gets 403 `FORBIDDEN` at login after email/password checks pass - SUSPENDED and REJECTED also get 403 `FORBIDDEN` - non-APPROVED operator login does not issue an access token, does not add a refresh-token row, does not update LastLoginAt, and does not reset failed-login counters unexpectedly - APPROVED operator's user logs in normally - SYSTEM_ADMIN/PASSENGER/DRIVER login paths unchanged - self-register path proves verify-email + approve + login succeeds with password set at register; admin-create path proves set-initial-password + login succeeds - existing login tests still green |
| source citations | block-until-APPROVED rule: technical_context_v7.md:585; login handler current state: apps/identity/src/VietRide.Identity.Application/Features/Auth/Login/LoginCommandHandler.cs:50-65; roles to gate: UserRole.cs:8-9; registration status enum: db-schema/identity-user/schema.sql:34-37 + BACKEND_SOURCE_OF_TRUTH.md:1597-1602 |

### Task 6.3 — Operator-created Driver/Assistant/OperatorStaff user creation (Day-5 carry-over)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | apps/identity/src/VietRide.Identity.Application/Features/OperatorUsers/CreateOperatorUser/** - apps/identity/src/VietRide.Identity.Api/Controllers/OperatorUsersController.cs (ADD POST create, preserve resend action) - apps/identity/src/VietRide.Identity.Api/Controllers/Requests/CreateOperatorUserRequest.cs - apps/identity/src/VietRide.Identity.Domain/Entities/User.cs (add operator-scoped pending-password factory/method only) - apps/identity/src/VietRide.Identity.Application/Abstractions/Repositories/IUserRepository.cs (only if create lookup helper needed) - .../IOperatorSubscriptionRepository.cs (limit/counter methods if missing) - apps/identity/src/VietRide.Identity.Infrastructure/Persistence/Repositories/OperatorSubscriptionRepository.cs (counter increment with concurrency/transaction) - apps/identity/tests/VietRide.Identity.UnitTests/Application/OperatorUsers/CreateOperatorUser* - apps/identity/tests/VietRide.Identity.UnitTests/Domain/UserTests.cs - apps/identity/tests/VietRide.Identity.IntegrationTests/Api/OperatorUsersEndpointsTests.cs |
| forbidden scope | .env, secrets, Gateway route table (already exists for /v1/operator/users), admin-create operator handlers (6.1), lifecycle handlers (6.2), Operator.cs/migrations, git ops |
| depends on | 6.0a, 6.0, 6.1 |
| invariant flags | CRLF - MediatR v11 - OPERATOR_ADMIN-only at Gateway and handler - caller's current Operator.registrationStatus must be APPROVED at handler time for both create and resend; if a token was issued before suspend/reject, `POST /v1/operator/users` returns 403 `FORBIDDEN` with no user/counter/token side effects, and `POST /v1/operator/users/{userId}/resend-initial-password` returns 403 `FORBIDDEN` with no token revoke/create, email, or ActivityLog side effects - tenant isolation: caller operatorId claim required and is the created user's OperatorId; no operatorId from body - allowed roles only DRIVER, ASSISTANT, OPERATOR_STAFF; never creates OPERATOR_ADMIN/SYSTEM_ADMIN/PASSENGER - user starts PENDING_INITIAL_PASSWORD and PasswordHash null - duplicate target email/phone maps to 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED` before DB constraint leakage - generate SET_INITIAL_PASSWORD token TTL 48h + send account-created link using Day-5 services - enforce subscription limits before insert: role DRIVER -> maxDrivers/currentDrivers; ASSISTANT -> maxAssistants/currentAssistants; OPERATOR_STAFF -> maxOperatorUsers/currentOperatorUsers where currentOperatorUsers already includes the initial OPERATOR_ADMIN from Task 6.1; increment matching counter atomically without concurrent overshoot (do not rely on default read-check-update; use a conditional update, row lock/`FOR UPDATE`, serializable transaction, or equivalent repository method) in the same transaction - no Idempotency-Key - ActivityLog SET_INITIAL_PASSWORD with `activity_logs.user_id = callerUserId` (actor convention) and metadata built via `JsonSerializer.Serialize(new { ... })` identifying actorUserId/callerUserId, targetUserId, operatorId, and action source `OPERATOR_USER_CREATE` (so it is not confused with the later user self-consuming the token) |
| acceptance | build + format clean - OPERATOR_ADMIN creates DRIVER/ASSISTANT/OPERATOR_STAFF under own APPROVED operator; response matches contract; token/email generated; `POST /v1/auth/set-initial-password` sets password and login succeeds for APPROVED operator - valid pre-suspend token cannot create an operator user after the operator is SUSPENDED/REJECTED (403 `FORBIDDEN`, no user/counter/token side effects) - valid pre-suspend token cannot resend initial-password after the operator is SUSPENDED/REJECTED (403 `FORBIDDEN`, no token revoke/create, email, or ActivityLog side effects) - non-admin/missing operatorId/cross-operator spoof attempt rejected 403 (structural: no operatorId body) - role outside allowed set returns 422 VALIDATION_ERROR - duplicate target email returns 409 AUTH_EMAIL_ALREADY_REGISTERED, duplicate target phone returns 409 AUTH_PHONE_ALREADY_REGISTERED - exceeding maxDrivers/maxAssistants/maxOperatorUsers returns 422 SUBSCRIPTION_LIMIT_EXCEEDED and does not create user/counter - counters increment exactly once in happy path - concurrent limit test proves no over-increment when remaining capacity is smaller than concurrent deltas - ActivityLog metadata is valid JSONB with required keys - existing resend initial-password successful behavior/response shape preserved |
| source citations | Day-5 mandatory carry-over: docs/handoff/day-5-plan.md:14 + docs/handoff/day-5-checklist.md:127-136; Driver/Assistant flow: technical_context_v7.md:535-541,1310; Operator Staff/Admin auth: technical_context_v7.md:1311; subscription limits: technical_context_v7.md:700-715; Gateway /v1/operator/users route: apps/gateway/src/config/routes.ts:70-75; create/resend contract: VietRide_API_Contract_v1.md §Identity & User Service resend-initial-password + §Operator/Admin Management |

### Task 6.4 — Operator profile read + update with policy JSONB
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | apps/identity/src/VietRide.Identity.Application/Features/Operators/GetOperatorProfile/** - .../Operators/UpdateOperatorProfile/** (incl. policy JSONB validator) - apps/identity/src/VietRide.Identity.Api/Controllers/OperatorProfileController.cs (new; routes [HttpGet]/[HttpPatch] under /v1/operator/profile) - .../Controllers/Requests/ (update DTO) - .../Abstractions/Repositories/IOperatorRepository.cs (add by-id read/update if missing) - apps/identity/tests/VietRide.Identity.UnitTests/Application/Operators/Profile* - apps/identity/tests/VietRide.Identity.IntegrationTests/Api/OperatorProfile* |
| forbidden scope | .env, secrets, other services, Operator.cs/migrations (6.0), admin lifecycle handlers (6.2), operator-user create handler (6.3), git ops |
| depends on | 6.0a, 6.0, 6.1 |
| invariant flags | CRLF - MediatR v11 - paths GET /v1/operator/profile + PATCH /v1/operator/profile, operatorId taken from caller JWT claim, NO `{id}` in path - tenant isolation is structural: caller can only read/update their own operator - RBAC/status: GET read is allowed for OPERATOR_ADMIN+OPERATOR_STAFF even when Operator.registrationStatus is non-APPROVED; PATCH requires OPERATOR_ADMIN AND current Operator.registrationStatus=APPROVED (STAFF 403; pre-suspend tokens 403 after suspend/reject) - policy JSONB validated against documented shapes/defaults - ApiResponse envelope - no Idempotency-Key |
| acceptance | build + format clean - OPERATOR_ADMIN and OPERATOR_STAFF read own operator profile (GET) incl. 3 policy JSONB fields; GET remains allowed for non-APPROVED operators so the UI can display status/policies - OPERATOR_ADMIN updates profile + policies (PATCH) only while current Operator.registrationStatus=APPROVED; a valid pre-suspend token gets 403 `FORBIDDEN` on PATCH after suspend/reject - invalid policy shape returns 422 VALIDATION_ERROR - OPERATOR_STAFF update returns 403 FORBIDDEN - caller with no operatorId claim rejected 403 - cancellationPolicy accepted as sorted `[{hoursBeforeDeparture,feePercent}]`; parcelNoShowPolicy default `{noShowFeePercent:0,additionalPaymentTimeoutMinutes:30}`; luggagePolicy default `{defaultLuggageKgPerSeat:10}` - >=1 happy + >=1 error test |
| source citations | policy JSONB shapes/defaults: schema.sql:105-137 + technical_context_v7.md:4726-4728; timeline profile/policy review: BE_TIMELINE_VU.md:85-87; operatorId immutable: technical_context_v7.md:4693; RBAC pattern: BACKEND_SOURCE_OF_TRUTH.md:1617-1636; profile path decision Q2/Q8 |

### Task 6.5 — Internal operator/subscription endpoints for cross-service validation and usage counters
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | apps/identity/src/VietRide.Identity.Application/Features/Internal/Operators/GetInternalOperator/** - .../GetInternalOperatorSubscription/** - .../IncrementOperatorUsage/** - apps/identity/src/VietRide.Identity.Api/Controllers/InternalOperatorsController.cs (new or extend internal controller pattern) - apps/identity/src/VietRide.Identity.Api/Controllers/Requests/IncrementOperatorUsageRequest.cs - apps/identity/src/VietRide.Identity.Application/Abstractions/Repositories/IOperatorRepository.cs - .../IOperatorSubscriptionRepository.cs - apps/identity/src/VietRide.Identity.Infrastructure/Persistence/Repositories/OperatorRepository.cs - .../OperatorSubscriptionRepository.cs - apps/identity/tests/VietRide.Identity.UnitTests/Application/Internal/Operators/** - apps/identity/tests/VietRide.Identity.IntegrationTests/Api/InternalOperatorsEndpointsTests.cs |
| forbidden scope | .env, secrets, Gateway (internal endpoints are NOT via Gateway), other services, Operator.cs/migrations, FE-facing operator controllers, git ops |
| depends on | 6.0a, 6.0, 6.1 |
| invariant flags | CRLF - MediatR v11 - Internal endpoint exception for add-endpoint workflow: DO NOT add a Gateway route for these `/internal/v1/*` endpoints - Internal JWT required (`X-Internal-Auth`) - internal success payloads are raw DTOs (no ApiResponse success envelope), internal errors use ADR 0004 error envelope (BSOT §5.4) - no cross-DB FK - no Idempotency-Key - usage increment body `{resource, delta}` with positive delta; map resources to counters and max limits; update atomically without concurrent overshoot (do not rely on default read-check-update; use a conditional update, row lock/`FOR UPDATE`, serializable transaction, or equivalent repository method) in one transaction; overflow returns 422 SUBSCRIPTION_LIMIT_EXCEEDED; module disabled/expired statuses use existing BSOT codes if relevant |
| acceptance | build + format clean - anonymous/user-JWT calls to internal endpoints rejected; valid Internal JWT allowed - GET operator returns raw lookup DTO for existing operator and 404 RESOURCE_NOT_FOUND for missing - GET subscription returns raw status + plan + limits + usage counters + module flags - POST usage/increment increments VEHICLES/DRIVERS/ASSISTANTS/OPERATOR_USERS/ROUTES/TRIPS_THIS_MONTH counters according to resource; current+delta > max returns SUBSCRIPTION_LIMIT_EXCEEDED with no increment; unknown resource/invalid delta returns 422 VALIDATION_ERROR; concurrent test or transaction-level test proves no over-increment beyond limit (for example, remaining capacity 1 with two concurrent delta=1 calls results in exactly one success) - internal endpoints are NOT added to Gateway routes |
| source citations | BSOT internal registry: BACKEND_SOURCE_OF_TRUTH.md:1649-1663; internal response convention: BACKEND_SOURCE_OF_TRUTH.md:1240; subscription limits/counters: technical_context_v7.md:700-720,780-789; user decision generic resource+delta 2026-06-06 |

### Task 6.6 — Gateway: operator-profile route (route-only)
| Field | Value |
|---|---|
| stack/owner | nest |
| implement agent | nest-worker |
| review agent | nest-reviewer |
| skill | (none) |
| owned files (write set) | apps/gateway/src/config/routes.ts (ADD the /v1/operator/profile prefix, authRequired user, requiredRoles [OPERATOR_ADMIN, OPERATOR_STAFF]) - apps/gateway/src/config/routes.spec.ts - apps/gateway/src/proxy/proxy.access-gates.spec.ts |
| forbidden scope | .env, secrets, .NET services, internal-jwt secret handling, other Gateway domains, proxy.middleware.ts operator-status gate (NOT added), git ops |
| depends on | 6.4 (route prefix/roles fixed by contract baseline) |
| invariant flags | LF on .ts - no banned deps - keep longest-prefix-wins match semantics: /v1/operator/profile and existing /v1/operator/users must match distinctly; do NOT add a bare /v1/operator prefix - do not weaken existing public/role gates - this task is ROUTE-ONLY: do NOT add an operator-status gate to the proxy - Gateway allows OPERATOR_STAFF for the prefix because GET is allowed; PATCH staff is blocked by Identity app handler |
| acceptance | npx nx run-many -t build lint test --all --exclude="VietRide.*" green for gateway - /v1/operator/profile route added (authRequired user, requiredRoles [OPERATOR_ADMIN, OPERATOR_STAFF]) and covered by routes.spec.ts - routes.spec proves longest-prefix behavior explicitly: `/v1/operator/profile` resolves to the profile route, `/v1/operator/users` still resolves to the users route, the two entries are distinct, and no generic `/v1/operator` prefix exists - access-gate spec covers OPERATOR_ADMIN and OPERATOR_STAFF allowed at Gateway, other roles blocked - NO operator-status gate added to proxy |
| source citations | existing routes incl. /v1/operator/users: apps/gateway/src/config/routes.ts:52-75; profile path /v1/operator/profile (Q2); RBAC decision staff read allowed (Q8); block-non-APPROVED login + app-layer write guard decision (Q3/Q18; technical_context_v7.md:585) |

### Task 6.7 — Postman / final docs sync for Operator + Subscription
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer |
| skill | (none) |
| owned files (write set) | docs/api/postman/vietride.postman_collection.json (operator register -> verify-email -> approve -> login -> profile update; admin-create -> set-initial-password -> login; operator-user create -> set-initial-password -> login; reject/suspend optional) - docs/api/postman/*environment*.json (only if new vars needed) - VietRide_API_Contract_v1.md (only if drift from Task 6.0a is discovered; otherwise read/verify only) - BACKEND_SOURCE_OF_TRUTH.md (only if changelog/registry drift from Task 6.0a is discovered; otherwise read/verify only) |
| forbidden scope | .env, secrets, any apps/** or libs/** code, db-schema/**, git ops |
| depends on | 6.1, 6.2, 6.2b, 6.3, 6.4, 6.5, 6.6 |
| invariant flags | LF on .md/.json - Postman JSON must parse - response-shape assertions match API contract - no new error/event/endpoint drift - do not contradict technical_context_v7/BSOT |
| acceptance | Postman collection parses and contains Day-6 operator items with response-shape assertions for the three required E2E paths; final contract remains in sync with shipped DTOs; BSOT changelog/registry remains in sync; no duplicate changelog row if 6.0a already did it |
| source citations | contract baseline Task 6.0a; Day-5 sync precedent: docs/handoff/day-5-checklist.md:125; Postman location precedent: docs/api/postman/vietride.postman_collection.json |

## Dispatch order
1. **Task 6.0a** (contract baseline) — completed in this session; keep as immutable baseline unless reviewer-approved drift is needed. parallel-safe: no.
2. **Task 6.0** (domain/EF/migration baseline), next implementation task. parallel-safe: no.
3. **Task 6.1** (register + admin-create + base repos/controllers), after 6.0. parallel-safe: no.
4. **Task 6.2** (approve/reject/suspend), after 6.1. parallel-safe: no (shared AdminOperatorsController + IOperatorRepository).
5. **Task 6.2b** (login handler block), after 6.1. Serialize with 6.2/6.3/6.4/6.5 if IOperatorRepository edits overlap.
6. **Task 6.3** (operator-created users carry-over), after 6.1. Serialize with 6.2b/6.5 if IOperatorSubscriptionRepository edits overlap.
7. **Task 6.4** (profile), after 6.1. Serialize with other IOperatorRepository edits unless already merged.
8. **Task 6.5** (internal endpoints), after 6.1. Serialize with 6.3 for counter repository methods.
9. **Task 6.6** (Gateway route-only), after 6.4. parallel-safe with 6.2b/6.3/6.5 if no .NET/Gateway conflict; default serial in one working tree.
10. **Task 6.7** (Postman/final docs sync), last after implementation and tests.

## Progress tracker
> Orchestrator bookkeeping — informational only, NOT audit evidence. /audit-day re-verifies independently.

| Task | Status | Review verdict | Date | Notes |
|---|---|---|---|---|
| 6.0a | ✅ done | reviewer-approved | 2026-06-07 | contract baseline synced in API Contract + BSOT; no new error code/Idempotency-Key/Outbox emission |
| 6.0 | ✅ done | dotnet-reviewer-approved | 2026-06-07 | 2 patch rounds; human verify pending; DB/integration rerun needs local Postgres |
| 6.1 | ⬜ todo | — | — | self-register password + admin-create initial-password |
| 6.2 | ⬜ todo | — | — | lifecycle |
| 6.2b | ⬜ todo | — | — | login-block |
| 6.3 | ⬜ todo | — | — | Day-5 operator-user carry-over |
| 6.4 | ⬜ todo | — | — | profile app RBAC |
| 6.5 | ⬜ todo | — | — | internal endpoints |
| 6.6 | ⬜ todo | — | — | Gateway profile route |
| 6.7 | ⬜ todo | — | — | Postman/final docs |

Legend: ⬜ todo · 🔄 in progress · ✅ done (reviewer APPROVED + human `/verify`) · ⚠️ done-with-carryover · ❌ blocked

## Open questions
> All Day-6 plan questions are resolved by the BE lead on 2026-06-06 / 2026-06-07. Kept here as a decision log; tasks above are un-gated accordingly.

- **Q1 — Reject endpoint shape. RESOLVED 2026-06-06.** Add to API contract mirroring approve/suspend: POST /v1/admin/operators/{operatorId}/reject, body `{reason}`, response `{operatorId, registrationStatus:"REJECTED"}`. Reject sets Operator REJECTED + rejected metadata and sets the PENDING_APPROVAL OperatorSubscription to status=CANCELLED ONLY — NO deletedAt.
- **Q2 — Operator profile endpoint path. RESOLVED 2026-06-06.** Use GET/PATCH `/v1/operator/profile`; operatorId from JWT claim; no `{id}` in path.
- **Q3 — Where is the "block until operator APPROVED" rule enforced? RESOLVED 2026-06-06; amended 2026-06-07.** Identity login handler blocks new logins; no Gateway->Identity synchronous status hop. Because tokens can outlive a later suspend/reject, Day 6 also adds application-layer current-status guards on operator write/action handlers (see Q18).
- **Q4 — POST /v1/admin/operators plan pick. RESOLVED 2026-06-06; clarified 2026-06-07.** Day 6 supports only Starter Free-Trial default; paid-plan pick/PENDING_PAYMENT deferred to Sprint 5 / Day 37. If the Day-6 request includes any paid-plan/`planId` field, reject with HTTP 422 `VALIDATION_ERROR` (do not silently ignore).
- **Q5 — SUSPENDED -> APPROVED reactivation. RESOLVED 2026-06-06.** Defer; Day 6 approve only handles PENDING->APPROVED.
- **Q6 — OPERATOR_ADMIN self-register password. RESOLVED 2026-06-06.** `POST /v1/operators/register` accepts password and hashes BCrypt cost 12; the self-registered OPERATOR_ADMIN remains PENDING_EMAIL_VERIFICATION until OTP verify, then is blocked by Operator status until approval.
- **Q7 — System Admin manual-create OPERATOR_ADMIN password. RESOLVED 2026-06-06.** `POST /v1/admin/operators` creates OPERATOR_ADMIN `PENDING_INITIAL_PASSWORD` and sends SET_INITIAL_PASSWORD link; no password in admin-create body.
- **Q8 — Operator profile staff read. RESOLVED 2026-06-06.** OPERATOR_STAFF can GET profile; only OPERATOR_ADMIN can PATCH. Gateway allows both roles for `/v1/operator/profile`, app blocks staff PATCH.
- **Q9 — Internal endpoints scope and usage body. RESOLVED 2026-06-06.** Implement the three BSOT internal endpoints in Day 6. Usage increment body is generic `{resource, delta}` with resources `VEHICLES|DRIVERS|ASSISTANTS|OPERATOR_USERS|ROUTES|TRIPS_THIS_MONTH`.
- **Q10 — Existing PK-only operator rows in migration. RESOLVED 2026-06-06 (Hướng A).** If the existing Day-5 DB has legacy `operators` rows with only `id`, the Day-6 migration keeps them and backfills deterministic LEGACY-* placeholder values derived from UUID shortId before enforcing NOT NULL and unique constraints. It must not abort solely because legacy rows exist.
- **Q11 — Initial OPERATOR_ADMIN subscription usage. RESOLVED 2026-06-06.** The initial OPERATOR_ADMIN created by self-register/admin-create counts toward `maxOperatorUsers`; Task 6.1 sets `currentOperatorUsers = 1`, and later OPERATOR_STAFF creation increments from there.
- **Q12 — Expanded Day-6 scope. RESOLVED 2026-06-07.** Keep the expanded plan scope (operator-created users, Identity login block, internal operator/subscription endpoints, usage counters, OPERATOR_STAFF profile read) because it closes the Day-5 carry-over and provides Day 7+ prerequisites.
- **Q13 — Invalid lifecycle transition error. RESOLVED 2026-06-07.** Approve/reject/suspend guard failures return HTTP 422 with canonical `VALIDATION_ERROR`; do not add a new `OPERATOR_INVALID_STATUS_TRANSITION` code.
- **Q14 — User duplicate handling for Day-6 user-create endpoints. RESOLVED 2026-06-07.** `POST /v1/operators/register`, `POST /v1/admin/operators`, and `POST /v1/operator/users` must catch duplicate user email/phone before DB constraint leakage and return 409 `AUTH_EMAIL_ALREADY_REGISTERED` / `AUTH_PHONE_ALREADY_REGISTERED`.
- **Q15 — Non-APPROVED operator login error code. RESOLVED 2026-06-07.** Use HTTP 403 `FORBIDDEN`; do not add `OPERATOR_NOT_APPROVED`.
- **Q16 — ActivityLog actor convention. RESOLVED 2026-06-07.** `activity_logs.user_id` is the actor user id for Day-6 actions; authenticated actions use caller user id, and public self-register uses the newly created OPERATOR_ADMIN as self actor. Metadata carries `operatorId`, `actorUserId`, `targetUserId` when different from actor, and `source`.
- **Q17 — Reject subscription no-`deletedAt` SOT conflict. RESOLVED 2026-06-07.** Despite technical_context_v7.md:587-594 mentioning `deletedAt`, the Day-6 canonical implementation follows the current canonical DDL (`operator_subscriptions` has no `deleted_at`) and sets only `OperatorSubscription.status = CANCELLED`; Task 6.0a must record this conflict resolution in BSOT/API changelog before feature code.
- **Q18 — Access tokens issued before operator suspend/reject. RESOLVED 2026-06-07.** Use app-layer current-status guards instead of a Gateway->Identity sync hop. `POST /v1/operator/users`, `POST /v1/operator/users/{userId}/resend-initial-password`, and `PATCH /v1/operator/profile` require the caller's current Operator.registrationStatus=APPROVED and return 403 `FORBIDDEN` without side effects when the operator is later SUSPENDED/REJECTED. `GET /v1/operator/profile` remains readable for OPERATOR_ADMIN/OPERATOR_STAFF even when non-APPROVED so the UI can display current status/policies.

## Carry-over for a later day
- **Operator reactivation (SUSPENDED -> APPROVED).** Deferred from Day 6 per Q5. Needs a reactivate action/endpoint + state-machine transition (BACKEND_SOURCE_OF_TRUTH.md:1923, technical_context_v7.md:4490) and an ActivityLog value if canonical DDL adds one. Schedule on a later operator-admin day so SUSPENDED operators can be restored.
- **Paid subscription plan pick / PENDING_PAYMENT onboarding.** Deferred to Sprint 5 / Day 37 per Q4; Day 6 keeps Starter Free-Trial only.
- **Day 10 Outbox wiring.** Day 6 registers/keeps the event contract only; Day 10 must emit `identity.operator.approved` and `identity.operator.suspended` for the lifecycle transitions shipped here, alongside `staff.password_set` per BE_TIMELINE_VU.md:119.
