# Day 14 — Plan (RE-PLANNED: operator self-create vouchers added)

- **Timeline ref**: BE_TIMELINE_VU.md -> Day 14 (Jira: SCV-86)
- **Prior checklist**: docs/handoff/day-13-checklist.md (found — READY; carry-overs: Day-17 cancel independence; Outbox publisher wiring is a pre-existing carry-over, NOT Day-14 scope)
- **Plan status**: APPROVED (reviewer PLAN-REVIEW gate, 2026-06-20) — ready for /implement-task 14.0a
- **Re-plan note**: human (BE lead) expanded scope on 2026-06-19 to ALSO allow operators to self-create operator-funded vouchers (full CRUD) in Day 14. This changes canonical SOT (v7 4.4 currently forbids operator self-create at v7:462, v7:573, v7:624) and the booking schema. SOT edits are mandated FIRST (Task 14.0a) before any EF/code. Design decisions in the re-plan brief are human-approved — do NOT re-litigate them.
- **Residual-Q resolutions applied (2026-06-19, human-approved)**: Q3, Q6, Q7, Q8 RESOLVED — see Open questions for the recorded decisions. Net changes: (Q6) freeze-on-first-use immutability guard on operator voucher PATCH (Task 14.1b) using new error code VOUCHER_LOCKED 409; (Q7) operator self-create emits NO event + a NEW admin oversight endpoint GET /v1/admin/vouchers (SYSTEM_ADMIN, optional filters) added to Tasks 14.0a/14.1/14.5; (Q8) canonical checkout validation order pinned in Task 14.3 with consent as an applicability-stage gate; (Q3) OPERATOR_FUNDED + null applicableOperatorIds -> VALIDATION_ERROR.

## Objective
Day 14 delivers voucher checkout for the Booking service: the Voucher / VoucherUsage / OperatorVoucherConsent aggregates (EF + migration), the System-Admin voucher-create endpoint, operator consent accept/reject endpoints, NEW operator self-service voucher CRUD (/v1/operator/vouchers), and voucher validation + discount application wired into the existing POST /v1/bookings (and round-trip) checkout. This replaces the discountAmount = Money.Zero voucher stub at CreateBookingCommandHandler.cs:142. It unblocks Day 15/16 payment+settlement (funding-type drives who bears the discount) and is required for the Sprint 3 demo voucher-reduces-total path.

## Success criteria (DoD — binary, verifiable)
- [ ] Canonical SOT edited FIRST (Task 14.0a) and human-approved: db-schema/booking/schema.sql (new owner_operator_id, name, deleted_at cols + CHECK + comments + soft-delete partial unique index on code), v7 sec 4.4 (operator self-create flow + consent-skip), BSOT sec 5.9 (+ new error codes VOUCHER_FORBIDDEN_FUNDING 422, VOUCHER_CODE_CONFLICT 409, VOUCHER_LOCKED 409) + sec 13 changelog bump, VietRide_API_Contract_v1.md (admin + operator voucher endpoints incl. GET /v1/admin/vouchers oversight list).
- [ ] EF migration creates vouchers (incl. owner_operator_id, name, deleted_at), voucher_usages, operator_voucher_consents + 3 enums matching the EDITED schema; applies, rolls back, reapplies clean on a temp DB.
- [ ] POST /v1/admin/vouchers (SYSTEM_ADMIN) creates a platform voucher (owner_operator_id NULL); OPERATOR_FUNDED INSERTs one PENDING consent per targeted operator; auto-code = unique 8-char uppercase base32; duplicate code -> VOUCHER_CODE_CONFLICT.
- [ ] GET /v1/admin/vouchers (SYSTEM_ADMIN) returns the platform-governance voucher list with optional filters ?ownerOperatorId= / ?fundingType= / ?isActive= (audit of operator-created vouchers); SYSTEM_ADMIN only (Q7 oversight).
- [ ] POST /v1/operator/vouchers (OPERATOR_ADMIN) creates owner_operator_id=caller, funding_type FORCED OPERATOR_FUNDED, NO consent rows (self-consented); duplicate global code -> VOUCHER_CODE_CONFLICT; fundingType not OPERATOR_FUNDED -> VOUCHER_FORBIDDEN_FUNDING.
- [ ] Operator voucher CRUD: PATCH (update), DELETE (soft-delete deleted_at), POST .../activate + /deactivate — all scoped to owner_operator_id == caller (cross-operator -> 404 VOUCHER_NOT_FOUND).
- [ ] Operator voucher PATCH obeys freeze-on-first-use (Q6): code/type/fundingType/ownerOperatorId ALWAYS immutable; while voucher_usages count == 0 the operator may edit name/value/minOrderAmount/maxDiscountAmount/totalUsageLimit/perUserLimit/validFrom/validUntil/applicableRouteIds; once >=1 usage exists the economic fields value/minOrderAmount/maxDiscountAmount FREEZE (editing them -> VOUCHER_LOCKED 409) and only name, EXTENDING validUntil, LOOSENING limits, applicableRouteIds, and deactivate remain editable.
- [ ] Voucher applied at checkout reduces totalAmount by computed discount (PERCENT_OFF capped at max_discount_amount, FIXED_AMOUNT), rounded to nearest dong half-up (MidpointRounding.AwayFromZero, NO floor-1000); a voucher_usages row created with funded_by snapshot.
- [ ] Checkout applicability branch: applies iff EITHER (a) owner_operator_id == booking operatorId (operator-owned, self-funded, NO consent check), OR (b) owner_operator_id IS NULL AND (VIETRIDE_FUNDED scope match, OR OPERATOR_FUNDED with ACCEPTED consent). Other failures -> registered code (VOUCHER_NOT_FOUND / _EXPIRED / _NOT_APPLICABLE / _USAGE_LIMIT_REACHED / _USER_LIMIT_REACHED / _MIN_ORDER_NOT_MET).
- [ ] OPERATOR_FUNDED admin voucher without ACCEPTED consent -> VOUCHER_NOT_APPLICABLE (does not reveal opt-in mechanics).
- [ ] Operator consent endpoints work: GET .../voucher-consents?status=, POST .../{id}/accept (OPERATOR_ADMIN, precond PENDING), POST .../{id}/reject (OPERATOR_ADMIN, precond PENDING|ACCEPTED, optional reason); emit booking.voucher.consent_accepted / _rejected via Outbox.
- [ ] Revoke after accept (ACCEPTED -> REJECTED) does NOT roll back discount on already-CONFIRMED bookings.
- [ ] Round-trip uses 2 VoucherUsage records — validated/applied per leg independently.
- [ ] Usage-limit boundary covered by an executed test (Nth succeeds, N+1th -> VOUCHER_USAGE_LIMIT_REACHED).
- [ ] Booking solution builds, dotnet format --verify-no-changes clean, unit + integration tests green; NetArchTest layering passes.
- [ ] Gateway routes proxy the new admin + operator endpoints with correct role gates; access-gates spec covers them.

## Contract changes
- **Canonical SOT edits (Task 14.0a — DO FIRST, human approves the exact DDL/shapes below):**
  - **db-schema/booking/schema.sql vouchers** — add three columns + one CHECK + comment + soft-delete-aware code uniqueness. Proposed exact DDL (worker mirrors into the table def + index block at schema.sql:229-254):
    - name VARCHAR(120) NOT NULL — passenger-facing display name; NOT unique.
    - owner_operator_id UUID NULL — logical FK; NULL = platform/admin voucher, non-null = operator self-created (scoped to that operator).
    - deleted_at TIMESTAMPTZ NULL — soft-delete (ADR 0003); operator CRUD soft-deletes.
    - CONSTRAINT chk_vouchers_operator_owned_funding CHECK (owner_operator_id IS NULL OR funding_type = 'OPERATOR_FUNDED'::voucher_funding_type) — note the explicit `::voucher_funding_type` cast (required for Postgres enum comparison).
    - Replace the plain uq_vouchers_code with a partial unique index WHERE deleted_at IS NULL (ADR 0003).
    - Add idx_vouchers_owner_operator ON vouchers (owner_operator_id) WHERE owner_operator_id IS NOT NULL AND deleted_at IS NULL.
    - COMMENT ON COLUMN vouchers.owner_operator_id documenting platform-vs-operator-owned + self-consent semantics.
    - Update COMMENT ON COLUMN vouchers.created_by_user_id (currently "logical FK SYSTEM_ADMIN" at schema.sql:244) to reflect BOTH SYSTEM_ADMIN (platform vouchers) AND OPERATOR_ADMIN (operator-owned vouchers).
    - NOTE for human: current vouchers table has NO deleted_at (only is_active). Operator soft-delete needs it; ADR 0003 then forces the partial-unique code index (a soft-deleted code becomes reusable). is_active stays as the orthogonal activation flag. See Open Q5.
  - **v7 sec 4.4 (v7:462, v7:573, v7:624, funding block v7:626-655)** — replace the operator-cannot-create absolutes with: admin creates platform vouchers (both funding types) AND operators MAY self-create operator-owned OPERATOR_FUNDED vouchers (full CRUD) scoped to their own operator, self-consented (no consent fan-out). Document the consent-skip + the checkout applicability branch (owner_operator_id == operatorId path). Keep the admin OPERATOR_FUNDED consent flow (v7:640-646) UNCHANGED.
  - **BSOT sec 5.9** — add THREE rows under Voucher (after BSOT:1352): VOUCHER_FORBIDDEN_FUNDING (422), VOUCHER_CODE_CONFLICT (409), and VOUCHER_LOCKED (409 — economic field edited on a voucher that already has >=1 usage; Q6 freeze-on-first-use). Verified absent from BSOT sec 5.9 (no existing code fits a post-use field freeze). Bump sec 13 changelog (MINOR — new feature surface + 3 error codes + schema cols + admin oversight list endpoint).
  - **VietRide_API_Contract_v1.md** — add admin + operator voucher endpoint section (shapes below).
- **New REST endpoints** (Booking service, behind Gateway):
  - POST /v1/admin/vouchers — SYSTEM_ADMIN. Request DTO (Open Q1 resolution, from schema cols): code (string or null; null => auto 8-char base32), name (string), type (PERCENT_OFF or FIXED_AMOUNT), value (int), minOrderAmount (long), maxDiscountAmount (long or null), totalUsageLimit (int or null), perUserLimit (int or null), validFrom, validUntil, applicableOperatorIds (uuid array or null; OPERATOR_FUNDED requires non-null — Q3 deferral), applicableRouteIds (uuid array or null), fundingType (VIETRIDE_FUNDED or OPERATOR_FUNDED). Response: ApiResponse of { id, code, name, type, value, fundingType, ownerOperatorId=null, isActive, validFrom, validUntil, createdAt }.
  - **GET /v1/admin/vouchers — SYSTEM_ADMIN (Q7 oversight, NEW).** Platform-governance list of ALL vouchers (admin + operator-created) for audit. Optional query filters: ?ownerOperatorId=<uuid> (operator-owned vouchers of one operator; omit/null is not special-cased — returns all), ?fundingType=<VIETRIDE_FUNDED|OPERATOR_FUNDED>, ?isActive=<bool>, plus standard QueryOptions paging/sort (BSOT sec 5.7/5.8 whitelist; default sortBy=createdAt desc). Response: ApiResponse of paged list of { id, code, name, type, value, fundingType, ownerOperatorId, isActive, validFrom, validUntil, createdAt } with meta paging. Read-only; no Idempotency-Key.
  - GET /v1/admin/vouchers/{voucherId}/consents — SYSTEM_ADMIN. Source: v7:685-688.
  - **POST /v1/operator/vouchers — OPERATOR_ADMIN.** Same request MINUS admin-only fields: fundingType FORCED OPERATOR_FUNDED (else VOUCHER_FORBIDDEN_FUNDING), applicableOperatorIds FORCED to the caller operator only, ownerOperatorId set server-side. Accepts code, name, type, value, minOrderAmount, maxDiscountAmount, totalUsageLimit, perUserLimit, validFrom, validUntil, applicableRouteIds. Response same shape with ownerOperatorId populated.
  - **PATCH /v1/operator/vouchers/{id} — OPERATOR_ADMIN.** Update mutable fields (name, value, minOrderAmount, maxDiscountAmount, totalUsageLimit, perUserLimit, validFrom, validUntil, applicableRouteIds). code, type, fundingType, ownerOperatorId immutable. Scoped to owner. PATCH for partial update per prior reviewer note.
  - **DELETE /v1/operator/vouchers/{id} — OPERATOR_ADMIN.** Soft-delete (sets deleted_at). No Idempotency-Key. Scoped to owner.
  - **POST /v1/operator/vouchers/{id}/activate + /deactivate — OPERATOR_ADMIN.** Flip is_active (IActivatable). No body, no Idempotency-Key, behavior-idempotent. Scoped to owner.
  - GET /v1/operator/voucher-consents?status= — OPERATOR_ADMIN/STAFF. Source: v7:659-663.
  - POST /v1/operator/voucher-consents/{id}/accept — OPERATOR_ADMIN. Source: v7:665-672.
  - POST /v1/operator/voucher-consents/{id}/reject (body reason optional text) — OPERATOR_ADMIN. Source: v7:674-683.
  - **Note (consent endpoint shape, v7 wins over timeline):** timeline line 160 wrote a single PATCH; v7:665-683 (higher authority) is two POSTs. Plan follows v7.
- **Changed endpoint**: POST /v1/bookings + POST /v1/bookings/round-trip — voucherCode now functional. Source: API contract Booking 700-789; v7 flow v7:3405-3418, 3733-3740.
- **Events** (already registered, no new rows): booking.voucher.consent_accepted, booking.voucher.consent_rejected — BSOT:1744-1745. Operator self-create emits NO event (no consumer — see Open Q7).
- **Error codes**: existing VOUCHER_NOT_FOUND/_EXPIRED/_NOT_APPLICABLE/_USAGE_LIMIT_REACHED/_USER_LIMIT_REACHED/_MIN_ORDER_NOT_MET (BSOT:1347-1352) + NEW VOUCHER_FORBIDDEN_FUNDING 422, VOUCHER_CODE_CONFLICT 409, VOUCHER_LOCKED 409 (added by 14.0a).
- **DB migration**: Booking — vouchers (incl. new cols), voucher_usages, operator_voucher_consents + 3 enums. Source: EDITED schema.sql.
- **Gateway routes**: add /v1/admin/vouchers (SYSTEM_ADMIN), /v1/operator/vouchers (OPERATOR_ADMIN), /v1/operator/voucher-consents (OPERATOR_ADMIN/STAFF) -> BOOKING_BASE_URL; existing /v1/vouchers (routes.ts:193) UNTOUCHED (Open Q2).

## Tasks

> **SOT vs CODE legend:** Task 14.0a edits CANONICAL SOT (docs/schema). Tasks 14.0+ implement CODE against the edited SOT and must not run until 14.0a is approved + landed.

### Task 14.0a — Edit canonical SOT for operator self-create vouchers (CANONICAL SOT EDIT; DO FIRST)
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer |
| skill | (none) |
| owned files (write set) | db-schema/booking/schema.sql (vouchers: add name, owner_operator_id, deleted_at cols + chk_vouchers_operator_owned_funding CHECK `CHECK (owner_operator_id IS NULL OR funding_type = 'OPERATOR_FUNDED'::voucher_funding_type)` + change uq_vouchers_code to partial WHERE deleted_at IS NULL + add idx_vouchers_owner_operator + COMMENT on owner_operator_id + update COMMENT on created_by_user_id to cover SYSTEM_ADMIN + OPERATOR_ADMIN), db-schema/booking/README.md (if it enumerates voucher cols), SU26SE101_VIETRIDE_technical_context_v7.md (sec 4.4: v7:462, v7:573, v7:624, funding block v7:626-655 — add operator self-create + consent-skip + checkout applicability branch), BACKEND_SOURCE_OF_TRUTH.md (sec 5.9 add VOUCHER_FORBIDDEN_FUNDING 422 + VOUCHER_CODE_CONFLICT 409 + VOUCHER_LOCKED 409 after BSOT:1352; sec 5.6 idempotency table: add rows for POST /v1/admin/vouchers + POST /v1/operator/vouchers (table ends ~BSOT:1262); sec 13 new top changelog row MINOR), VietRide_API_Contract_v1.md (new admin + operator voucher endpoint section — incl. GET /v1/admin/vouchers oversight list with ?ownerOperatorId=/?fundingType=/?isActive= filters, SYSTEM_ADMIN) |
| forbidden scope | ANY .cs/.ts code (docs/schema only — NO EF, NO migration, NO controllers), other services schema files, .env, secrets, git ops; do NOT change the admin OPERATOR_FUNDED consent flow semantics (v7:640-646, cite v7:640-646) — only ADD the operator self-create branch |
| depends on | none |
| invariant flags | LF for .md/.sql; cite human-approved design decisions verbatim (do not invent rules); Money to-the-dong no floor-1000; no cross-DB FK (owner_operator_id logical FK only); ADR 0003 soft-delete partial-unique pattern; SemVer bump in BSOT sec 13 |
| acceptance | schema.sql vouchers has the 3 new cols + CHECK + partial unique index + comment, internally consistent (no contradiction with voucher_usages/consents); chk_vouchers_operator_owned_funding CHECK uses the explicit `::voucher_funding_type` cast on the OPERATOR_FUNDED literal; COMMENT ON COLUMN vouchers.created_by_user_id updated to document BOTH SYSTEM_ADMIN (platform) AND OPERATOR_ADMIN (operator-owned); v7 sec 4.4 no longer asserts operator-cannot-create and documents self-create + consent-skip + checkout-applicability branch; BSOT sec 5.9 has the 3 new error rows (VOUCHER_FORBIDDEN_FUNDING 422, VOUCHER_CODE_CONFLICT 409, VOUCHER_LOCKED 409); BSOT sec 5.6 idempotency table has new rows for POST /v1/admin/vouchers and POST /v1/operator/vouchers; sec 13 has a new dated MINOR changelog row; API contract has the admin + operator voucher endpoints with the exact shapes above INCLUDING GET /v1/admin/vouchers (SYSTEM_ADMIN oversight list with ?ownerOperatorId=/?fundingType=/?isActive= filters); human has approved this task diff before any downstream task runs |
| source citations | re-plan brief (human-approved design decisions); db-schema/booking/schema.sql:229-254; v7 sec 4.4 v7:462, v7:573, v7:624, v7:626-655; BSOT sec 5.9 BSOT:1347-1352; BSOT sec 13 BSOT:2683-2689; ADR 0003 (soft-delete); VietRide_API_Contract_v1.md:722,763 |

### Task 14.0 — Voucher / VoucherUsage / OperatorVoucherConsent aggregates + EF migration (architecture baseline)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | scaffold-aggregate + ef-migration |
| owned files (write set) | apps/booking/src/VietRide.Booking.Domain/Entities/Voucher.cs (incl. OwnerOperatorId, Name, DeletedAt; implements IActivatable for IsActive), .../Entities/VoucherUsage.cs, .../Entities/OperatorVoucherConsent.cs, apps/booking/src/VietRide.Booking.Domain/Enums/VoucherType.cs, .../Enums/VoucherFundingType.cs, .../Enums/OperatorVoucherConsentStatus.cs, apps/booking/src/VietRide.Booking.Infrastructure/Persistence/Configurations/VoucherConfiguration.cs, .../VoucherUsageConfiguration.cs, .../OperatorVoucherConsentConfiguration.cs, apps/booking/src/VietRide.Booking.Infrastructure/BookingDbContext.cs (add 3 DbSets; extend OnModelCreating with 3 HasPostgresEnum calls AND extend the ConfigurePostgresTypes static method with 3 MapEnum calls — BOTH methods must be updated, mirror BookingDbContext.cs:29-36 and :47-54), apps/booking/src/VietRide.Booking.Infrastructure/Migrations/* (new migration + designer + snapshot regen) |
| forbidden scope | Booking.cs/Passenger.cs/BookingPendingAction.cs (do not alter), Application/Api layers, any other service, db-schema/** (14.0a owns it; here MIRROR the EDITED schema, do not edit), .env, secrets, Gateway, git ops |
| depends on | 14.0a (schema edited + approved first) |
| invariant flags | CRLF for .cs; CPM no Version=; no cross-DB FK (user_id, created_by_user_id, owner_operator_id, operator_id, responded_by_user_id, applicable_operator_ids[], applicable_route_ids[] logical FK only — NO EF relationship to other services); Money = BIGINT VND, no floor-1000 (BSOT v1.11.0); enum-as-Postgres-enum mapping mirrors BookingStatus at BookingDbContext.cs:29-54; soft-delete deleted_at + partial unique index (ADR 0003); IActivatable for is_active; one class per file |
| acceptance | dotnet build apps/booking/VietRide.Booking.sln -c Release clean; dotnet format --verify-no-changes clean; NetArchTest layering green; migration via IDesignTimeDbContextFactory applies -> rolls back -> reapplies clean on temp DB; generated tables/columns/indexes/enums match the EDITED schema exactly (owner_operator_id nullable, name not-null, deleted_at nullable, chk_vouchers_operator_owned_funding CHECK, uq_vouchers_code partial WHERE deleted_at IS NULL, idx_vouchers_owner_operator, value>0 / validity-window / min-order checks, voucher_usages.funded_by snapshot col, operator_voucher_consents unique (operator_id, voucher_id), reject_reason nullable); voucher_usages.booking_id FK ON DELETE CASCADE to local bookings only; VoucherConfiguration has an EF global query filter `HasQueryFilter(v => v.DeletedAt == null)` (BSOT sec 9.6) |
| source citations | EDITED db-schema/booking/schema.sql (vouchers/voucher_usages/operator_voucher_consents + enums :49-53); BSOT persistence (snake_case, enum mapping, soft-delete ADR 0003); BookingDbContext.cs:29-54 (mirror enum pattern) |

### Task 14.1 — Admin create-voucher endpoint + consent-row fan-out + admin oversight list (Q7)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | apps/booking/src/VietRide.Booking.Application/Features/Vouchers/CreateVoucher/* (Command/Handler/Validator/Result), apps/booking/src/VietRide.Booking.Application/Features/Vouchers/ListVouchers/* (Query/Handler/Validator/Result — admin oversight list, Q7), apps/booking/src/VietRide.Booking.Application/Abstractions/Repositories/IVoucherRepository.cs, apps/booking/src/VietRide.Booking.Infrastructure/Persistence/VoucherRepository.cs, apps/booking/src/VietRide.Booking.Api/Controllers/AdminVouchersController.cs (POST create + GET list), apps/booking/src/VietRide.Booking.Api/Controllers/Requests/CreateVoucherRequest.cs, apps/booking/src/VietRide.Booking.Api/Controllers/Requests/ListVouchersRequest.cs (ownerOperatorId/fundingType/isActive + QueryOptions paging), apps/booking/src/VietRide.Booking.Application/Services/VoucherCodeGenerator.cs (+ its abstraction under Abstractions/Services/), DI registration files for the new repository/service (Infrastructure + Api DependencyInjection) |
| forbidden scope | BookingDbContext.cs (wired in 14.0 — no re-edit beyond DI), entities/migrations (14.0), Booking checkout handlers, operator voucher CRUD (14.1b), other services, Gateway routes (14.5), db-schema (14.0a), .env, secrets, git ops |
| depends on | 14.0 |
| invariant flags | CRLF for .cs; controller calls MediatR.Send only; ApiResponse envelope (ADR 0004); MediatR v11; Idempotency-Key on POST mutation; SYSTEM_ADMIN authorization at controller; owner_operator_id always NULL for admin-created vouchers; OPERATOR_FUNDED requires non-null applicableOperatorIds (Open Q3 — reject null with VALIDATION_ERROR); auto-code = 8-char uppercase base32 unique among non-deleted; duplicate code -> VOUCHER_CODE_CONFLICT (409); admin GET /v1/admin/vouchers is read-only (no Idempotency-Key), sortBy whitelisted (BSOT sec 5.8 — reject non-whitelisted with INVALID_SORT_FIELD), pageSize clamped 100 (BSOT sec 5.7); VoucherCodeGenerator is a stateless utility (no repo/EF dependency) — placing it in Application/Services/ (not Infrastructure/, not Domain/) is correct; GET /v1/admin/vouchers does NOT support includeDeleted in v1 — returns active (non-soft-deleted) vouchers only (respects VoucherConfiguration HasQueryFilter); soft-deleted audit deferred; no cross-DB FK; Money VND |
| acceptance | build + dotnet format clean; POST /v1/admin/vouchers creates voucher (owner_operator_id NULL); OPERATOR_FUNDED with applicableOperatorIds inserts one PENDING consent per listed operator; VIETRIDE_FUNDED creates no consent rows; duplicate code -> 409 VOUCHER_CODE_CONFLICT; OPERATOR_FUNDED + null applicableOperatorIds -> VALIDATION_ERROR (Q3 RESOLVED); GET /v1/admin/vouchers?ownerOperatorId=X returns that operator's vouchers (and unfiltered returns all; ?fundingType=/?isActive= filter), paged + sortBy-whitelisted, SYSTEM_ADMIN only (Q7); at least 1 happy-path + 1 error unit test + 1 list-filter test; SYSTEM_ADMIN-only enforced on BOTH POST and GET |
| source citations | v7 sec 4.4 v7:624, v7:626-651 (funding model + consent fan-out), business-rules v7:4564 (code generation manual/auto 8-char base32 unique), v7:4507-4509 (enum values); BSOT sec 5.9 (+ VOUCHER_CODE_CONFLICT from 14.0a); BSOT sec 5.7 (paging) + 5.8 (filter/sort whitelist) for the oversight list; re-plan Q7 resolution (admin oversight endpoint, human-approved 2026-06-19); ADR 0004 envelope |

### Task 14.1b — Operator self-service voucher CRUD endpoints (NEW)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | apps/booking/src/VietRide.Booking.Application/Features/OperatorVouchers/CreateOperatorVoucher/*, .../UpdateOperatorVoucher/*, .../DeleteOperatorVoucher/*, .../SetOperatorVoucherActive/* (Command/Handler/Validator/Result; reuse IVoucherRepository from 14.1 — extend it ADDITIVELY (append-only) with operator-scoped query/get methods AND a CountUsages(voucherId) method for the Q6 freeze-on-first-use guard in UpdateOperatorVoucherHandler), apps/booking/src/VietRide.Booking.Api/Controllers/OperatorVouchersController.cs, apps/booking/src/VietRide.Booking.Api/Controllers/Requests/CreateOperatorVoucherRequest.cs, .../UpdateOperatorVoucherRequest.cs |
| forbidden scope | admin CreateVoucher feature (14.1) — do not edit its files; entities/migrations (14.0), checkout handlers (14.3/14.4), consent endpoints (14.2), Gateway routes (14.5), db-schema (14.0a), other services, .env, secrets, git ops; reuse VoucherCodeGenerator + IVoucherRepository (do not duplicate); if IVoucherRepository needs a new method ADD it (append-only) — coordinate ordering with 14.1 (see dispatch note) |
| depends on | 14.0, 14.1 (IVoucherRepository + VoucherCodeGenerator exist) |
| invariant flags | CRLF; controller -> MediatR.Send; ApiResponse envelope; MediatR v11; Idempotency-Key on POST create (PATCH/DELETE/activate/deactivate idempotent — no key, mirror BSOT sec 5.6 driver-schedule-activation precedent); OPERATOR_ADMIN-only; tenant isolation — owner_operator_id FORCED to caller operatorId on create, all read/update/delete/activate scoped to owner_operator_id == caller (cross-operator -> 404 VOUCHER_NOT_FOUND, do not leak existence); PATCH freeze-on-first-use (Q6 RESOLVED, no new schema column — guard counts voucher_usages by voucher_id): ALWAYS immutable code/type/fundingType/ownerOperatorId (attempt -> validation error / ignored); while CountUsages==0 operator may edit name/value/minOrderAmount/maxDiscountAmount/totalUsageLimit/perUserLimit/validFrom/validUntil/applicableRouteIds; once CountUsages>=1 the economic fields value/minOrderAmount/maxDiscountAmount FREEZE -> editing them returns VOUCHER_LOCKED 409, and only name, EXTENDING validUntil (not shortening below current), LOOSENING limits, applicableRouteIds, and deactivate stay editable; funding_type FORCED OPERATOR_FUNDED (else 422 VOUCHER_FORBIDDEN_FUNDING); applicableOperatorIds FORCED to self; NO operator_voucher_consents rows (self-consented); code globally unique among non-deleted -> VOUCHER_CODE_CONFLICT; soft-delete sets deleted_at (ADR 0003); no cross-DB FK; Money VND |
| acceptance | build + dotnet format clean; POST /v1/operator/vouchers creates owner_operator_id=caller, funding_type=OPERATOR_FUNDED, NO consent rows; fundingType=VIETRIDE_FUNDED in body -> 422 VOUCHER_FORBIDDEN_FUNDING; PATCH updates only mutable fields scoped to owner; PATCH freeze-on-first-use (Q6): editing value/minOrderAmount/maxDiscountAmount on a voucher with >=1 usage -> 409 VOUCHER_LOCKED, editing name on the same used voucher -> allowed, editing value on a zero-usage voucher -> allowed, attempting to change code/type/fundingType -> rejected; DELETE soft-deletes (deleted_at set, code reusable); activate/deactivate flip is_active; cross-operator access -> 404 VOUCHER_NOT_FOUND (tenant-isolation test); duplicate global code -> 409 VOUCHER_CODE_CONFLICT; at least 1 happy + 1 tenant-isolation + 1 forbidden-funding + 2 freeze-on-first-use tests (used-voucher value edit -> VOUCHER_LOCKED; used-voucher name edit -> allowed); no existing IVoucherRepository method signature created in 14.1 is modified — operator CRUD only APPENDS new methods (append-only) |
| source citations | re-plan brief requirement 1/2/3 (human-approved); EDITED v7 sec 4.4 (14.0a operator self-create + consent-skip); EDITED schema.sql (owner_operator_id, deleted_at); BSOT sec 5.6 (idempotency precedent for activation/PATCH no-key), sec 5.9 (VOUCHER_FORBIDDEN_FUNDING, VOUCHER_CODE_CONFLICT, VOUCHER_LOCKED from 14.0a); re-plan Q6 resolution (freeze-on-first-use model, human-approved 2026-06-19); ADR 0003 (soft-delete), ADR 0004 (envelope) |

### Task 14.2 — Operator voucher-consent endpoints (list / accept / reject) + events
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint + add-integration-event |
| owned files (write set) | apps/booking/src/VietRide.Booking.Application/Features/VoucherConsents/ListVoucherConsents/*, .../AcceptVoucherConsent/*, .../RejectVoucherConsent/* (Query/Command/Handler/Validator/Result), apps/booking/src/VietRide.Booking.Application/Abstractions/Repositories/IOperatorVoucherConsentRepository.cs, apps/booking/src/VietRide.Booking.Infrastructure/Persistence/OperatorVoucherConsentRepository.cs, apps/booking/src/VietRide.Booking.Api/Controllers/OperatorVoucherConsentsController.cs, apps/booking/src/VietRide.Booking.Api/Controllers/AdminVoucherConsentsController.cs (GET /v1/admin/vouchers/{voucherId}/consents), apps/booking/src/VietRide.Booking.Api/Controllers/Requests/RejectVoucherConsentRequest.cs, DI registration for the new repository |
| forbidden scope | entities/migrations (14.0), admin create-voucher (14.1), operator voucher CRUD (14.1b), checkout handlers (14.3/14.4), Gateway routes (14.5), db-schema (14.0a), other services, .env, secrets, git ops; do NOT add new event-registry rows (events already in BSOT) |
| depends on | 14.0 (entities); serial after 14.1b (disjoint feature folders but shares Infrastructure/Api DI files — see dispatch note) |
| invariant flags | CRLF; controller -> MediatR.Send; ApiResponse envelope; Idempotency-Key on accept/reject POSTs; OPERATOR_ADMIN-only for accept/reject, OPERATOR_ADMIN/STAFF for list; tenant isolation — operator may only see/act on consents for operatorId from JWT; Outbox routing key booking.voucher.consent_accepted / booking.voucher.consent_rejected; accept precond status PENDING, reject precond status in PENDING or ACCEPTED; revoke (ACCEPTED->REJECTED) sets responded_at but does NOT touch existing bookings |
| acceptance | build + dotnet format clean; list returns operator-scoped consents filtered by status; accept flips PENDING->ACCEPTED + emits consent_accepted; reject flips ->REJECTED (+ optional reason) + emits consent_rejected; cross-operator access -> 403/404 (tenant isolation test); OPERATOR_STAFF calling POST accept/reject -> 403 FORBIDDEN (fine-grained role enforced in the .NET controller, not just the Gateway prefix); at least 1 happy + 1 precondition-violation test per command; admin consents query returns rows for a voucherId |
| source citations | v7 sec 4.4 v7:657-690 (endpoint spec, roles, preconditions, revoke semantics, rejectReason); BSOT event registry BSOT:1744-1745; db-schema/booking/schema.sql:279-302 |

### Task 14.3 — Voucher validation + discount application in single-trip checkout
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none — modify existing feature) |
| owned files (write set) | apps/booking/src/VietRide.Booking.Application/Features/Bookings/CreateBooking/CreateBookingCommandHandler.cs (replace discountAmount = Money.Zero at line 142), apps/booking/src/VietRide.Booking.Application/Services/VoucherService.cs (shared validate+compute+record-usage logic), apps/booking/src/VietRide.Booking.Application/Abstractions/Services/IVoucherService.cs (standalone interface file — Task 14.4 depends on it; name `VoucherService`/`IVoucherService` per BSOT §3.2 Booking Application/Services tree + <Aggregate>Service convention — NOT VoucherApplicationService; VoucherCodeGenerator stays a separate utility), DI registration for the new service; CreateBookingResult.cs only if a discountAmount/voucherUsageId field is missing (it already exposes DiscountAmount) |
| forbidden scope | Voucher.cs/VoucherUsage.cs entities + migrations (14.0), admin/consent/operator-CRUD endpoints (14.1/14.1b/14.2), round-trip handler (14.4), EditPickup/EditDropoff features, Gateway, db-schema (14.0a), other services, .env, secrets, git ops |
| depends on | 14.0, 14.1 (IVoucherRepository), 14.1b (owner-scoped voucher lookup methods), 14.2 (consent repository) |
| invariant flags | CRLF; applicability branch (re-plan 4): applies iff EITHER (a) owner_operator_id == Trip.operatorId (operator-owned, self-funded, SKIP consent check), OR (b) owner_operator_id IS NULL AND (VIETRIDE_FUNDED scope match, OR OPERATOR_FUNDED with ACCEPTED consent for Trip.operatorId); CANONICAL validation order (Q8 RESOLVED — consent is an eligibility gate at the applicability stage, NOT after limit/min-order; fixes prior reviewer NIT): (1) exists + not soft-deleted + is_active; (2) within valid_from..valid_until window; (3) applicability — operator-scope [branch (a) owner_operator_id == Trip.operatorId auto-passes; branch (b) applicableOperatorIds match] + route-scope (applicable_route_ids) + consent [branch (b) OPERATOR_FUNDED requires ACCEPTED consent for Trip.operatorId; branch (a) skips consent]; (4) min_order_amount met; (5) usage limits (total + per-user) not exceeded; (6) compute discount (apply max_discount_amount cap, round half-up AwayFromZero, no floor-1000). Q8: branch (a) bypasses ONLY the consent check and the operator-scope check — ALL other filters (route-scope, validity window, min-order, total/per-user usage limits) apply universally, including to operator-owned vouchers; each failure throws the registered coded exception (404 vs 422 per BSOT sec 5.9); PERCENT_OFF discount rounded to nearest dong half-up (MidpointRounding.AwayFromZero), NO floor-1000 (Open Q4 RESOLVED, BSOT v1.11.0); voucher_usages.funded_by = snapshot of funding_type; VoucherUsage created in same tx as Booking (TransactionBehavior); voucherUsageId propagated into the booking.booking.confirmed event payload (currently hardcoded null at handler line 290); per-user limit counts this user usages; total limit counts all usages; on checkout compensation, explicitly DELETE the VoucherUsage row (physical DELETE in the same compensation scope — v7:4562 requires DELETE on CANCELLED/REFUNDED; ON DELETE CASCADE does not fire for soft-delete) |
| acceptance | build + dotnet format clean; happy path: valid voucher reduces totalAmount and discountAmount reflected in CreateBookingResult + voucher_usages row written; operator-owned voucher (owner_operator_id == operator) applies WITHOUT any consent row (test); usage-limit boundary test (Nth succeeds, N+1th -> VOUCHER_USAGE_LIMIT_REACHED); per-user boundary -> VOUCHER_USER_LIMIT_REACHED; min-order not met -> VOUCHER_MIN_ORDER_NOT_MET; expired -> VOUCHER_EXPIRED; wrong operator/route -> VOUCHER_NOT_APPLICABLE; admin OPERATOR_FUNDED without ACCEPTED consent -> VOUCHER_NOT_APPLICABLE; operator-owned voucher of a DIFFERENT operator -> VOUCHER_NOT_APPLICABLE; PERCENT_OFF rounding half-up verified to the dong; after payment compensation fires, no voucher_usages row exists for the booking (physical-deletion test); booking.booking.confirmed event payload carries a non-null voucherUsageId when a voucher was applied (replaces the hardcoded null at CreateBookingCommandHandler.cs:290; voucherUsageId is already an optional field in the event registry BSOT:1741 — no new registry row) |
| source citations | v7 flow v7:3405-3418 (validation steps + rounding), v7:3441-3443 (ledger funding — Payment/Day-16 scope, only funded_by snapshot here), v7 sec 4.4 v7:626-646 (funding semantics + consent gate), EDITED v7 sec 4.4 (operator-owned applicability branch, 14.0a); re-plan Q8 resolution (canonical checkout validation order, human-approved 2026-06-19); BSOT sec 5.9 BSOT:1347-1352; BSOT Money rounding BSOT:1110, 2079 (half-up); CreateBookingCommandHandler.cs:139-143, 290 (current stub + event payload); BSOT §3.2 (Booking Application/Services tree names VoucherService.cs / IVoucherService.cs — BSOT:417,432) + AGENTS.md naming (<Aggregate>Service) |

### Task 14.4 — Round-trip voucher: 2 VoucherUsage records, per-leg apply
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none — modify existing feature) |
| owned files (write set) | apps/booking/src/VietRide.Booking.Application/Features/Bookings/CreateRoundTripBooking/CreateRoundTripBookingCommandHandler.cs (+ its Result file if a discount field is missing) |
| forbidden scope | single-trip handler (14.3), IVoucherService definition (consume it, do not redefine), entities/migrations, admin/consent/operator-CRUD endpoints, Gateway, db-schema, other services, .env, secrets, git ops |
| depends on | 14.3 (reuses IVoucherService) |
| invariant flags | CRLF; voucher applied PER booking leg, NOT per BookingGroup; validate each leg minOrder independently; create 2 separate voucher_usages rows (one per booking) each carrying booking_group_id; cancelling one leg later must DELETE only that leg usage (Day-17 carry — per-booking booking_id enables this); usage-limit counting must count the group correctly (idx_voucher_usages_voucher_group); discount per leg rounded to nearest dong half-up |
| acceptance | build + dotnet format clean; round-trip with valid voucher creates 2 voucher_usages rows sharing booking_group_id, each with its own booking_id + per-leg discount_amount; if only one leg meets min-order, only that leg gets a usage (test); WALLET all-or-nothing batch-charge still uses post-discount per-leg totals; grandTotal reflects both discounts; at least 1 round-trip voucher test |
| source citations | v7 round-trip voucher v7:1733-1740 (per-booking, 2 usages, cancel-one-leg independence); BE_TIMELINE Day 14 line 161; db-schema/booking/schema.sql:264, 271-272 (booking_group_id col + group index) |

### Task 14.5 — Gateway routes + access-gates spec for admin/operator voucher endpoints
| Field | Value |
|---|---|
| stack/owner | nest |
| implement agent | nest-worker |
| review agent | nest-reviewer |
| skill | (none) |
| owned files (write set) | apps/gateway/src/config/routes.ts (add /v1/admin/vouchers SYSTEM_ADMIN, /v1/operator/vouchers OPERATOR_ADMIN, /v1/operator/voucher-consents OPERATOR_ADMIN/STAFF -> BOOKING_BASE_URL; do NOT modify existing /v1/vouchers line 193 per Open Q2), apps/gateway/src/proxy/proxy.access-gates.spec.ts (add role-gate assertions for the new prefixes) |
| forbidden scope | all .NET services, db-schema, other Gateway modules (auth/jwt minting, proxy middleware logic), existing /v1/vouchers route (Open Q2 — untouched), .env/.env.example secrets, Postman collection (not in Day-14 scope unless human asks), git ops |
| depends on | none for the route table; assertions presume 14.1/14.1b/14.2 endpoint paths (paths fixed by spec, so parallel-safe) |
| invariant flags | LF for .ts; CPM not applicable; mirror existing route-object shape (prefix/target/authRequired/requiredRoles/publicSubpaths); admin = requiredRoles SYSTEM_ADMIN (mirror /v1/admin/booking-stats at routes.ts:91-96) — the single /v1/admin/vouchers prefix entry proxies BOTH POST create (14.1) and GET oversight list (14.1/Q7); no separate route entry needed (prefix-based proxy); operator vouchers = OPERATOR_ADMIN; consent list allows STAFF — gateway prefix gates coarse role, fine-grained OPERATOR_ADMIN-only on accept/reject enforced in .NET controller (14.2); no internal-JWT-minting changes |
| acceptance | nx build (TS) green; gateway lint green; nx test gateway green incl. new access-gate assertions; route entries proxy to BOOKING_BASE_URL; cross-role request to admin/operator prefix blocked by the gate test (incl. a non-SYSTEM_ADMIN GET /v1/admin/vouchers -> blocked); existing /v1/vouchers route unchanged |
| source citations | v7 sec 4.4 v7:657-688 (roles per endpoint); routes.ts:91-96 (admin role-gate pattern), :186-199 (booking/voucher block); AGENTS.md domain (Gateway route per new endpoint) |

## Dispatch order
1. **Task 14.0a** (CANONICAL SOT EDIT) — must land + be human-approved FIRST; blocks ALL code tasks. Parallel-safe: no.
2. **Task 14.0** (entities + migration) — depends on 14.0a; blocks 14.1/14.1b/14.2/14.3/14.4. Parallel-safe: no.
3. **Task 14.1** (admin create-voucher + admin oversight GET list, Q7) — depends on 14.0; creates IVoucherRepository + VoucherCodeGenerator. Run BEFORE 14.1b (14.1b reuses both). Parallel-safe: no (shared DI + IVoucherRepository with 14.1b/14.2). (Q7 admin oversight list stays inside this task + 14.5 — no dispatch-order/parallel-safety change.)
4. **Task 14.1b** (operator voucher CRUD) — depends on 14.0 + 14.1 (reuses IVoucherRepository/VoucherCodeGenerator; may append methods). Run after 14.1. Parallel-safe: no (shares IVoucherRepository/DI).
5. **Task 14.2** (consent endpoints) — depends on 14.0; disjoint feature folder from 14.1/14.1b but shares Infrastructure/Api DI files. Run serially after 14.1b (shares Infrastructure/Api DI files). Parallel-safe: no (shared DI files).
6. **Task 14.3** (single-trip checkout) — depends on 14.0/14.1/14.1b/14.2 (voucher + consent repos + owner-scoped lookup). Parallel-safe: no.
7. **Task 14.4** (round-trip checkout) — depends on 14.3 (reuses IVoucherService). Parallel-safe: no.
8. **Task 14.5** (Gateway routes + spec) — independent of .NET tasks (paths fixed by spec); may run any time after 14.0a fixes endpoint shapes, ideally last so E2E paths exist. Parallel-safe: yes (disjoint write set — only apps/gateway/**).

## Progress tracker
> Orchestrator bookkeeping — the main thread updates this table after each /implement-task (Step 3) with the task review verdict. **Informational only — NOT audit evidence.** /audit-day MUST re-verify every task independently against the SOT.

| Task | Status | Review verdict | Date | Notes |
|---|---|---|---|---|
| 14.0a | done | APPROVED | 2026-06-20 | Human-approved diff + committed; SOT baseline for Tasks 14.0-14.5 |
| 14.0 | done | APPROVED | 2026-06-20 | 100 tests pass; migration apply/rollback/reapply clean; NetArchTest green |
| 14.1 | done | APPROVED | 2026-06-20 | admin create-voucher + OPERATOR_FUNDED consent fan-out + admin oversight GET list; 89 unit + 23 integration tests pass; NetArchTest green; APPROVE after 3 review rounds (idempotency-key enforce, 8-char auto-code stub, sortDir guard, IClock.UtcNow createdAt, Q3 validator test). Carry-over (non-blocking, pre-existing cross-service): INVALID_SORT_FIELD returns 422 via validator vs BSOT:1415 400; 2 NITs (cache clock once, doc-comment typo). Human /verify pending |
| 14.1b | done | APPROVED | 2026-06-20 | operator self-create CRUD (POST/PATCH/DELETE/activate/deactivate); tenant isolation, OPERATOR_FUNDED forced, freeze-on-first-use (Q6), DELETE idempotent soft-delete, partial PATCH null=keep-current, contract response shapes; IVoucherRepository extended append-only; 112 unit + 23 integration pass; APPROVE after 4 review rounds. Human /verify pending |
| 14.2 | done | APPROVED | 2026-06-20 | operator consent list/accept/reject + admin consents view + Outbox events (consent_accepted/rejected); IOperatorVoucherConsentRepository; 128 unit + 23 integration pass; NetArchTest green; Api domain-free (status parsed in handler). Required SOT addendum (BSOT 1.14.0, commit fd5f8a0): registered CONSENT_NOT_PENDING 409 + CONSENT_ALREADY_REJECTED 409 (human-approved). APPROVE after 2 review rounds (list-handler tests, accept/reject result trimmed to {id,status} per contract). Human /verify pending |
| 14.3 | done | APPROVED | 2026-06-20 | single-trip checkout voucher: IVoucherService/VoucherService (Q8 canonical validation order, branch (a) operator-owned skip consent+operator-scope / branch (b) platform VIETRIDE_FUNDED scope or OPERATOR_FUNDED ACCEPTED consent; route/validity/min-order/limits apply universally); PERCENT_OFF half-up AwayFromZero + max-discount cap, no floor-1000; funded_by snapshot; VoucherUsage in same tx; voucherUsageId into booking.booking.confirmed (replaces hardcoded null); physical-delete compensation; IVoucherRepository extended append-only; replaces discountAmount=Money.Zero stub. 147 unit + 23 integration pass; APPROVE after 1 review round (VOUCHER_INACTIVE→VOUCHER_NOT_FOUND, +handler/inactive tests). Human /verify pending |
| 14.4 | done | APPROVED | 2026-06-20 | round-trip voucher per-leg: 2 voucher_usages rows (own booking_id + shared booking_group_id), per-leg min-order independent (partial apply), group-level usage/per-user cap (outbound-first, no whole-request fail), per-leg discount half-up, grandTotal reflects both; voucherUsageId per leg into booking.booking.confirmed; physical-delete compensation on all failure paths (WALLET + non-WALLET InsufficientFunds + batch-validation). Reuses IVoucherService (consumed, not redefined). 152 unit + 23 integration pass; APPROVE after 2 review rounds (TOCTOU cap, compensation gaps, integration stub). Carry-over NIT: 1 dead test var. Human /verify pending |
| 14.5 | done | APPROVED | 2026-06-20 | Gateway routes + access-gates spec: /v1/admin/vouchers (SYSTEM_ADMIN, prefix proxies POST create + GET oversight), /v1/operator/vouchers (OPERATOR_ADMIN), /v1/operator/voucher-consents (OPERATOR_ADMIN/STAFF) → BOOKING_BASE_URL; /v1/vouchers untouched (Open Q2); longest-prefix-wins correct; spec covers pass-through + 403 blocks (incl. non-SYSTEM_ADMIN GET /v1/admin/vouchers). 97 gateway tests + all TS suites pass; lint green. APPROVE first round. Carry-over NIT: add OPERATOR_STAFF challenger for /v1/operator/vouchers 403 test. Human /verify pending |

Legend: todo / in progress / done (reviewer APPROVED + human /verify) / done-with-carryover / blocked

## Open questions
Resolve with the human before dispatch — do NOT guess.

1. **RESOLVED — Admin create-voucher request/response shape.** Now proposed in the Contract-changes block (derived strictly from schema cols + code-gen mode). Open item: confirm exact field casing and that the contract doc update belongs in Task 14.0a (it does, per re-plan). Operator create mirrors it minus admin-only fields.
2. **Existing /v1/vouchers Gateway route (routes.ts:193, authRequired user, no role gate) — OUT OF SCOPE.** Passenger voucher-list endpoint; not part of Day-14 admin/operator surface. Task 14.5 will NOT modify it. Confirm at a later day whether it becomes passenger list-my-applicable-vouchers.
3. **RESOLVED (2026-06-19) — OPERATOR_FUNDED admin voucher + applicableOperatorIds = null -> VALIDATION_ERROR.** Decision: admin OPERATOR_FUNDED REQUIRES an explicit non-null applicableOperatorIds list; null is rejected with VALIDATION_ERROR (Task 14.1 invariant + acceptance assert this). VIETRIDE_FUNDED + null still = applies to all operators. The all-operator fan-out (v7:651) is a DEFERRED future enhancement — blocked anyway by no-cross-DB-FK (Booking would have to call Identity for the APPROVED-operator list); needs an Identity APPROVED-operator feed in a later version.
4. **RESOLVED — PERCENT_OFF rounding.** Round to nearest dong, half rounds UP (MidpointRounding.AwayFromZero), NO floor-1000 (BSOT v1.11.0, BSOT:2079). Baked into Task 14.3 acceptance + invariants.

9. **RESOLVED / DEFERRED — GET /v1/admin/vouchers does NOT support includeDeleted in v1.** The admin oversight list (Q7) returns only active (non-soft-deleted) vouchers — it respects the VoucherConfiguration `HasQueryFilter(v => v.DeletedAt == null)`. Audit/oversight of soft-deleted operator vouchers is a DEFERRED future enhancement (needs an IgnoreQueryFilters path + an explicit includeDeleted flag, out of Day-14 scope). Baked into Task 14.1 invariant flags.

### Residual ambiguities (need a human decision before the relevant task)
5. **RESOLVED — add deleted_at + partial unique index WHERE deleted_at IS NULL per Task 14.0a (ADR 0003).** vouchers.deleted_at is a NET-NEW column (mirror soft-delete conventions); ADR 0003 then forces uq_vouchers_code to become a partial unique index WHERE deleted_at IS NULL (a soft-deleted code can be reused). The column is table-wide (one vouchers table), so the partial-unique change applies to admin + operator vouchers alike. Decided in Task 14.0a's owned-files + acceptance — no longer a residual ambiguity.
6. **RESOLVED (2026-06-19) — freeze-on-first-use model for operator PATCH (Task 14.1b).** ALWAYS immutable: code, type, fundingType, ownerOperatorId (attempt -> validation error / ignored). While the voucher has ZERO usages (CountUsages by voucher_id == 0) the operator may edit name, value, minOrderAmount, maxDiscountAmount, totalUsageLimit, perUserLimit, validFrom, validUntil, applicableRouteIds. Once >=1 usage exists: additionally FREEZE the economic fields value/minOrderAmount/maxDiscountAmount — only allow editing name, EXTENDING validUntil (not shortening below current), LOOSENING limits, applicableRouteIds, and deactivate. NO new schema column — a guard in the PATCH handler counts voucher_usages by voucher_id. Error code: NEW VOUCHER_LOCKED 409 — verified absent from BSOT sec 5.9 (no existing code fits a post-use economic-field freeze), so Task 14.0a ADDS it to the registry alongside VOUCHER_FORBIDDEN_FUNDING / VOUCHER_CODE_CONFLICT.
7. **RESOLVED (2026-06-19) — no event + ADD admin oversight list endpoint.** Operator self-create emits NO integration event (no consumer; YAGNI per BSOT sec 3.2.3) — admin OPERATOR_FUNDED consent flow still emits consent_accepted/rejected. PLUS a NEW admin oversight endpoint GET /v1/admin/vouchers (SYSTEM_ADMIN) with optional filters ?ownerOperatorId= / ?fundingType= / ?isActive= returning the voucher list for platform governance/audit of operator-created vouchers. Added to: Task 14.1 (admin vouchers controller GET + ListVouchers query handler), the Contract-changes block (DTO shape), Task 14.0a's API-contract edit list, and Task 14.5 (rides the existing /v1/admin/vouchers SYSTEM_ADMIN prefix).
8. **RESOLVED (2026-06-19) — route-scope applies in branch (a) + canonical validation order (Task 14.3).** Branch (a) (operator-owned, owner_operator_id == Trip.operatorId) bypasses ONLY the consent check and the operator-scope check; ALL other filters apply (applicable_route_ids, validity window, min_order, total/per-user usage limits). Canonical checkout validation order (consent is an eligibility gate at the applicability stage, NOT after limit/min-order — fixes the prior reviewer NIT): (1) exists + not soft-deleted + is_active; (2) within valid_from..valid_until window; (3) applicability — operator-scope [branch (a) auto-pass / branch (b) applicableOperatorIds] + route-scope (applicable_route_ids) + consent [branch (b) OPERATOR_FUNDED requires ACCEPTED]; (4) min_order_amount met; (5) usage limits (total + per-user) not exceeded; (6) compute discount (apply max_discount_amount cap, round half-up AwayFromZero, no floor-1000). Pinned in Task 14.3 invariant flags.
