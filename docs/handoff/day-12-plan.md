# Day 12 — Plan

- **Timeline ref**: BE_TIMELINE_VU.md → Day 12 (Jira: SCV-82) — "Booking Service: Seat lock + Booking entity"
- **Prior checklist**: docs/handoff/day-11-checklist.md → **not found** (no day-11 plan/checklist; last trip checklist is day-10; day-9 has a plan but no checklist). Carry-over read from docs/handoff/day-10-checklist.md (see Carryover). **Open questions Q1-Q5 RESOLVED by the human (2026-06-11) — see Resolved decisions; none block dispatch.**
- **Plan status**: DRAFT (open questions baked in 2026-06-11) → (reviewer) APPROVED / REVISION-REQUIRED

## Objective
Stand up the Booking-side of the seat-lock saga: bootstrap the Booking service architecture baseline (MediatR v11 + behaviors + NetArchTest + EF migration for bookings/passengers/booking_pending_actions/outbox_events), implement the Booking aggregate + BookingCode generator, and implement POST /v1/bookings (max 5 seats, all-or-nothing) driving the frozen Trip-Booking seam through an inter-service Trip HTTP client (ITripServiceClient, BSOT §3.2.5/§3.5 naming) (lock-seats then create PENDING_PAYMENT booking then on wallet success book-seats; compensate release-seats on failure). The seat inventory (trip_seats), the Redis lock, and the lock/book/release endpoints are owned by Trip-Route-Vehicle (API Contract Trip-Booking seam; BSOT section 13 row 1.8.0) and are NOT in this day write set — Booking calls them through a client mocked in tests until the Trip side lands. This unblocks Day-13 pickup/dropoff + round-trip and Day-15/16 payment.

## Success criteria (DoD — binary, verifiable)
- [ ] Booking service has MediatR v11 + ValidationBehavior/LoggingBehavior/TransactionBehavior wired (mirrors Trip AddVietRideMediatRBehaviors); NetArchTest layering test green in VietRide.Booking.UnitTests.
- [ ] EF migration creates vietride_booking tables bookings, passengers, booking_pending_actions, outbox_events per db-schema/booking/schema.sql (incl. the 2 pickup/dropoff CHECKs, chk_bookings_total_le_base, uq_bookings_booking_code, uq_passengers_booking_seat, partial-unique active pending action); dotnet ef database update runs clean from empty DB and Down() reverses. **TripSeat / trip_seats are OUT OF SCOPE for this migration** — they are Trip-owned (vietride_trip schema) and land with the Day-9/11 Trip catch-up (RESOLVED D1: Booking-side only, mocked Trip; the live seat-race is the Carryover dependency, not a Day-12 DoD item); /audit-day must NOT flag the Day-12 DoD incomplete for the absence of trip_seats nor for the deferred live race.
- [ ] Booking aggregate + Passenger sub-entity + BookingCode VO exist; BookingCode produces the long form VR-yyyyMMdd-XXXXXXXX (date + 8-char base32 uppercase) per db-schema/booking/schema.sql COMMENT line 124 + API contract example line 713. RESOLVED D2: long form VR-yyyyMMdd-XXXXXXXX is confirmed canonical (schema.sql:124 + API contract:713 agree); the BSOT VR-<4char> short form at lines 1985/2165 is a stale example (reconcile per Carryover C2). No longer a block on dispatch.
- [ ] POST /v1/bookings (Idempotency required, PASSENGER): validates max 5 seats (BOOKING_MAX_SEATS_EXCEEDED 422), calls ITripServiceClient lock-seats all-or-nothing (propagates BOOKING_SEAT_UNAVAILABLE 409 / BOOKING_TRIP_NOT_BOOKABLE 409 / TRIP_NOT_FOUND 404), computes totalAmount (base_fare minus discount, floor 1000), creates Booking PENDING_PAYMENT + N Passenger, and on WALLET path calls book-seats to CONFIRMED (+ enqueue booking.booking.confirmed via Outbox); on any downstream failure calls release-seats (compensation). Response is the ApiResponse 201 envelope from POST /v1/bookings.
- [ ] Release-seats compensation lives in Application/Services/BookingService.cs (IBookingService.ReleaseSeatsAsync) — NOT inlined in CreateBookingHandler — so Day-17 cancel/refund reuses it (BSOT section 3.2.5/3.2.6 shared-logic-2-handler rule).
- [ ] Seat-lock all-or-nothing + compensation proven by unit/integration tests with a mocked ITripServiceClient (2 concurrent same-seat one-wins asserted at the Trip lock-client contract level, since the real Redis lock lives in Trip — RESOLVED D1: the live stress-test is DEFERRED to the Carryover dependency; Day-12 acceptance is the mocked-client path with at least 1 happy + 1 error per handler, NOT the live race).
- [ ] dotnet build + dotnet format --verify-no-changes clean on apps/booking/VietRide.Booking.sln and libs/dotnet/VietRide.Libs.sln; new handler tests at least 1 happy + 1 error; Swagger renders POST /v1/bookings; Postman updated.

## Contract changes
- **REST (FE-facing):** POST /v1/bookings implemented to the existing frozen shape — VietRide_API_Contract_v1.md Booking Service POST /v1/bookings (lines 681-721). Gateway route /v1/bookings to booking already exists (BSOT section 10 routes, line 156; no gateway change). Round-trip / history / cancel / edit endpoints are Day 13+ — out of scope.
- **Internal seam (consumed, not implemented here):** POST /internal/v1/trips/{tripId}/lock-seats, /book-seats, /release-seats, GET /internal/v1/trips/{tripId} — VietRide_API_Contract_v1.md Trip-Booking seam (lines 975-1179), frozen by BSOT section 13 row 1.8.0. Booking implements ITripServiceClient against these (Abstractions/ServiceClients/ + Infrastructure/Http/ per BSOT §3.5 line 935); the server side stays in Trip-Route-Vehicle (NOT this day).
- **Events (enqueued via Outbox):** booking.booking.confirmed { bookingId, tripId, totalAmount, userId, voucherUsageId? } — already in BSOT section 7.3 registry (line 1734). No new routing key.
- **Error codes:** all reused from BSOT section 5.9 (BOOKING_SEAT_UNAVAILABLE, BOOKING_TRIP_NOT_BOOKABLE, BOOKING_MAX_SEATS_EXCEEDED, BOOKING_NOT_FOUND, TRIP_NOT_FOUND, IDEMPOTENCY_KEY_MISMATCH — lines 1335-1339, 1401). No new error codes.
- **Migration:** new Booking EF migration (vietride_booking). Voucher / booking_stats / booking_transfers tables are deferred to Day 14 / Day 17 — only the four tables this day flow needs.

## Tasks

### Task 12.0 — Booking architecture baseline (DO FIRST; blocks every feature task)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none) — mirror Trip Program.cs + AddVietRideMediatRBehaviors; CPM entries already present (MediatR 11.x, FluentValidation 11.x, NetArchTest.Rules used by Identity/Trip) |
| owned files (write set) | apps/booking/src/VietRide.Booking.Api/Program.cs; apps/booking/src/VietRide.Booking.Application/ApplicationAssemblyMarker.cs; apps/booking/src/VietRide.Booking.Application/VietRide.Booking.Application.csproj; apps/booking/src/VietRide.Booking.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs; apps/booking/src/VietRide.Booking.Infrastructure/VietRide.Booking.Infrastructure.csproj; apps/booking/src/VietRide.Booking.Api/VietRide.Booking.Api.csproj; apps/booking/tests/VietRide.Booking.UnitTests/** (new project, incl. Architecture/LayeringTests.cs); apps/booking/src/VietRide.Booking.Api/appsettings.json (Redis/Trip BaseUrl config keys only — NO secrets) |
| forbidden scope | .env, secrets, INTERNAL_JWT_SECRET; any apps/{identity,trip,payment,parcel} path; apps/gateway/**; db-schema/**; libs/** (reuse Shared.* as-is — do NOT edit shared libs); git branch/commit/push; Directory.Packages.props (CPM entries already exist; if a needed version is missing, STOP and report) |
| depends on | — |
| invariant flags | CRLF on .cs/.csproj; CPM — no Version= on any PackageReference; MediatR pinned v11; Clean Architecture dep direction (Api to App+Infra, Infra to Domain+App, App to Domain, Domain to nothing); **DB-CONTEXT GUARD — the existing apps/booking/src/VietRide.Booking.Api/Program.cs line 20 ALREADY calls `AddVietRideDbContext<BookingDbContext>(builder.Configuration)`; AddInfrastructure / any 12.0 wiring MUST NOT re-register the DbContext (no second AddVietRideDbContext / AddDbContext) — double registration is forbidden** |
| acceptance | dotnet build apps/booking/VietRide.Booking.sln -c Release 0W/0E; dotnet format --verify-no-changes clean; dotnet test runs the new NetArchTest layering test green; AddVietRideMediatRBehaviors(handlerAssemblies:[typeof(ApplicationAssemblyMarker).Assembly]) registered in Program.cs; AddInfrastructure(configuration) exists (repos + Trip client registered in 12.2/12.3 build on it) and does NOT call AddVietRideDbContext / AddDbContext (DbContext already wired at Program.cs line 20 — no double registration); Idempotency wiring lives here since 12.0 owns Program.cs — AddVietRideIdempotency("booking") in the service collection AND UseVietRideIdempotency() in the request pipeline (Task 12.3 only depends on this, does not re-wire it) |
| source citations | apps/trip/src/VietRide.Trip.Api/Program.cs (pattern); libs/dotnet/VietRide.Shared.Application/DependencyInjection (AddVietRideMediatRBehaviors); libs/dotnet/VietRide.Shared.Application/Behaviors/{Validation,Logging,Transaction}Behavior.cs; libs/dotnet/VietRide.Shared.Web/Middleware/IdempotencyMiddleware.cs + AddVietRideIdempotency/UseVietRideIdempotency; AGENTS.md Clean Architecture dependency direction; BE_TIMELINE_VU.md Day 3 pre-reqs (baseline pattern) |

### Task 12.1 — Booking EF migration (bookings + passengers + booking_pending_actions + outbox_events)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | ef-migration |
| owned files (write set) | apps/booking/src/VietRide.Booking.Domain/Entities/{Booking,Passenger,BookingPendingAction}.cs; apps/booking/src/VietRide.Booking.Domain/Enums/{BookingStatus,PassengerBoardingStatus,TripDirection,BookingCancellationReason,BookingPendingActionReason,BookingPendingActionSeverity,BookingPendingActionResolved}.cs; apps/booking/src/VietRide.Booking.Domain/ValueObjects/BookingCode.cs; apps/booking/src/VietRide.Booking.Infrastructure/BookingDbContext.cs; apps/booking/src/VietRide.Booking.Infrastructure/Persistence/Configurations/*.cs; apps/booking/src/VietRide.Booking.Infrastructure/Migrations/** (the new migration must hand-write the max-5-passengers trigger via migrationBuilder.Sql in Up()/Down() — see acceptance); apps/booking/src/VietRide.Booking.Infrastructure/Design/BookingDbContextDesignFactory.cs (MANDATORY — must update the design factory so `dotnet ef migrations add` runs without booting the host) |
| forbidden scope | .env, secrets; db-schema/** (read-only reference — do NOT edit canonical DDL); other services; libs/**; gateway; git ops; do NOT add Voucher/BookingStats/BookingTransfer/VoucherUsage/OperatorVoucherConsent tables (deferred to Day 14/17); do NOT add trip_seats / TripSeat (Trip-owned, vietride_trip schema — Day-9/11 Trip side, NOT this migration) |
| depends on | 12.0 |
| invariant flags | CRLF .cs; snake_case schema vietride_booking; soft-delete only where schema declares it (this set has none — do NOT invent deleted_at); Money = BIGINT VND (base_fare/discount_amount/total_amount); no cross-DB FK (trip_id/operator_id/passenger_user_id/pickup_*/dropoff_* are logical FK — no REFERENCES); migration reversible (Down()); EF migration via IDesignTimeDbContextFactory (no host boot) |
| acceptance | dotnet ef migrations add Name succeeds via IDesignTimeDbContextFactory (BookingDbContextDesignFactory) with NO host boot; dotnet ef database update from empty DB creates the 4 tables matching schema.sql incl. CHECK constraints chk_bookings_pickup_exactly_one, chk_bookings_dropoff_at_most_one, chk_bookings_total_le_base, chk_bookings_amounts_non_negative; indexes uq_bookings_booking_code, uq_passengers_booking_seat, uq_booking_pending_actions_active_per_booking (partial WHERE resolved_at IS NULL), idx_bookings_passenger_user_id_created_at; Down() drops cleanly; total_amount configured immutable-after-insert per schema comment; migration Up() emits via migrationBuilder.Sql(...) the trigger function trg_check_passenger_max_per_booking() AND the BEFORE INSERT trigger trg_passengers_max_5_per_booking ON passengers (EF code-first does NOT auto-generate Postgres triggers; max-5-passengers-per-booking enforced at DB layer per schema comment line 149) and Down() drops both, verbatim per db-schema/booking/schema.sql lines 344-363; build+format clean |
| source citations | db-schema/booking/schema.sql lines 13-172, 305-319 (enums + the 4 tables); db-schema/booking/README.md Design Decisions + Cross-service References; technical_context_v7 section 6.1 line 1695 (Passenger operational-only, no PII; max 5); AGENTS.md Persistence (BIGINT VND, no cross-DB FK) |

### Task 12.2 — Trip inter-service HTTP client (ITripServiceClient, seat-lock seam consumer) + abstraction
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | (none) — mirror the DI/AddHttpClient + delegating-handler WIRING of apps/trip/.../ExternalClients/IdentityInternalClient.cs, but follow BSOT §3.5 naming/location (ITripServiceClient at Abstractions/ServiceClients/, impl TripServiceClient at Infrastructure/Http/) — the Trip file uses the older non-compliant *InternalClient/ExternalClients shape; do NOT copy its names/paths |
| owned files (write set) | apps/booking/src/VietRide.Booking.Application/Abstractions/ServiceClients/ITripServiceClient.cs (+ result/DTO records: TripSnapshot, SeatLockResult, error variants); apps/booking/src/VietRide.Booking.Infrastructure/Http/TripServiceClient.cs; apps/booking/src/VietRide.Booking.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs (register typed HttpClient + Internal JWT + correlation handlers + resilience policies); apps/booking/src/VietRide.Booking.Api/appsettings.json (Trip:BaseUrl key) |
| forbidden scope | .env, secrets; the Trip service (apps/trip/**) — Booking does NOT implement lock/book/release endpoints, only calls them; db-schema/**; libs/** (reuse VietRide.Shared.Http handlers/policies as-is); gateway; git ops |
| depends on | 12.0 |
| invariant flags | CRLF .cs; Internal JWT outbound via InternalJwtDelegatingHandler (HS256, X-Internal-Auth, TTL 120s — reuse Shared.Http, do NOT re-implement); seam request/response shapes are FROZEN (BSOT section 13 1.8.0) — match verbatim, do NOT alter; no cross-DB FK (this is the HTTP enforcement of the logical FK) |
| acceptance | ITripServiceClient (at Abstractions/ServiceClients/, impl TripServiceClient at Infrastructure/Http/ per BSOT §3.5 line 935) exposes GetTripSnapshotAsync, LockSeatsAsync (all-or-nothing; returns token+lockedSeats+expiresAt or typed BOOKING_SEAT_UNAVAILABLE/BOOKING_TRIP_NOT_BOOKABLE/TRIP_NOT_FOUND), BookSeatsAsync (idempotent 204), ReleaseSeatsAsync (idempotent no-op 204); typed HttpClient registered with CorrelationIdDelegatingHandler + InternalJwtDelegatingHandler + retry + circuit-breaker policies (mirrors Trip); unit tests with a stubbed HttpMessageHandler cover 200/404/409 mapping; build+format clean |
| source citations | VietRide_API_Contract_v1.md lines 1065-1179 (GET trip snapshot + lock/release/book seat endpoints, request/response/errors); BSOT section 13 row 1.8.0 (seam freeze, book-seats name, no event on seat path); BSOT §3.2.5/§3.5 lines 424-428, 476-480, 935 (inter-service client = I<Service>ServiceClient at Abstractions/ServiceClients/ + impl at Infrastructure/Http/; ExternalClients/ is for VnPay/SendGrid/Firebase/Google only); apps/trip/src/VietRide.Trip.Infrastructure/ExternalClients/IdentityInternalClient.cs + InfrastructureServiceCollectionExtensions.cs (DI/handler WIRING reference ONLY — its naming predates BSOT and is non-compliant); libs/dotnet/VietRide.Shared.Http/{Handlers,Resilience}; AGENTS.md Internal JWT |

### Task 12.3 — POST /v1/bookings: CreateBooking command + handler + controller (saga core)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | apps/booking/src/VietRide.Booking.Application/Features/Bookings/CreateBooking/{CreateBookingCommand,CreateBookingCommandHandler,CreateBookingCommandValidator,CreateBookingResult}.cs; apps/booking/src/VietRide.Booking.Application/Abstractions/Repositories/IBookingRepository.cs; apps/booking/src/VietRide.Booking.Application/Abstractions/Services/IBookingService.cs (at least ReleaseSeatsAsync — the cross-handler compensation primitive, reused by Day-17 cancel/refund); apps/booking/src/VietRide.Booking.Application/Services/BookingService.cs (implements IBookingService; THIS day inject ONLY the dependencies that already exist — ITripServiceClient + IClock + IBookingRepository; do NOT inject IVoucherRepository even though BSOT §3.2.5 line 657 lists it in the FINAL constructor — voucher is deferred to Day 14; release-seats compensation lives HERE, NOT inlined in the handler, per BSOT §3.2.5/§3.2.6 line 686 shared-logic-2-handler rule); apps/booking/src/VietRide.Booking.Application/Abstractions/ServiceClients/IPaymentServiceClient.cs (payment seam — minimal shape this day); apps/booking/src/VietRide.Booking.Infrastructure/Http/PaymentServiceClient.cs (stub returning success for WALLET this day; real debit Day 15/16 — the seam path is POST /internal/v1/payments/charge with {referenceType, referenceId, userId, amount, method} -> {paymentId, status, paymentRedirectUrl} per API Contract line 1565; do NOT invent /payments/wallet-charge — see post-dispatch carryover C1 for the BSOT discrepancy); apps/booking/src/VietRide.Booking.Infrastructure/Persistence/Repositories/BookingRepository.cs; apps/booking/src/VietRide.Booking.Domain/Entities/Booking.cs (domain methods Confirm()/MarkPendingPayment() if not added in 12.1 — coordinate; 12.1 owns the file shape); apps/booking/src/VietRide.Booking.Api/Controllers/BookingsController.cs; apps/booking/src/VietRide.Booking.Api/Controllers/Requests/CreateBookingRequest.cs; apps/booking/src/VietRide.Booking.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs (register IBookingRepository + IBookingService + IPaymentServiceClient via AddInfrastructure — NOTE 12.2 also touches this file; serialize 12.2 then 12.3, do NOT parallel); apps/booking/tests/VietRide.Booking.UnitTests/Features/Bookings/**; apps/booking/tests/VietRide.Booking.IntegrationTests/** (new project; mock ITripServiceClient + IPaymentServiceClient); docs/api/postman/* (booking POST request) — NOTE: the handler injects IIntegrationEventOutbox (libs/dotnet/VietRide.Shared.Application/Outbox/IIntegrationEventOutbox.cs) for booking.booking.confirmed — the SAME application-facing outbox seam existing Identity handlers use (RegisterCommandHandler ctor); enqueue via _outbox.EnqueueAsync(eventType, payloadJson, ct) within the booking tx, do NOT reference Shared.Messaging/Persistence outbox types directly. NOTE: Idempotency middleware (UseVietRideIdempotency() + AddVietRideIdempotency("booking")) is wired in Task 12.0 (owns Program.cs); 12.3 depends on it but does NOT touch Program.cs |
| forbidden scope | .env, secrets; apps/trip/** (only the mocked client); voucher logic beyond a no-op passthrough (full voucher validation is Day 14 — accept voucherCode but treat as no discount this day, or omit; do NOT implement voucher tables); payment integration beyond the IPaymentServiceClient seam call (real wallet debit is Day 15/16 — the handler MUST route the payment step through IPaymentServiceClient (BSOT §3.5 line 427 Abstractions/ServiceClients/, impl Infrastructure/Http/ line 479) and MUST NOT auto-flip CONFIRMED unless that seam returns success; this day the stub returns success for WALLET so the saga reaches CONFIRMED, VNPay leaves PENDING_PAYMENT + returns redirect placeholder); libs/**; gateway; round-trip / history / cancel / edit endpoints (Day 13+); do NOT add PII columns (fullName/phoneNumber/idNumber) to the passengers table or Passenger entity — passengers is operational-only per db-schema/booking/schema.sql COMMENT line 149; git ops |
| depends on | 12.0, 12.1, 12.2 |
| invariant flags | CRLF .cs; MediatR v11 (controller calls MediatR.Send, never a service directly); ApiResponse envelope (ADR 0004) with meta.traceId; Idempotency-Key required on POST (IDEMPOTENCY_KEY_MISMATCH 422 on same-key/different-body); Money floor-1000 before persist (Money.FromRaw); error codes UPPER_SNAKE_CASE from section 5.9; tenant/role = PASSENGER (Gateway authRequired user); booking.booking.confirmed Outbox routing key shape svc.aggregate.verb_past |
| acceptance | POST /v1/bookings returns the documented 201 envelope; passenger PII (fullName/phoneNumber/idNumber from the request) is validated non-empty in CreateBookingValidator but NOT persisted — dropped after validation, only seat_number + operational fields land in passengers (schema.sql line 149); max 5 seats enforced (BOOKING_MAX_SEATS_EXCEEDED 422); calls lock-seats first, all-or-nothing — on lock failure returns the mapped 409/404 and creates NO booking; on lock success creates Booking PENDING_PAYMENT + N Passenger in one tx; payment goes through IPaymentServiceClient (stub success for WALLET this day) — handler does NOT flip CONFIRMED unless the seam returns success; WALLET success to book-seats + status CONFIRMED + booking.booking.confirmed enqueued in the SAME tx via IIntegrationEventOutbox; any downstream failure after lock to release-seats called via IBookingService.ReleaseSeatsAsync (compensation lives in BookingService, not inlined in the handler) and no dangling booking; at least 1 happy + 1 error unit test; integration test with mocked ITripServiceClient proves all-or-nothing + compensation ordering; Swagger renders; dotnet build+format+test clean on booking sln |
| source citations | VietRide_API_Contract_v1.md lines 681-721 (request/response; passenger.fullName/phoneNumber/idNumber lines 694-698); db-schema/booking/schema.sql lines 133-149 (passengers operational-only, no PII columns, COMMENT line 149); BSOT section 3.2.5 lines 641-666 (IBookingService incl. ReleaseSeatsAsync line 646) + 3.2.6 line 686 (service mandatory when shared logic >=2 handler); BSOT section 3.5 lines 427,479 (IPaymentServiceClient + PaymentServiceClient location); technical_context_v7 section 6.9 lines 3386-3497 (saga happy path + compensation table); BSOT section 5.9 lines 1335-1339,1401 (error codes); BSOT section 7.3 line 1734 (booking.booking.confirmed payload); libs/dotnet/VietRide.Shared.Application/Outbox/IIntegrationEventOutbox.cs (application-facing outbox seam the handler injects — same one Identity RegisterCommandHandler uses); VietRide_API_Contract_v1.md line 1565 (POST /internal/v1/payments/charge — canonical payment-charge seam path the IPaymentServiceClient stub targets; supersedes BSOT §7.2 line 1699 /payments/wallet-charge per SOT hierarchy, see carryover C1); BSOT section 10 line 2360 (SEAT_LOCK_TTL_MINUTES=10); libs/dotnet/VietRide.Shared.Kernel/ValueObjects/Money.cs (floor-1000); libs/dotnet/VietRide.Shared.Web/Middleware/IdempotencyMiddleware.cs (wired in 12.0) |

## Dispatch order
1. **Task 12.0** (baseline) — serial, blocks all. Parallel-safe: no.
2. **Task 12.1** (migration + domain entities) and **Task 12.2** (Trip internal client) — both depend only on 12.0 and have disjoint write sets (Domain/Infra-Persistence/Migrations vs Application-Abstractions/Infra-ExternalClients). Parallel-safe in principle, BUT both could touch InfrastructureServiceCollectionExtensions.cs (12.2 registers the client). To keep disjoint write sets, 12.1 does NOT edit that file (repo registration folded into 12.3). Under the single-tree / no-worktree policy, run them serial (either order). Parallel-safe: no.
3. **Task 12.3** (endpoint + saga) — depends on 12.0+12.1+12.2; serial last. Both 12.2 and 12.3 edit apps/booking/src/VietRide.Booking.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs (12.2 registers the Trip client; 12.3 registers repos + IBookingService + IPaymentServiceClient) — run 12.2 fully then 12.3, never in parallel. Parallel-safe: no.

## Progress tracker
> Orchestrator bookkeeping — informational only. /audit-day re-verifies independently against the SOT.

| Task | Status | Review verdict | Date | Notes |
|---|---|---|---|---|
| 12.0 | done | APPROVED | 2026-06-11 | 0 patch rounds; idempotency wiring + NetArchTest baseline; human /verify batched at end of day |
| 12.1 | done | APPROVED | 2026-06-11 | 1 patch round (BookingCode.Parse guards + crypto RNG); migration apply+rollback verified on empty Postgres 16; human /verify batched at end of day |
| 12.2 | done | APPROVED | 2026-06-11 | 1 patch round (ILogger in release compensation + 409 release test + XML doc); 11 client tests (200/404/409 mapping); InternalJwtTokenFactory duplication = carryover; human /verify batched at end of day |
| 12.3 | todo | — | — | — |

Legend: todo / in progress / done (reviewer APPROVED + human /verify) / done-with-carryover / blocked

## Resolved decisions (open questions closed 2026-06-11 — baked in, no longer block dispatch)
The human resolved every open question. Decisions are baked into the DoD and tasks above; recorded here for the audit trail and so the reviewer PLAN-REVIEW gate sees the rationale.

- **D1 (was Q1) — Ship Booking-side with mocked Trip + Payment.** Implement Day-12 against a mocked `ITripServiceClient` + `IPaymentServiceClient`. The BE_TIMELINE live-concurrency DoD ("2 concurrent bookings on same seat -> one wins" + "HELD auto-release after 10 min") is **DEFERRED** because the real Redis lock + `trip_seats` live in Trip (Day 9/11, not yet built). **Day-12 acceptance is the mocked-path tests** — at least 1 happy + 1 error per handler, all-or-nothing + compensation ordering proven against the mocked client — **NOT the live race.** The live stress test is tracked as Carryover CO1 below.
- **D2 (was Q2) — BookingCode long form `VR-yyyyMMdd-XXXXXXXX`** (date + 8-char base32 uppercase). Confirmed canonical: db-schema/booking/schema.sql COMMENT line 124 + VietRide_API_Contract_v1.md line 713 (`VR-20260518-ABCD1234`) agree. Task 12.1 keeps this default. The BSOT §9.10 short form is stale -> Carryover C2.
- **D3 (was Q3) — Redis seat-lock key prefix is Trip-side, no Day-12 action.** The `seat_lock:` (API Contract line 985) vs `booking:seat_lock:` (BSOT line 2148) discrepancy only affects the Trip implementation of lock-seats (Trip owns the key). No Booking change this day; flagged for the Day-11 Trip track -> Carryover CO2.
- **D4 (was Q4) — IPaymentServiceClient stub.** The WALLET path goes through the `IPaymentServiceClient` seam (POST `/internal/v1/payments/charge`) returning success this day so the saga reaches CONFIRMED; real wallet debit is Day 15/16. Already baked into Task 12.3 (the handler does NOT auto-flip CONFIRMED unless the seam returns success; VNPay leaves PENDING_PAYMENT + redirect placeholder).
- **D5 (was Q5) — Voucher deferred to Day 14.** Accept `voucherCode` in the request but apply **no discount** (no-op passthrough). No voucher tables this day (Voucher/VoucherUsage/OperatorVoucherConsent are the Day-14 migration). Baked into Task 12.3 forbidden scope + the totalAmount computation.

## Carryover (preconditions + dependencies the human/next-day tracks)
Two flavours: SOT-doc reconciles (C*) the plan resolved locally per the source-of-truth hierarchy, and forward dependencies (CO*) that a later day must close. None block Day-12 dispatch.

**Forward dependencies (a later day closes these):**
- **CO1 — Live seat-race + 10-min auto-release stress test (Day 11, or whenever the Trip seam endpoints land).** Run the live "2 concurrent bookings on same seat -> one wins" race and the "HELD auto-release after 10 min" expiry end-to-end Booking <-> Trip, against the real Redis lock + `trip_seats` in Trip. Until then, Day-12 covers it ONLY via mocked-client unit/integration tests (D1). This is the deferred half of the BE_TIMELINE Day-12 Review; `/audit-day` must NOT mark Day-12 incomplete for it — it is a Trip-side precondition, not a Booking-side gap. Source: BE_TIMELINE_VU.md Day 12 Review; BSOT §13 row 1.8.0 (seam freeze, commit a4c69bd); SEAT_LOCK_TTL_MINUTES=10 (BSOT §10 line 2360).
- **CO2 — Redis seat-lock key prefix (Trip-side, was Q3).** When the Trip track implements lock-seats, pick ONE key shape to avoid a split-brain: API Contract line 985 = `seat_lock:{tripId}:{seatNumber}`; BSOT line 2148 = `booking:seat_lock:{tripId}:{seatNumber}`. Trip owns the key; no Booking change.

**SOT-doc reconciles (human to patch the losing doc; dispatch proceeds on the resolved value):**
- **C1 — Payment charge seam path (BSOT §7.2 vs API Contract).** API Contract line 1565 = `POST /internal/v1/payments/charge` (`{referenceType, referenceId, userId, amount, method}` -> `{paymentId, status, paymentRedirectUrl}`); BSOT §7.2 line 1699 = `POST /internal/v1/payments/wallet-charge`. Resolved per hierarchy (API Contract #2 > BSOT #3) -> Task 12.3 IPaymentServiceClient targets `/internal/v1/payments/charge`. **Human action: reconcile BSOT §7.2 line 1699 (and §13 if it repeats the name) to `/internal/v1/payments/charge`, or document why wallet-charge is a separate distinct endpoint.** Until then, do NOT invent a third path.
- **C2 — BookingCode format (BSOT §9.10 stale short form).** BSOT §9.10 line 2165 still says `VR-<4 char base32>`; db-schema/booking/schema.sql COMMENT line 124 + API Contract line 713 both say the long form `VR-yyyyMMdd-XXXXXXXX` (date + 8-char base32 uppercase). Task 12.1 implements the long form (RESOLVED D2). **Human action: patch BSOT §9.10 line 2165 `VR-<4 char base32>` -> `VR-yyyyMMdd-XXXXXXXX` (and the stale example at line ~1985) to kill the short form.**

## Open questions
None — Q1-Q5 were resolved by the human on 2026-06-11 (see Resolved decisions D1-D5). No genuinely-unknown items remain; dispatch is unblocked.
