# Day 10 — Plan

- **Timeline ref**: BE_TIMELINE_VU.md -> Day 10 (Jira: SCV-78; passenger stub SCV-76)
- **Prior checklist**: docs/handoff/day-9-checklist.md -> **not found** (no day-8 or day-9 plan/checklist exists; see Carry-over)
- **Plan status**: DRAFT -> (reviewer) APPROVED / REVISION-REQUIRED

## Carry-over / repo-state findings (READ before dispatch)
- Days 8 and 9 are NOT implemented. apps/trip Domain has only Station, OperatorStation, Stop (migration 20260608111138_InitialTripStationsStops). No day-8/day-9 handoff docs. Does NOT block Day 10. Day 10 is scoped to Identity + shared libs and does NOT touch Trip Route/Vehicle.
- Outbox infra largely exists: libs/dotnet/VietRide.Shared.Messaging/Outbox/ (OutboxBackgroundService, OutboxEventEnvelope, OutboxOptions, IOutboxStore, IOutboxPublisher), RabbitMq/RabbitMqEventPublisher.cs, AddVietRideMessaging DI helper. outbox_events table exists in BOTH Identity and Trip DBs via VietRideDbContextBase.OutboxEvents. Table + poller + publisher are DONE.
- Wiring is NOT done; blocking interface split (Task 10.0): TWO incompatible IOutboxStore interfaces. VietRide.Shared.Persistence.Outbox.IOutboxStore = AddAsync(OutboxEvent) + GetUnprocessedAsync -> OutboxEvent (implemented by OutboxStore, registered by AddVietRideDbContext). VietRide.Shared.Messaging.Outbox.IOutboxStore = FetchPendingAsync -> OutboxEventEnvelope + MarkPublishedAsync + MarkFailedAsync (resolved by OutboxBackgroundService.DrainOnceAsync). The Messaging-side store has NO implementation, so the worker idles forever (logs IOutboxStore not registered). OutboxEvent entity has no NextAttemptAt column though the envelope/worker assume backoff.
- Identity emits NO events today. Program.cs does not call AddVietRideMessaging; handlers write ActivityLog but no outbox row.
- No IdempotencyMiddleware exists under libs/dotnet (BSOT 3.2 lists Shared.Web/Middleware/IdempotencyMiddleware.cs but file absent). Day-10 deliverable is the placeholder in Shared.Web; Booking/Payment/Parcel do not exist yet so it cannot be wired into them.
- No passenger stub endpoints exist. Contract has GET /v1/users/me (Day 4) but NO /v1/passenger/me or /v1/passenger/bookings -- see Open questions.
- Handlers commit via TransactionBehavior pipeline (one SaveChanges at end), so an outbox AddAsync inside a handler commits atomically with the business write.
- Working tree clean (no dirty/untracked files to protect).

## Objective
Make the Outbox actually deliver: reconcile the split IOutboxStore so OutboxBackgroundService drains real rows, wire AddVietRideMessaging into Identity, and emit the three Identity integration events the registry mandates (identity.user.created, identity.operator.approved, identity.operator.suspended) transactionally from their handlers. Add the placeholder Redis IdempotencyMiddleware to Shared.Web for Sprint-3 reuse, and ship the passenger profile/booking-history stub endpoints (empty data).

## Success criteria (DoD -- binary, verifiable)
- [ ] OutboxBackgroundService drains a PENDING outbox_events row to RabbitMQ vietride.events and marks it PUBLISHED (one IOutboxStore, implemented, registered).
- [ ] Admin approving an operator inserts an identity.operator.approved outbox row in the SAME transaction as the status change; after one poll tick it is published with routing key identity.operator.approved and payload { operatorId, approvedAt } (BSOT 7.3).
- [ ] Passenger register inserts identity.user.created row, payload { userId, role, email, createdAt }; operator suspend inserts identity.operator.suspended, payload { operatorId, suspendedAt }.
- [ ] Killing the publisher (RabbitMQ down) leaves the row PENDING/FAILED with incremented RetryCount; on restart it publishes (at-least-once). Retries are BOUNDED: `FetchPendingAsync` excludes rows whose `RetryCount > MaxRetryCount`, so an exhausted row is parked as FAILED (no infinite republish) — this is what makes "retry <= MaxRetryCount" verifiable. Retry cadence = poll interval (NO per-row backoff delay under Q4 option (a); `ComputeBackoff` is not persisted/gating).
- [ ] IdempotencyMiddleware placeholder exists in Shared.Web: same Idempotency-Key + same body -> cached response; same key + different body -> 422 IDEMPOTENCY_KEY_MISMATCH (BSOT 5.6/9.8). Not required wired to any service yet.
- [ ] GET /v1/passenger/me and GET /v1/passenger/bookings return 200 ApiResponse envelope (bookings = empty list) and route through the Gateway.
- [ ] dotnet build + dotnet format --verify-no-changes clean on libs/dotnet/VietRide.Libs.sln and the Identity solution; NetArchTest layering test still green; new handler tests pass.

## Contract changes
- Events (existing registry, now implemented -- no new keys): identity.user.created { userId, role, email, createdAt }, identity.operator.approved { operatorId, approvedAt }, identity.operator.suspended { operatorId, suspendedAt } -- BSOT 7.3. Routing-key shape svc.aggregate.verb_past enforced.
- Error code: IDEMPOTENCY_KEY_MISMATCH (422, Same key different body) -- PRESENT in BSOT 5.9 error-code registry (BACKEND_SOURCE_OF_TRUTH.md:1401). Q3 RESOLVED -- code exists, NO BSOT edit needed.
- Endpoints (new -- Q1 RESOLVED, both ship now as stubs): GET /v1/passenger/me -> 200 ApiResponse whose data REUSES the existing GetMeResponseDto projection VERBATIM (fields Id, Email, DisplayName, Phone, Role, OperatorId, Status, AvatarUrl -- apps/identity/src/VietRide.Identity.Application/Features/Users/GetMe/GetMeResponseDto.cs; same mapping as GetMeQueryHandler.cs lines 23-31). Do NOT invent passenger-specific fields. GET /v1/passenger/bookings -> 200 ApiResponse with data = empty paginated envelope { items: [], page: 1, pageSize: 20, total: 0 } (SCV-76). Only the pagination envelope is fixed; the booking ITEM schema is NOT defined here. Add a VietRide_API_Contract_v1.md + Postman entry for BOTH, each explicitly marked `stub -- item schema finalized in Sprint 3 (SCV-76 / Booking)`.
- Gateway routes: add /v1/passenger/* -> identity (5001) in apps/gateway/src/config/routes.ts.
- No migration, no schema change. Q4 DECIDED = option (a): Task 10.0 reuses the existing outbox_events schema as-is. NO next_attempt_at column is added to either Identity or Trip DB. Retry is poll-cadence (every not-yet-exhausted FAILED row re-fetched each tick — there is NO per-row backoff delay; `ComputeBackoff` is computed/logged but not persisted/gating) and BOUNDED by `FetchPendingAsync` excluding `RetryCount > MaxRetryCount`. DB-level due-time scheduling [option (b): add next_attempt_at to BOTH DBs, giving real per-row exponential backoff] is a DEFERRED future enhancement, not in scope this day.

## Tasks

### Task 10.0 -- Reconcile the split IOutboxStore so the Outbox worker drains real rows (architecture baseline; DO FIRST)

> **Verified repo state (read 2026-06-09, before this revision):** ProjectReference direction is **Messaging -> Persistence** (libs/dotnet/VietRide.Shared.Messaging/VietRide.Shared.Messaging.csproj line 6 references VietRide.Shared.Persistence.csproj; Persistence does NOT reference Messaging). So the unified `IOutboxStore` lives in **VietRide.Shared.Persistence.Outbox** and OutboxBackgroundService (in Shared.Messaging) resolves it via the existing reference -- NO new/reverse ProjectReference, NO circular ref, no layering violation. Today there are TWO `IOutboxStore`: Persistence = `AddAsync(OutboxEvent)` + `GetUnprocessedAsync -> IReadOnlyList<OutboxEvent>` (implemented by OutboxStore, registered line 42 of PersistenceServiceCollectionExtensions); Messaging = stub `FetchPendingAsync -> IReadOnlyList<OutboxEventEnvelope>` + `MarkPublishedAsync` + `MarkFailedAsync` (NO impl -> worker resolves null -> idles). `OutboxEvent` entity has NO NextAttemptAt field and NO DB column; `OutboxEventEnvelope.NextAttemptAt` is unbacked. **Verified additionally for this revision:** (i) the only references to `GetUnprocessedAsync` are its own interface decl + the OutboxStore impl -- NO production or test caller (grep, repo-wide) -> safe to delete; (ii) `OutboxBackgroundService.DrainOnceAsync` NEVER reads `OutboxEventEnvelope.NextAttemptAt` -- backoff is computed solely from `RetryCount` via `ComputeBackoff` (OutboxBackgroundService.cs lines 84-108), so projecting `NextAttemptAt = null` cannot break the worker; (iii) **type mismatch to bridge in the projection:** `OutboxEvent.CreatedAt`/`PublishedAt` are `DateTimeOffset`/`DateTimeOffset?` (OutboxEvent.cs lines 13-14) but `OutboxEventEnvelope.CreatedAt`/`PublishedAt` are `DateTime`/`DateTime?` (envelope lines 19-20) -- `FetchPendingAsync` MUST convert (`.UtcDateTime` / `?.UtcDateTime`); (iv) **`OutboxEventEnvelope` MUST be MOVED from Messaging into VietRide.Shared.Persistence/Outbox/** (namespace becomes `VietRide.Shared.Persistence.Outbox`): the unified `IOutboxStore.FetchPendingAsync` returns it and lives in Persistence, so leaving the DTO in Messaging would force a Persistence->Messaging reference and CYCLE -- moving it keeps interface + DTO co-located in Persistence with no new edge; (v) `OutboxBackgroundService` is declared IN `namespace VietRide.Shared.Messaging.Outbox`, so today it resolves `IOutboxStore` same-namespace -- after deleting the stub it needs ONE added `using VietRide.Shared.Persistence.Outbox;` (there is NO existing Messaging-IOutboxStore using line to remove); (vi) `MessagingServiceCollectionExtensions.cs` references the Messaging `IOutboxStore` only inside an XML-doc see-cref (lines 17-19) -- repoint that doc comment at the Persistence interface so no dangling cref remains.
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | none -- surgical refactor of the Messaging/Persistence outbox seam. NO migration (Q4 = option (a), reuse existing schema) |
| owned files (write set) | libs/dotnet/VietRide.Shared.Messaging/Outbox/IOutboxStore.cs (**DELETE** -- duplicate stub), libs/dotnet/VietRide.Shared.Messaging/Outbox/OutboxEventEnvelope.cs (**MOVE -> delete here, recreate under VietRide.Shared.Persistence/Outbox/ with namespace VietRide.Shared.Persistence.Outbox**), libs/dotnet/VietRide.Shared.Messaging/Outbox/OutboxBackgroundService.cs, libs/dotnet/VietRide.Shared.Messaging/DependencyInjection/MessagingServiceCollectionExtensions.cs, libs/dotnet/VietRide.Shared.Messaging/VietRide.Shared.Messaging.csproj (**verify only; existing Messaging -> Persistence ProjectReference should remain**), libs/dotnet/VietRide.Shared.Persistence/Outbox/IOutboxStore.cs (**canonical single interface**), libs/dotnet/VietRide.Shared.Persistence/Outbox/OutboxEventEnvelope.cs (**NEW -- moved from Messaging**), libs/dotnet/VietRide.Shared.Persistence/Outbox/OutboxStore.cs, libs/dotnet/VietRide.Shared.Persistence/Outbox/OutboxEvent.cs, libs/dotnet/VietRide.Shared.Application/Outbox/IIntegrationEventOutbox.cs (**NEW — B2-clean application-facing abstraction; string-based, see Decision B2**), libs/dotnet/VietRide.Shared.Persistence/Outbox/IntegrationEventOutbox.cs (**NEW — impl wrapping OutboxStore.AddAsync; registered in PersistenceServiceCollectionExtensions**), libs/dotnet/VietRide.Shared.Persistence/DependencyInjection/PersistenceServiceCollectionExtensions.cs, libs/dotnet/VietRide.Shared.Persistence/VietRide.Shared.Persistence.csproj (**verify only; MUST NOT add Persistence -> Messaging ProjectReference**), outbox tests in the existing shared-lib test project (place under the project that already covers Shared.Persistence/Shared.Messaging outbox -- discover the exact .csproj before adding; do NOT create a new test project). No migration files. **Verify-only (MUST NOT be edited):** both .csproj files above and RabbitMqEventPublisher.cs. **Do NOT touch** OutboxOptions.cs / OutboxEventStatus enum / IEventPublisher / IOutboxPublisher (unchanged). |
| forbidden scope | Identity Application/handlers (10.2), any controller, .env/secrets, RabbitMqEventPublisher.cs (correct -- do not touch), other services, git branch/commit/push |
| depends on | none |
| invariant flags | CRLF for .cs - CPM no Version= - MediatR v11 (untouched) - Outbox routing-key svc.aggregate.verb_past preserved - no cross-DB FK - NO migration / NO schema change (Q4 option (a)) |
| acceptance | **ONE `IOutboxStore`, canonical in `VietRide.Shared.Persistence.Outbox` (the interface stays where it already is -- it is NOT relocated). NO new ProjectReference: the existing Messaging->Persistence ref (Messaging.csproj line 6) already lets OutboxBackgroundService reach it; Persistence MUST NOT gain a ->Messaging ref (would cycle). The `OutboxEventEnvelope` DTO it returns IS moved into Persistence -- see (2).** (1) **DELETE** libs/dotnet/VietRide.Shared.Messaging/Outbox/IOutboxStore.cs (the stub duplicate -- it has no implementation and no registration). (2) The surviving Persistence `IOutboxStore` is the UNION needed by both sides; its members are EXACTLY four: `AddAsync(OutboxEvent outboxEvent, CancellationToken ct = default)`, `FetchPendingAsync(int batchSize, int maxRetryCount, CancellationToken ct) -> Task<IReadOnlyList<OutboxEventEnvelope>>` (the `maxRetryCount` param bounds retries — see (7)), `MarkPublishedAsync(Guid id, DateTime publishedAt, CancellationToken ct)`, `MarkFailedAsync(Guid id, string error, DateTime nextAttemptAt, CancellationToken ct)`. `FetchPendingAsync` returns `OutboxEventEnvelope`, which today lives in Messaging. Since the interface returning it lives in Persistence, the DTO cannot stay in Messaging without forcing a Persistence->Messaging ref (CYCLE). **So MOVE `OutboxEventEnvelope.cs` from VietRide.Shared.Messaging/Outbox/ INTO VietRide.Shared.Persistence/Outbox/, changing its namespace to `VietRide.Shared.Persistence.Outbox`.** This co-locates the single interface with its DTO in Persistence; the existing Messaging->Persistence ref resolves both for OutboxBackgroundService. (3) **REMOVE the redundant `GetUnprocessedAsync`** -- verified there is NO caller anywhere except its own decl + impl (grep, repo-wide); the worker uses `FetchPendingAsync`, handlers (10.2) use only `AddAsync`. (4) OutboxStore implements all four members. `FetchPendingAsync` projects `OutboxEvent -> OutboxEventEnvelope`; **bridge the type mismatch: `OutboxEvent.CreatedAt`/`PublishedAt` are `DateTimeOffset`/`DateTimeOffset?` -> set `OutboxEventEnvelope.CreatedAt = row.CreatedAt.UtcDateTime` and `PublishedAt = row.PublishedAt?.UtcDateTime`** (envelope properties are `DateTime`/`DateTime?`). Because OutboxEvent has NO NextAttemptAt field/column, **set `OutboxEventEnvelope.NextAttemptAt = null`** in the projection -- keep the property on the DTO for forward-compat (deferred option (b)). This is safe: `DrainOnceAsync` NEVER reads `NextAttemptAt` (backoff comes from `RetryCount` via `ComputeBackoff`, OutboxBackgroundService.cs 84-108), so null cannot break the worker. (5) OutboxBackgroundService is declared in `namespace VietRide.Shared.Messaging.Outbox` and resolves `IOutboxStore` + `OutboxEventEnvelope` same-namespace today; after the delete+move, **add `using VietRide.Shared.Persistence.Outbox;`** to OutboxBackgroundService.cs. There is NO pre-existing Messaging-`IOutboxStore` `using` line to delete (the types were same-namespace). (6) `MessagingServiceCollectionExtensions.cs` mentions the Messaging `IOutboxStore` only in an XML-doc `<see cref="IOutboxStore"/>` (lines 17-19) -- repoint it at `VietRide.Shared.Persistence.Outbox.IOutboxStore` (or drop the cref) so the build has no dangling reference; do NOT change its DI registrations. (7) `FetchPendingAsync(batchSize, maxRetryCount, ct)` filters `Status IN (PENDING,FAILED) AND RetryCount <= maxRetryCount` ordered by `CreatedAt ASC`, `Take(batchSize)`. **The `RetryCount <= maxRetryCount` clause is the bounded-retry mechanism (REQUIRED so DoD "retry <= MaxRetryCount" is actually achievable):** a row whose RetryCount has exceeded the cap is NEVER fetched again, so it stays parked as FAILED for ops inspection instead of being republished forever. `DrainOnceAsync` MUST pass `_options.MaxRetryCount` at the call site (change the existing `store.FetchPendingAsync(_options.BatchSize, ct)` -> `store.FetchPendingAsync(_options.BatchSize, _options.MaxRetryCount, ct)`; OutboxBackgroundService.cs is an owned file). **Honest framing of Q4 option (a):** there is NO per-row backoff *delay* (no due-time column) -- a not-yet-exhausted FAILED row is re-fetched every poll tick, so the effective retry cadence is the poll interval, NOT the exponential `ComputeBackoff` value. `ComputeBackoff`/the `nextAttemptAt` it produces are computed and logged but NOT persisted and NOT used to gate fetch; do NOT describe this as "exponential backoff" anywhere -- it is poll-cadence retry, bounded by `maxRetryCount`. (Real per-row due-time backoff = deferred option (b).) ALSO update the stale class XML-doc on `OutboxBackgroundService` (OutboxBackgroundService.cs:12-13 currently says "Transient failures use exponential backoff capped at MaxRetryCount") to the honest option-(a) framing (poll-cadence retry bounded by MaxRetryCount, no per-row delay) -- the file is already in this task's owned set. `MarkPublishedAsync` sets Status=PUBLISHED + PublishedAt. `MarkFailedAsync` persists ONLY incremented RetryCount + LastError + Status=FAILED; it **accepts the `nextAttemptAt` param (signature unchanged so DrainOnceAsync still compiles) but MUST NOT write any due-time column -- none exists** (Q4 option (a)). (8) Registration: PersistenceServiceCollectionExtensions line 42 already has `services.AddScoped<IOutboxStore, OutboxStore>()` -- now satisfies the worker (DrainOnceAsync resolves a real store, no more "not registered" idle log); keep it scoped, do NOT add a second registration. (9) **B2-clean abstraction (RESOLVED):** define `IIntegrationEventOutbox` in **Shared.Application** with EXACTLY `Task EnqueueAsync(string eventType, string payloadJson, CancellationToken ct = default)` (string-based ON PURPOSE — must not take any Shared.Messaging type, else Application<-Messaging would cycle since Messaging already references Application). Implement it in **Shared.Persistence** as `IntegrationEventOutbox` that builds `new OutboxEvent { EventType = eventType, Payload = payloadJson }` and calls `OutboxStore.AddAsync` (so it enlists in the ambient EF transaction). Register it in PersistenceServiceCollectionExtensions (scoped) right beside the IOutboxStore registration. Shared.Application MUST NOT gain a Shared.Persistence/Messaging ref (the interface is pure); Shared.Persistence already references Shared.Application so the impl compiles. NO migration, NO schema change. Tests: seed a PENDING OutboxEvent -> `DrainOnceAsync` -> publisher called once, row PUBLISHED with PublishedAt set; simulated publish failure -> RetryCount incremented + Status FAILED + LastError set, and the FAILED row (RetryCount still <= MaxRetryCount) is re-fetched on the next `FetchPendingAsync`; a FAILED row seeded with `RetryCount > MaxRetryCount` is NOT returned by `FetchPendingAsync` (bounded-retry / parked assertion); assert returned envelopes have `NextAttemptAt == null` and `CreatedAt`/`PublishedAt` correctly converted from the DateTimeOffset source. dotnet build + dotnet format --verify-no-changes clean on libs/dotnet/VietRide.Libs.sln. |
| source citations | BSOT 4.3 / 7.4 (outbox in business tx, background publisher), 7.3 (routing-key format, exchange vietride.events); repo OutboxBackgroundService.DrainOnceAsync, OutboxStore, OutboxEvent.cs (no NextAttemptAt column -- confirmed), PersistenceServiceCollectionExtensions, MessagingServiceCollectionExtensions |

### Task 10.1 -- Wire AddVietRideMessaging into Identity composition root
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | none |
| owned files (write set) | apps/identity/src/VietRide.Identity.Api/Program.cs, apps/identity/src/VietRide.Identity.Api/appsettings.json (RabbitMq + RabbitMq:Outbox sections only), apps/identity/src/VietRide.Identity.Api/VietRide.Identity.Api.csproj (add Shared.Messaging ProjectReference if Api needs it directly) |
| forbidden scope | Outbox lib internals (10.0), handlers (10.2), .env/secrets, Directory.Packages.props, other services, git ops |
| depends on | 10.0 |
| invariant flags | CRLF for .cs - LF for .json - CPM no Version= - no banned deps - do NOT add a new NuGet package |
| acceptance | Identity boots with AddVietRideMessaging(builder.Configuration) registered; OutboxBackgroundService hosted service starts; with RabbitMQ up and a seeded PENDING row, it publishes within one poll interval. dotnet build + dotnet format clean on the Identity solution. No secret committed to appsettings. |
| source citations | repo MessagingServiceCollectionExtensions.AddVietRideMessaging, OutboxOptions.SectionName=RabbitMq:Outbox, RabbitMqOptions.SectionName; Identity Program.cs; BSOT 7.4 |

### Task 10.2 -- Emit Identity integration events transactionally (user.created, operator.approved, operator.suspended)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-integration-event (per event) |
| owned files (write set) | apps/identity/src/VietRide.Identity.Application/Events/*.cs (new: UserCreatedIntegrationEvent, OperatorApprovedIntegrationEvent, OperatorSuspendedIntegrationEvent), .../Features/Auth/Register/RegisterCommandHandler.cs, .../Features/Admin/ApproveOperator/ApproveOperatorCommandHandler.cs, .../Features/Admin/SuspendOperator/SuspendOperatorCommandHandler.cs, .../VietRide.Identity.Application.csproj (add Shared.Messaging ref for IntegrationEventBase if needed), Identity unit/integration tests for the three handlers |
| forbidden scope | Outbox lib internals (10.0), Program.cs/DI (10.1), Domain invariants beyond reading, Operator initial-password flows (NO staff.password_set -- not in registry; see Q2), .env/secrets, other services, Gateway, git ops |
| depends on | 10.0, 10.1 |
| invariant flags | CRLF for .cs - routing keys EXACTLY identity.user.created / identity.operator.approved / identity.operator.suspended (lowercase svc.aggregate.verb_past) - MediatR v11 - payloads MUST match BSOT 7.3 essentials exactly - the outbox enqueue MUST run inside the handler so TransactionBehavior commits it atomically with the business write - **the handler depends on the B2-clean abstraction `IIntegrationEventOutbox` (in Shared.Application), NOT on `VietRide.Shared.Persistence.Outbox.IOutboxStore`** (Decision B2 RESOLVED = clean). Application MUST NOT gain a Shared.Persistence reference. (For context: NetArchTest only forbids the service's OWN `VietRide.Identity.Infrastructure`/`.Api` namespaces (LayeringTests.cs:79), so a direct Shared.Persistence ref would still compile/pass CI — but B2-clean was chosen for boundary cleanliness, so do not take that shortcut.) - BCrypt cost 12 unchanged - RegisterCommandHandler creates ONLY passengers (`User.CreatePassenger`, RegisterCommandHandler.cs:81), so `identity.user.created` payload `role` is ALWAYS the passenger role — do NOT emit user.created from any operator/staff-creation path (those are out of scope this task) |
| acceptance | Each handler serializes its event and enqueues it via the B2-clean `IIntegrationEventOutbox.EnqueueAsync(eventType, payloadJson, ct)` abstraction (defined in Shared.Application, implemented in Shared.Persistence wrapping `OutboxStore.AddAsync` — Application does NOT reference Shared.Persistence) with EventType = routing key and JSON payload matching 7.3 (user.created {userId,role,email,createdAt}; operator.approved {operatorId,approvedAt}; operator.suspended {operatorId,suspendedAt}). Integration test (WebApplicationFactory + EF): register passenger -> exactly one outbox_events row with EventType identity.user.created and correct payload; approve -> identity.operator.approved; suspend -> identity.operator.suspended. Row + business change share one transaction (forced-failure rollback leaves zero outbox rows). dotnet build + dotnet format clean; NetArchTest layering still green. |
| source citations | BSOT 7.3 (event keys + payload essentials), 3.5 naming; repo ApproveOperatorCommandHandler, SuspendOperatorCommandHandler, RegisterCommandHandler, IntegrationEventBase, Persistence IOutboxStore.AddAsync; API_Contract changelog 1.6.3 |

### Task 10.3 -- Placeholder Redis Idempotency-Key middleware in Shared.Web
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | none |
| owned files (write set) | libs/dotnet/VietRide.Shared.Web/Middleware/IdempotencyMiddleware.cs, libs/dotnet/VietRide.Shared.Web/DependencyInjection/* (registration helper only), libs/dotnet/VietRide.Shared.Web/VietRide.Shared.Web.csproj (**add `<PackageReference Include="StackExchange.Redis" />` with NO Version — the package is already in CPM at Directory.Packages.props:78; Shared.Web currently has only `AspNetCore.HealthChecks.Redis`, NOT the StackExchange.Redis client, verified**), unit tests under the Shared.Web test project |
| forbidden scope | Identity (idempotency not required on any Identity endpoint per 5.6) -- do NOT wire into a pipeline this day; .env/secrets; other services; the BSOT error-code registry doc (IDEMPOTENCY_KEY_MISMATCH already present at 5.9 -- no edit needed); git ops |
| depends on | none (parallel-safe with 10.0/10.1/10.2 -- disjoint write set) |
| invariant flags | CRLF for .cs - CPM no Version= (StackExchange.Redis already in CPM -- reuse) - no banned deps - error envelope per ADR 0004 (error.code UPPER_SNAKE_CASE, meta.traceId) |
| acceptance | Middleware reads Idempotency-Key header; first POST/PATCH caches {statusCode,body,bodyHash} at Redis key svc:idem:{key} TTL 24h via SETNX; same key + same body hash -> cached response verbatim; same key + different body hash -> 422 IDEMPOTENCY_KEY_MISMATCH in ApiResponse error envelope; missing header -> pass-through no-op. Unit tests cover all three branches with a fake Redis. dotnet build + dotnet format clean on libs/dotnet/VietRide.Libs.sln. NOT registered into any running service. |
| source citations | BSOT 5.6 (idempotency endpoints, mismatch -> 422 IDEMPOTENCY_KEY_MISMATCH, UUID v4), 5.9 (IDEMPOTENCY_KEY_MISMATCH registered, BACKEND_SOURCE_OF_TRUTH.md:1401), 9.8 (Redis key svc:idem:{key} TTL 24h, SETNX, body-hash compare), 3.2 directory map, ADR 0004 |

### Task 10.4 -- Passenger profile + booking-history stub endpoints + Gateway route
| Field | Value |
|---|---|
| stack/owner | cross-cutting (split: dotnet endpoints + nest gateway route) |
| implement agent | dotnet-worker (endpoints), then worker for the Gateway route edit (or split 10.4a dotnet / 10.4b nest) |
| review agent | dotnet-reviewer (endpoints) + reviewer (gateway seam) |
| skill | add-endpoint (for the .NET side) |
| owned files (write set) | apps/identity/src/VietRide.Identity.Api/Controllers/PassengerController.cs (new), apps/identity/src/VietRide.Identity.Application/Features/Passenger/** (GetPassengerProfile + GetPassengerBookings query/handler/DTO), apps/gateway/src/config/routes.ts (add /v1/passenger/* -> identity), Identity API integration tests, Postman collection entry |
| forbidden scope | Outbox/events (other tasks), real booking data (Sprint 3 -- bookings MUST return empty list), .env/secrets, other downstream services in routes.ts, git ops |
| depends on | none structurally (Q1 RESOLVED -- response shapes ratified, no longer blocked). Parallel-safe with the 10.0->10.1->10.2 chain and with 10.3 (disjoint write set) |
| invariant flags | CRLF for .cs - LF for .ts (gateway) - ApiResponse envelope (ADR 0004) - JWT-protected (passenger identity from JWT sub/userId claim) - MediatR v11 (controller -> Send, never a service) - Gateway stays thin proxy (ADR 0002) |
| acceptance | GET /v1/passenger/me returns 200 ApiResponse whose data REUSES the existing GetMeResponseDto projection VERBATIM (fields Id, Email, DisplayName, Phone, Role, OperatorId, Status, AvatarUrl -- mirror GetMeQueryHandler.cs lines 23-31; do NOT invent passenger-specific fields, do NOT add a new DTO -- reference/return GetMeResponseDto). GET /v1/passenger/bookings returns 200 ApiResponse with data = empty paginated envelope EXACTLY { items: [], page: 1, pageSize: 20, total: 0 } per SCV-76 (booking item schema NOT defined this day). Both require a valid user JWT (401 without). Gateway proxies /v1/passenger/* to identity:5001 with Internal JWT minted. Contract + Postman row added for both, each marked `stub -- item schema finalized in Sprint 3 (SCV-76 / Booking)`. Integration tests: authed 200, unauthed 401. dotnet build + dotnet format clean; gateway lint/build clean. |
| source citations | BE_TIMELINE_VU.md Day 10 (SCV-76: GET /passenger/me, GET /passenger/bookings returns empty list, real data Sprint 3); apps/identity/src/VietRide.Identity.Application/Features/Users/GetMe/GetMeResponseDto.cs (verbatim profile projection -- Id/Email/DisplayName/Phone/Role/OperatorId/Status/AvatarUrl) + GetMeQueryHandler.cs lines 23-31; apps/identity/src/VietRide.Identity.Api/Controllers/UsersController.cs (GET v1/users/me); ADR 0002 (thin gateway), ADR 0004 (envelope); apps/gateway/src/config/routes.ts |

## Dispatch order
1. **Task 10.0** (blocking baseline -- fixes the outbox seam). Serial.
2. **Task 10.1** (after 10.0). Serial.
3. **Task 10.2** (after 10.0 + 10.1). Serial.
4. **Task 10.3** -- parallel-safe (disjoint write set: only Shared.Web). May run anytime; default serial in one tree.
5. **Task 10.4** -- Q1 RESOLVED, no longer blocked. Parallel-safe (disjoint write set); may run anytime, default serial after the chain.

Parallel-safe: 10.3 is disjoint from the 10.0->10.1->10.2 chain. 10.4 is disjoint too and (Q1 RESOLVED) no longer gated. The chain 10.0->10.1->10.2 is strictly serial.

## Progress tracker
> Orchestrator bookkeeping -- main thread updates after each /implement-task (Step 3). Informational only -- NOT audit evidence. /audit-day re-verifies every task against the SOT.

| Task | Status | Review verdict | Date | Notes |
|---|---|---|---|---|
| 10.0 | done | APPROVE | 2026-06-10 | reconcile split IOutboxStore -- blocking; Q4 decided option (a), no migration; new test project VietRide.Shared.Persistence.UnitTests (human-approved) |
| 10.1 | done | APPROVE | 2026-06-10 | wire AddVietRideMessaging into Identity; boot verified, OutboxBackgroundService starts |
| 10.2 | done | APPROVE | 2026-06-10 | emit user.created/operator.approved/operator.suspended; transactional + rollback test green; prod outbox_event_status enum mapping verified |
| 10.3 | done | APPROVE | 2026-06-10 | Idempotency middleware placeholder (not wired); Q3 resolved; 3+1 unit-test branches green |
| 10.4 | done | APPROVE | 2026-06-10 | passenger stub endpoints + gateway route -- Q1 RESOLVED; /me reuses GetMeResponseDto, /bookings empty paginated envelope; 127 integration + 65 gateway tests green |

Legend: todo - in progress - done (reviewer APPROVED + human /verify) - done-with-carryover - blocked

## Open questions
1. **Q1 -- passenger stub response shapes. RESOLVED (human, 2026-06-09).** Ship both stubs now with minimal stable shapes. GET /v1/passenger/me REUSES the EXISTING GetMeResponseDto profile projection VERBATIM (apps/identity/src/VietRide.Identity.Application/Features/Users/GetMe/GetMeResponseDto.cs -- Id, Email, DisplayName, Phone, Role, OperatorId, Status, AvatarUrl; same mapping as GetMeQueryHandler.cs lines 23-31); no passenger-specific fields invented, no new DTO. GET /v1/passenger/bookings returns the empty paginated envelope { items: [], page: 1, pageSize: 20, total: 0 } inside the ApiResponse wrapper; only the pagination envelope is fixed, the booking ITEM schema is NOT defined here. API-contract + Postman entries added for both, each marked `stub -- item schema finalized in Sprint 3 (SCV-76 / Booking)`. Task 10.4 unblocked (status todo).
2. **Q2 -- staff.password_set event. RESOLVED (human, 2026-06-09): DROP ENTIRELY from Day 10 / v1.** EXCLUDED from Day 10 and v1 -- NO BSOT 7.3 registry row, NO emit -- because there is NO designed consumer and Identity already sends the password-setup email directly. Per conflict order (BSOT 7.3 registry > timeline) the registry omission is authoritative. Task 10.2 emits ONLY the three registered events (identity.user.created, identity.operator.approved, identity.operator.suspended). Revisit ONLY when a real consumer exists -- that would first require a BSOT 7.3 registry row (key + payload + consumer owner) + version bump before any handler emits it.
3. **Q3 -- IDEMPOTENCY_KEY_MISMATCH in error registry. RESOLVED.** Verified PRESENT in BSOT 5.9 at BACKEND_SOURCE_OF_TRUTH.md:1401 (IDEMPOTENCY_KEY_MISMATCH | 422 | Same key, different body). Code exists -- NO BSOT edit and NO version bump needed; Task 10.3 just references it.
4. **Q4 -- Outbox backoff column + interface consolidation. DECIDED = option (a) + consolidation specifics nailed (reviewer-confirmed, no human input needed).** OutboxEvent (libs/dotnet/VietRide.Shared.Persistence/Outbox/OutboxEvent.cs) has NO NextAttemptAt/due-time field or column. Task 10.0 takes option (a): poll-cadence retry (every not-yet-exhausted FAILED row re-fetched each tick — NO per-row backoff delay; `ComputeBackoff` is not persisted/gating), BOUNDED by `FetchPendingAsync` excluding `RetryCount > MaxRetryCount` so the DoD "retry <= MaxRetryCount" is verifiable, NO migration, NO schema change on either DB. Three consolidation specifics now pinned in Task 10.0 acceptance so the worker cannot guess: (i) the single unified `IOutboxStore` lives in **VietRide.Shared.Persistence.Outbox** (verified ref direction Messaging->Persistence -- the Messaging stub interface is DELETED, the worker resolves the Persistence interface through the existing ProjectReference, NO new/reverse ref). Because `FetchPendingAsync` returns `OutboxEventEnvelope`, that DTO is **MOVED from Messaging into Persistence** (else Persistence would need a ->Messaging ref and cycle); (ii) **`GetUnprocessedAsync` is REMOVED** (no caller exists repo-wide) -- the four members are AddAsync/FetchPendingAsync/MarkPublishedAsync/MarkFailedAsync (worker uses FetchPendingAsync, handlers use AddAsync); (iii) **`OutboxEventEnvelope.NextAttemptAt` is kept on the DTO but projected as `null`** from OutboxEvent (forward-compat for deferred option (b)); this is provably safe because DrainOnceAsync never reads NextAttemptAt (backoff = RetryCount via ComputeBackoff). MarkFailedAsync accepts but does NOT persist its nextAttemptAt param. The projection also converts CreatedAt/PublishedAt from DateTimeOffset to the envelope DateTime via .UtcDateTime. Option (b) [add next_attempt_at to BOTH Identity + Trip DBs] recorded as a DEFERRED future enhancement.

## Decisions to confirm (manager defaults applied -- human may override)
- **Q2 / staff.password_set** -- RATIFIED 2026-06-09: option (a), DROP ENTIRELY for v1 (no registry row, no emit; no designed consumer + Identity emails directly). Task 10.2 emits only the three registered events. See Open questions Q2 (RESOLVED) for the revisit path.
- **Q4 / Outbox due-time column** -- default applied: option (a), no migration. Option (b) (add next_attempt_at to Identity + Trip DBs) deferred; revisit if multi-worker FOR UPDATE SKIP LOCKED scheduling is needed post-Sprint-2.
- **B2 / Outbox enqueue boundary for Identity handlers (RESOLVED 2026-06-10 = B2-clean).** The canonical `IOutboxStore` lives in `VietRide.Shared.Persistence.Outbox` (per Task 10.0). Identity.Application today references Domain + Shared.Kernel + Shared.Application + Shared.Messaging, **NOT Shared.Persistence** (verified VietRide.Identity.Application.csproj). For a handler to enqueue an outbox row there are two options:
  - **Option B2-clean (RECOMMENDED):** define a thin application-facing abstraction `IIntegrationEventOutbox` (e.g. `EnqueueAsync(IIntegrationEvent evt, CancellationToken ct)`) in **Shared.Application** (already referenced by every service's Application), implemented in **Shared.Persistence** by wrapping `OutboxStore.AddAsync` (serialize event -> build `OutboxEvent` with EventType=routing key). Application handlers depend only on the abstraction + the event contract; they never see the `OutboxEvent` EF entity or `IOutboxStore`. This keeps the Clean-Architecture boundary clean. **If chosen, Task 10.0 scope grows to define+implement this abstraction (Shared.Application + Shared.Persistence are already in 10.0's owned set), and 10.2 owned-files add NO Shared.Persistence reference.**
  - **Option B2-compromise:** add a direct `Application -> Shared.Persistence` ProjectReference and call `IOutboxStore.AddAsync(new OutboxEvent{...})` from handlers. It COMPILES and PASSES NetArchTest (the layering test only blocks `VietRide.Identity.Infrastructure`/`.Api`, LayeringTests.cs:79), but Application then knows a persistence entity + store — a boundary smell. Only pick this for speed, and record it as an intentional compromise.
  **RESOLVED (human, 2026-06-10) = B2-clean.** Task 10.0 defines+implements the abstraction; Task 10.2 handlers depend ONLY on it. To guarantee NO project-reference cycle, the abstraction is STRING-based (does NOT take a Shared.Messaging type — note Messaging already references Shared.Application, so an Application->Messaging edge would cycle): `IIntegrationEventOutbox.EnqueueAsync(string eventType, string payloadJson, CancellationToken ct)` in **Shared.Application**, implemented in **Shared.Persistence** (builds an `OutboxEvent{ EventType=eventType, Payload=payloadJson }` and calls the existing `OutboxStore.AddAsync` inside the ambient transaction). Identity.Application handlers build the routing-key string + `JsonSerializer.Serialize(payloadRecord)` themselves (payload records are plain records under Identity.Application/Events — no Shared.Messaging ref needed for enqueue) and call the abstraction. Application gains NO Shared.Persistence reference. No longer blocked.
