# Day 8 — Plan

> Produced by `manager`. Gated by `reviewer` (PLAN-REVIEW) before any worker runs.

- **Timeline ref**: BE_TIMELINE_VU.md → Day 8 (Wed 2026-06-03) — Trip-Route-Vehicle: Route + RouteStop + AlternativeRoute (`BE_TIMELINE_VU.md:98-105`). No Jira key listed on the Day-8 line.
- **Prior checklist**: docs/handoff/day-7-checklist.md (found — READY; no blocker; Route/RouteStop may build on Station/Stop, reuse internal lookup + operator-write-eligibility patterns, keep inherited shared outbox as `outbox_events`).
- **Plan status**: DRAFT → (reviewer) APPROVED / REVISION-REQUIRED — PATCHED after PLAN-REVIEW (added Task 8.G Gateway routes; baked in verified defaults for error codes, AlternativeRoute deactivate semantics, RouteStop hard-delete, fare floor-to-1000, fare-template stopId rule).

## Objective
Deliver the Trip Route layer on top of Day-7 Station/Stop: Route CRUD (with `returnRouteId` self-reference), RouteStop add/remove with `orderIndex`/`allowPickup`/`allowDropoff`, future-dated `RouteStopFareTemplate`, and AlternativeRoute CRUD (max 2 per main route). This is the configuration backbone that Day-9 Vehicle/DriverSchedule and Day-11 Trip search/generation consume. It introduces the second Trip migration (4 new tables + 1 junction) and the first Trip operator-write CRUD beyond Stop.

## Success criteria (DoD — binary, verifiable)
- [ ] EF migration creates `routes`, `route_stops`, `route_stop_fare_templates`, `alternative_routes`, `alternative_route_stops` tables matching `db-schema/trip-route-vehicle/schema.sql:149-258`; applies on empty DB, reverts to `0`, re-applies (`BE_TIMELINE_VU.md:99`).
- [ ] Operator can create a Route with main intermediate stops + a future-dated fare template; flags enforced (cannot add a stop with allowPickup=false AND allowDropoff=false -> `422 ROUTE_STOP_FLAGS_INVALID` with `error.fields`) (`BE_TIMELINE_VU.md:104`; schema CHECK `chk_route_stops_allow_at_least_one`).
- [ ] Route create validates originStationId/destinationStationId EXIST (app-layer lookup -> 404 STATION_NOT_FOUND, not a raw 500 FK violation), that the caller operator has an ACTIVE OperatorStation link for BOTH (`SU26SE101_VIETRIDE_technical_context_v7.md:3540`; no link -> 422 VALIDATION_ERROR), and origin != destination; `returnRouteId` self-reference accepted (`BE_TIMELINE_VU.md:100`; `SU26SE101_VIETRIDE_technical_context_v7.md:157,550`; schema `chk_routes_origin_dest_different`; schema:153-154 FK ON DELETE RESTRICT).
- [ ] RouteStop add/remove with `orderIndex`, `allowPickup`, `allowDropoff`; adding a stop with an order-index conflict on the same route returns `422 ROUTE_STOP_ORDER_CONFLICT` with `error.fields` discriminator; DELETE hard-removes the junction row (`BE_TIMELINE_VU.md:101,105`; schema `uq_route_stops_route_order`; schema 178-199 has no `deleted_at`).
- [ ] AlternativeRoute CRUD enforces max 2 active per main route at API layer (3rd create -> `422 ALTERNATIVE_ROUTE_LIMIT_EXCEEDED`); delete = deactivate via `is_active=false` (no hard row delete; `alternative_routes` has no `deleted_at`) (`BE_TIMELINE_VU.md:103`; `SU26SE101_VIETRIDE_technical_context_v7.md:2361-2364`; schema 227-238).
- [ ] All write endpoints tenant-scoped to caller operator; cross-operator Route returns `404 ROUTE_NOT_FOUND`; non-APPROVED/inactive operator write returns `403`.
- [ ] Gateway forwards `/v1/operator/routes` + `/v1/operator/alternative-routes` to `TRIP_BASE_URL` with user auth + operator-role union (mirrors `/v1/operator/stops`); Gateway TS build/lint/test green (`apps/gateway/src/config/routes.ts:126-131`).
- [ ] `dotnet build`/`dotnet format --verify-no-changes`/`dotnet test` (incl. NetArchTest layering) green for Trip + shared libs; Gateway TS build/lint/test green.

## Contract changes
New REST endpoints under Trip. The Day-8 endpoints are NOT yet in `VietRide_API_Contract_v1.md` (verified — no Route/RouteStop/AlternativeRoute section exists). Contract authored in Task 8.0a.

- Route: `POST /v1/operator/routes`, `GET /v1/operator/routes`, `GET /v1/operator/routes/{id}`, `PATCH /v1/operator/routes/{id}`.
- RouteStop: `POST /v1/operator/routes/{id}/stops` (add), `DELETE /v1/operator/routes/{id}/stops/{stopId}` (remove — hard-delete junction row).
- RouteStopFareTemplate: `POST /v1/operator/routes/{id}/fare-templates`, `GET /v1/operator/routes/{id}/fare-templates`.
- AlternativeRoute: `POST /v1/operator/routes/{id}/alternative-routes`, `GET /v1/operator/routes/{id}/alternative-routes`, `PATCH /v1/operator/alternative-routes/{altId}`, `DELETE /v1/operator/alternative-routes/{altId}` (DELETE = deactivate `is_active=false`).
- **Per-method roles (DECISION — mirror Day-7 `OperatorStopsController`):** the Gateway entry carries the union `[OPERATOR_ADMIN, OPERATOR_STAFF]` (proxy only), but each Trip controller enforces method-level roles EXACTLY like Day-7 (`apps/trip/src/VietRide.Trip.Api/Controllers/OperatorStopsController.cs:15-16`): **WRITE (POST/PATCH/DELETE) = `OPERATOR_ADMIN` only**; **READ (GET list/by-id) = `OPERATOR_STAFF` + `OPERATOR_ADMIN`**. Applies to all Day-8 controllers (Route, RouteStop, FareTemplate, AlternativeRoute). Document this matrix in the 8.0a contract; enforce via `[Authorize(Roles = ...)]` in Tasks 8.1-8.4. Override only if the human wants STAFF to have write access (re-opens 8.0a + the controller attributes).
- **Error codes (was OQ1) — RESOLVED = dedicated codes.** Three NEW BSOT §5.9 rows (all 422), added by Task 8.0a: `ROUTE_STOP_ORDER_CONFLICT` (order-index conflict, Task 8.2), `ROUTE_STOP_FLAGS_INVALID` (allowPickup AND allowDropoff both false, Task 8.2), `ALTERNATIVE_ROUTE_LIMIT_EXCEEDED` (3rd active AlternativeRoute, Task 8.4); each still carries an `error.fields` discriminator. The remaining Day-8 validations (OperatorStation-not-active on route create, fare-template overlap, stopId-not-on-route, effective-window order) stay generic `VALIDATION_ERROR`. Already-registered codes also used: `ROUTE_NOT_FOUND` (404), `ROUTE_RETURN_NOT_CONFIGURED` (422), `STATION_NOT_FOUND` (404), `STOP_NOT_FOUND` (404), `VALIDATION_ERROR` (422), `FORBIDDEN` (403) (`BACKEND_SOURCE_OF_TRUTH.md:1373-1382`). NOTE: the registry's `STOP_NOT_PICKUP_ALLOWED`/`STOP_NOT_DROPOFF_ALLOWED` (422, `BACKEND_SOURCE_OF_TRUTH.md:1377-1378`) are **booking-time** codes (passenger picks a stop whose flag is false) and are NOT the Day-8 **config-time** "both flags false on add" validation — that uses the dedicated `ROUTE_STOP_FLAGS_INVALID` (422). See Decision #1 (RESOLVED = dedicated codes).
- Events: none emitted by Day-8 handlers (Outbox wiring is Day 10). `RouteCreatedEvent` in technical_context_v7:160 is illustrative pseudo-code, NOT a Day-8 deliverable — do NOT add an integration event.
- Gateway: NEW — add `/v1/operator/routes` + `/v1/operator/alternative-routes` entries (Task 8.G). These out-prefix the existing generic `/v1/routes` (`apps/gateway/src/config/routes.ts:112`) because `matchRoute` is longest-prefix-wins (`routes.ts:187-191`) and the new prefixes start with `/v1/operator/...`, not `/v1/routes`.
- Migration: second Trip migration; keep inherited shared outbox table as `outbox_events`.

## Tasks

### Task 8.0a — Contract + BSOT registry sync for Day-8 Route endpoints (DO FIRST)
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer |
| skill | (none) |
| owned files (write set) | `VietRide_API_Contract_v1.md` (append Day-8 Route/RouteStop/FareTemplate/AlternativeRoute section), `BACKEND_SOURCE_OF_TRUTH.md` (ADD three §5.9 error-registry rows: `ROUTE_STOP_ORDER_CONFLICT` 422, `ROUTE_STOP_FLAGS_INVALID` 422, `ALTERNATIVE_ROUTE_LIMIT_EXCEEDED` 422, in the Stop/Route group near :1373-1380; + §13 changelog row; + version bump per §13 rules) |
| forbidden scope | any code (`apps/**`, `libs/**`), `db-schema/**` (canonical DDL already correct — do not edit), `.env`/secrets, git ops. Do NOT invent columns/enums — derive request/response field names from `db-schema/trip-route-vehicle/schema.sql` columns rendered camelCase, mirroring the Day-7 contract style. Add ONLY the three §5.9 codes named above — no other new error codes. |
| depends on | none |
| invariant flags | LF/.md · ApiResponse envelope (ADR 0004) · BSOT §5.6 (Idempotency-Key) / §5.7 (pagination) / §5.8 (search) conventions · three dedicated 422 codes (ROUTE_STOP_ORDER_CONFLICT / ROUTE_STOP_FLAGS_INVALID / ALTERNATIVE_ROUTE_LIMIT_EXCEEDED) for those specific failures; other validations stay `VALIDATION_ERROR` + `error.fields` |
| acceptance | New contract section documents all endpoints above with auth roles, request/response shapes in the ADR 0004 envelope; tenant-isolation `404 ROUTE_NOT_FOUND` rule; the three validation failures documented with their DEDICATED codes (order-index conflict -> `ROUTE_STOP_ORDER_CONFLICT` 422; allowPickup-AND-allowDropoff both false -> `ROUTE_STOP_FLAGS_INVALID` 422; 3rd-active AlternativeRoute -> `ALTERNATIVE_ROUTE_LIMIT_EXCEEDED` 422), each with `error.fields` discriminator; RouteStop DELETE documented as hard-delete; AlternativeRoute DELETE documented as deactivate (`is_active=false`); fare-template `fareFromThisStop` documented as VND BIGINT floored to 1000; fare-template effective-window OVERLAP documented as rejected -> 422 VALIDATION_ERROR (per README:48); Route-create documents the OperatorStation-active precondition for origin+destination (-> 422 VALIDATION_ERROR if the operator has no active link, per technical_context_v7:3540); the per-method role matrix documented (WRITE = OPERATOR_ADMIN only, READ = OPERATOR_ADMIN+OPERATOR_STAFF, mirroring Day-7); the three new §5.9 codes added to BSOT with a §13 changelog row + version bump; markdown renders; no new column/enum invented beyond schema |
| source citations | `db-schema/trip-route-vehicle/schema.sql:149-258`; `BACKEND_SOURCE_OF_TRUTH.md:1373-1382`; `VietRide_API_Contract_v1.md:2010-2214`; `BE_TIMELINE_VU.md:98-105`; `SU26SE101_VIETRIDE_technical_context_v7.md:550,1850-1897,2361-2364`; `apps/gateway/src/config/routes.ts:126-131` |

### Task 8.G — Gateway routes for /v1/operator/routes + /v1/operator/alternative-routes
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer (or /code-review) |
| skill | (none) |
| owned files (write set) | `apps/gateway/src/config/routes.ts` (append two ProxyRoute entries in the Trip/Vehicle block), Gateway unit test for the new routes (mirror existing route-table test if present, e.g. `apps/gateway/src/config/*.spec.ts`) |
| forbidden scope | any other route entry (do NOT modify the existing `/v1/routes`, `/v1/operator/stops`, or any other prefix), code under `apps/trip/**` / `libs/**`, `.env`/secrets, `db-schema/**`, contract/BSOT docs, git ops |
| depends on | 8.0a |
| invariant flags | LF/.ts · no new dependency · mirror the Day-7 `/v1/operator/stops` entry exactly (`routes.ts:126-131`): target = `env.TRIP_BASE_URL`, authRequired = `user`, requiredRoles = OPERATOR_ADMIN + OPERATOR_STAFF, no `stripPrefix`/`rewriteTo` · finer per-method roles are enforced by the Trip controllers, NOT the Gateway |
| acceptance | `routes.ts` has two new entries — one for prefix `/v1/operator/routes` and one for `/v1/operator/alternative-routes`, each with target `env.TRIP_BASE_URL`, `authRequired: user`, `requiredRoles: [OPERATOR_ADMIN, OPERATOR_STAFF]` — placed in the Trip/Vehicle block; `matchRoute` on a `/v1/operator/routes/...` path resolves to the new entry (longest-prefix-wins over `/v1/routes`, verified `routes.ts:187-191`); Gateway `nx build`/`lint`/`test` green; no other prefix changed |
| source citations | `apps/gateway/src/config/routes.ts:110-140` (Trip/Vehicle block), `routes.ts:126-131` (`/v1/operator/stops` template), `routes.ts:187-191` (`matchRoute` longest-prefix-wins); Day-7 Task 7.5 gateway pattern (`docs/handoff/day-7-plan.md`) |

### Task 8.0 — Domain entities + EF config + second Trip migration (Route, RouteStop, RouteStopFareTemplate, AlternativeRoute, AlternativeRouteStop)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | scaffold-aggregate (Route as aggregate root) + ef-migration |
| owned files (write set) | `apps/trip/src/VietRide.Trip.Domain/Entities/Route.cs`, `.../RouteStop.cs`, `.../RouteStopFareTemplate.cs`, `.../AlternativeRoute.cs`, `.../AlternativeRouteStop.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/Persistence/Configurations/RouteConfiguration.cs`, `.../RouteStopConfiguration.cs`, `.../RouteStopFareTemplateConfiguration.cs`, `.../AlternativeRouteConfiguration.cs`, `.../AlternativeRouteStopConfiguration.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/TripDbContext.cs` (add DbSets only), `apps/trip/src/VietRide.Trip.Infrastructure/Migrations/*` (new migration + regenerated snapshot), `apps/trip/tests/VietRide.Trip.UnitTests/**` (entity/config/model tests) |
| forbidden scope | any other service, `.env`/secrets, existing Day-7 entities/configs (Station/OperatorStation/Stop), shared libs, `db-schema/**`, `VietRide_API_Contract_v1.md`, `BACKEND_SOURCE_OF_TRUTH.md`, `apps/gateway/**`, git ops. Do NOT add Application/Api code in this task. Do NOT reintroduce `outbox_messages`. |
| depends on | 8.0a |
| invariant flags | CRLF/.cs · CPM no `Version=` · no cross-DB FK (`operator_id` plain UUID, no REFERENCES) · soft-delete via `deleted_at` only — present on Route ONLY; RouteStop/RouteStopFareTemplate/AlternativeRoute/AlternativeRouteStop have NO `deleted_at` per schema · IActivatable only where `is_active` exists (Route AND AlternativeRoute — both have `is_active`) · Money BIGINT VND for `base_fare`/`fare_from_this_stop` · keep outbox table `outbox_events` |
| acceptance | build clean; `dotnet format --verify-no-changes` clean; `dotnet test` green incl. NetArchTest; migration Up() creates the 5 tables with snake_case columns, composite PKs (`route_stops (route_id, stop_id)`, `alternative_route_stops (alternative_route_id, stop_id)`), unique indexes (`uq_route_stops_route_order`, `uq_alternative_route_stops_route_order`), CHECKs (`chk_routes_origin_dest_different`, `chk_routes_base_fare_non_negative`, `chk_route_stops_allow_at_least_one`, `chk_route_stops_order_positive`, `chk_route_stop_fare_templates_fare_non_negative`, `chk_route_stop_fare_templates_effective_order`, `chk_alternative_route_stops_order_positive`); `AlternativeRoute` maps `is_active` and implements IActivatable; apply on empty temp DB -> update 0 -> re-apply passes; has-pending-model-changes reports none |
| source citations | `db-schema/trip-route-vehicle/schema.sql:149-258` (Route 149-173; RouteStop 178-199; FareTemplate 204-222; AlternativeRoute 227-238 — `is_active` line 235, no `deleted_at`; AlternativeRouteStop 245-258); `db-schema/trip-route-vehicle/README.md:21-25,44-48,70-72`; `BE_TIMELINE_VU.md:99-100`; `apps/trip/src/VietRide.Trip.Infrastructure/TripDbContext.cs:18-22`; `apps/trip/src/VietRide.Trip.Infrastructure/Persistence/Configurations/StopConfiguration.cs`; AGENTS.md Persistence + Naming |

### Task 8.1 — Route CRUD (create/list/get/update) with returnRouteId + tenant scope
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | `apps/trip/src/VietRide.Trip.Application/Features/Routes/*.cs`, `apps/trip/src/VietRide.Trip.Application/Abstractions/Repositories/IRouteRepository.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/Persistence/Repositories/RouteRepository.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs` (register repo only), `apps/trip/src/VietRide.Trip.Api/Controllers/OperatorRoutesController.cs`, `apps/trip/src/VietRide.Trip.Api/Controllers/Requests/*Route*.cs`, Trip unit + integration tests for Route |
| forbidden scope | other services, `.env`/secrets, Day-7 features (Stations/Stops), shared libs, `db-schema/**`, `VietRide_API_Contract_v1.md`, `BACKEND_SOURCE_OF_TRUTH.md`, migration files, `apps/gateway/**`, git ops. Do NOT touch RouteStop/FareTemplate/AlternativeRoute handlers (8.2/8.3/8.4). |
| depends on | 8.0, 8.G |
| invariant flags | CRLF/.cs · MediatR v11 (controller calls MediatR.Send, never a service directly) · Clean Architecture direction (NetArchTest) · tenant isolation (filter by operatorId from JWT; cross-operator get/update -> 404 ROUTE_NOT_FOUND via CodedNotFoundException) · operator write-eligibility via Identity internal client (reuse Day-7 StopWriteEligibilityGuard: 403 -> FORBIDDEN, other -> VALIDATION_ERROR) · originStationId/destinationStationId validation — TWO app-layer checks, in order: (1) the Station MUST EXIST (reuse the Day-7 internal Station lookup) -> else 404 STATION_NOT_FOUND; (2) **the caller operator MUST have an ACTIVE `OperatorStation` link (`OperatorStation.isActive = true`) for BOTH origin AND destination station BEFORE the route can be created** — this is a CANONICAL business rule (`SU26SE101_VIETRIDE_technical_context_v7.md:3540`: "operator phải có OperatorStation.isActive = true cho cả origin/destination station trước khi tạo route"). Missing/inactive OperatorStation link -> 422 VALIDATION_ERROR with `error.fields` on `originStationId`/`destinationStationId` (operator has no right to operate that station). Both checks run BEFORE the origin==destination check. The DB FK `origin/destination_station_id REFERENCES stations(id) ON DELETE RESTRICT` (schema:153-154) enforces only raw existence (a violation surfaces as 500) — the clean 404/422 MUST come from these app-layer checks. Stations are canonical/shared (Day-7), NOT operator-scoped — so check (1) is existence-only, while check (2) is the operator-eligibility gate via OperatorStation · returnRouteId validation (when present, target Route MUST also be NOT soft-deleted / active in addition to exist + same-operator) (DECISION, was unvalidated): when present, target Route MUST (1) EXIST and (2) belong to the SAME caller operator -> else 404 ROUTE_NOT_FOUND (reuse the same tenant-isolation lookup as get/update). DB FK `return_route_id REFERENCES routes(id) ON DELETE SET NULL` (schema:155) enforces only EXISTENCE within the Trip DB, NOT same-operator — so the same-operator check is app-layer. Link is ONE-WAY: setting A.returnRouteId=B does NOT auto-set B.returnRouteId=A (no reciprocal write) · Money BIGINT for baseFare (floor-to-1000 via Money.FromRaw) · ApiResponse envelope · pagination BSOT 5.7 |
| acceptance | POST /v1/operator/routes creates Route (originStationId or destinationStationId not found -> 404 STATION_NOT_FOUND; caller operator has NO active OperatorStation link for origin OR destination -> 422 VALIDATION_ERROR with error.fields (SU26SE101_VIETRIDE_technical_context_v7.md:3540); both checks BEFORE origin==destination; origin==destination -> 422; returnRouteId optional self-ref accepted only when target exists AND belongs to caller operator); a returnRouteId pointing at another operator's Route -> 404 ROUTE_NOT_FOUND (cross-operator returnRouteId test); a returnRouteId pointing at a non-existent Route -> 404 ROUTE_NOT_FOUND; one-way behavior noted (setting A.returnRouteId=B does NOT mutate B); GET list paginated + tenant-scoped; GET by id cross-operator -> 404 ROUTE_NOT_FOUND; PATCH by id updates mutable fields, cross-operator -> 404; non-APPROVED operator write -> 403; build/format/test green incl. one happy + one error unit test per endpoint and a tenant-isolation integration test; OpenAPI annotations present |
| source citations | `db-schema/trip-route-vehicle/schema.sql:149-173` (return_route_id FK schema:155 — existence-only, no operator scope); `SU26SE101_VIETRIDE_technical_context_v7.md:157,550,3540` (3540: operator must have active OperatorStation for origin+destination before route create); `db-schema/trip-route-vehicle/README.md:44` (returnRouteId one-way, no DB 2-way enforcement); `BACKEND_SOURCE_OF_TRUTH.md:1379`; `apps/trip/src/VietRide.Trip.Api/Controllers/OperatorStopsController.cs`; `apps/trip/src/VietRide.Trip.Application/Features/Stops/StopWriteEligibilityGuard.cs`; `VietRide_API_Contract_v1.md` Day-8 section (from 8.0a) |

### Task 8.2 — RouteStop add/remove with orderIndex + allowPickup/allowDropoff
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | `apps/trip/src/VietRide.Trip.Application/Features/RouteStops/*.cs`, `apps/trip/src/VietRide.Trip.Application/Abstractions/Repositories/IRouteStopRepository.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/Persistence/Repositories/RouteStopRepository.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs` (register repo only — serial writer; see Dispatch order), `apps/trip/src/VietRide.Trip.Api/Controllers/OperatorRoutesController.cs` (add stop sub-routes — serial writer), `apps/trip/src/VietRide.Trip.Api/Controllers/Requests/*RouteStop*.cs`, Trip unit + integration tests for RouteStop |
| forbidden scope | other services, `.env`/secrets, Day-7 features, shared libs, `db-schema/**`, contract/BSOT docs, migration files, `apps/gateway/**`, git ops. Do NOT edit Route create/update handlers (8.1). |
| depends on | 8.0, 8.1 |
| invariant flags | CRLF/.cs · MediatR v11 · tenant isolation (parent Route belongs to caller operator -> else 404 ROUTE_NOT_FOUND) · stopId must belong to caller operator -> else 404 STOP_NOT_FOUND · enforce allowPickup-OR-allowDropoff (both false -> 422 `ROUTE_STOP_FLAGS_INVALID`, fields discriminator on allowPickup) · enforce unique route_id+order_index at app layer -> order conflict 422 `ROUTE_STOP_ORDER_CONFLICT`, fields discriminator on orderIndex (dedicated codes per Decision #1 RESOLVED; codes registered in 8.0a) · DELETE = hard-delete the junction row (no deleted_at on route_stops; no booking-impact check — Trips/Bookings do not exist yet) · operator write-eligibility guard |
| acceptance | POST /v1/operator/routes/id/stops adds RouteStop; both-flags-false -> 422 `ROUTE_STOP_FLAGS_INVALID` with error.fields; duplicate orderIndex on same route -> 422 `ROUTE_STOP_ORDER_CONFLICT` with error.fields; stopId of another operator -> 404 STOP_NOT_FOUND; DELETE /v1/operator/routes/id/stops/stopId hard-removes the junction row; cross-operator route -> 404; build/format/test green incl. order-conflict 422 test (`BE_TIMELINE_VU.md:105`) |
| source citations | `db-schema/trip-route-vehicle/schema.sql:178-199` (no deleted_at); `SU26SE101_VIETRIDE_technical_context_v7.md:1850-1897`; `BE_TIMELINE_VU.md:101,105`; `BACKEND_SOURCE_OF_TRUTH.md:1373,1379` |

### Task 8.3 — RouteStopFareTemplate with effectiveFrom/effectiveUntil (future-dated pricing)
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | `apps/trip/src/VietRide.Trip.Application/Features/RouteStopFareTemplates/*.cs`, `apps/trip/src/VietRide.Trip.Application/Abstractions/Repositories/IRouteStopFareTemplateRepository.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/Persistence/Repositories/RouteStopFareTemplateRepository.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs` (register repo only — serial writer), `apps/trip/src/VietRide.Trip.Api/Controllers/OperatorRoutesController.cs` (fare-template sub-routes — serial writer), `apps/trip/src/VietRide.Trip.Api/Controllers/Requests/*FareTemplate*.cs`, Trip unit + integration tests for fare templates |
| forbidden scope | other services, `.env`/secrets, Day-7 features, shared libs, `db-schema/**`, contract/BSOT docs, migration files, `apps/gateway/**`, git ops. Do NOT edit Route/RouteStop/AlternativeRoute handlers. |
| depends on | 8.0, 8.1 |
| invariant flags | CRLF/.cs · MediatR v11 · tenant isolation (parent Route belongs to caller operator -> else 404) · stopId belongs to caller operator -> else 404 · stopId MUST already be a RouteStop on the SAME route -> else 422 VALIDATION_ERROR (fields discriminator on stopId); fare templates are per-stop overrides of Route.baseFare so the stop must be on the route (schema binds stop_id and route_id via separate FKs but the business rule binds them — flag for dotnet-reviewer to confirm against technical_context_v7) · Money BIGINT for fareFromThisStop — floor-to-1000 via Money.FromRaw (baked default, was OQ2) · effectiveUntil greater than effectiveFrom when present (422) · effective-window overlap (DECISION CORRECTED — was "latest-wins/allow", now REJECT): a new template whose `[effectiveFrom, effectiveUntil)` window INTERSECTS an existing (non-deleted) template for the SAME `(route_id, stop_id)` MUST be REJECTED -> 422 VALIDATION_ERROR with `error.fields` on `effectiveFrom`/`effectiveUntil`. This is a CANONICAL requirement: `db-schema/trip-route-vehicle/README.md:48` states "Validation overlap chỗ effective windows enforced app-layer". The schema index `(route_id, stop_id, effective_from)` (schema:218-219) is a PLAIN non-unique CREATE INDEX so the DB does NOT enforce this — the overlap check MUST be done app-layer (open-ended `effectiveUntil = NULL` counts as extending to +infinity for the intersection test). Fare lookup (selecting the template with the greatest effectiveFrom <= the query instant) is Day-11+ scope — do NOT implement the lookup query here · operator write-eligibility guard |
| acceptance | POST /v1/operator/routes/id/fare-templates creates a template; effectiveUntil <= effectiveFrom -> 422; stopId not a RouteStop on this route -> 422 (fields on stopId); fareFromThisStop floored to 1000 before persist; future-dated effectiveFrom accepted; a new template whose window OVERLAPS an existing one for the same (route_id, stop_id) -> 422 VALIDATION_ERROR with error.fields (overlap-REJECTED test; open-ended effectiveUntil=NULL treated as +infinity); two NON-overlapping windows for the same (route_id, stop_id) both accepted; GET lists templates for the route tenant-scoped; cross-operator route -> 404; build/format/test green incl. effective-window validation test, the overlap-REJECTED test, a non-overlapping-accepted test, and stopId-not-on-route test |
| source citations | `db-schema/trip-route-vehicle/schema.sql:204-222` (FareTemplate); schema:218-219 (plain non-unique index `(route_id, stop_id, effective_from)` — does NOT enforce uniqueness, so overlap MUST be checked app-layer); `db-schema/trip-route-vehicle/README.md:23,48 (overlap validation enforced app-layer),72`; `SU26SE101_VIETRIDE_technical_context_v7.md:1870-1874`; `BE_TIMELINE_VU.md:102`; AGENTS.md Money rule |

### Task 8.4 — AlternativeRoute CRUD (max 2 active per main route, app-validated; DELETE = deactivate) + AlternativeRouteStop sequence
| Field | Value |
|---|---|
| stack/owner | dotnet |
| implement agent | dotnet-worker |
| review agent | dotnet-reviewer |
| skill | add-endpoint |
| owned files (write set) | `apps/trip/src/VietRide.Trip.Application/Features/AlternativeRoutes/*.cs` (Create/List/Update/Deactivate; include AlternativeRouteStop sequence in payload), `apps/trip/src/VietRide.Trip.Application/Abstractions/Repositories/IAlternativeRouteRepository.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/Persistence/Repositories/AlternativeRouteRepository.cs`, `apps/trip/src/VietRide.Trip.Infrastructure/DependencyInjection/InfrastructureServiceCollectionExtensions.cs` (register repo only — serial writer), `apps/trip/src/VietRide.Trip.Api/Controllers/OperatorAlternativeRoutesController.cs` + alt-route sub-routes under OperatorRoutesController (serial writer), `apps/trip/src/VietRide.Trip.Api/Controllers/Requests/*Alternative*.cs`, Trip unit + integration tests for AlternativeRoute |
| forbidden scope | other services, `.env`/secrets, Day-7 features, shared libs, `db-schema/**`, contract/BSOT docs, migration files, `apps/gateway/**`, git ops. Do NOT edit Route/RouteStop/FareTemplate handlers. |
| depends on | 8.0, 8.1 |
| invariant flags | CRLF/.cs · MediatR v11 · tenant isolation (parent Route belongs to caller operator -> else 404) · max 2 active (is_active=true) AlternativeRoute per Route enforced at app layer — count only active rows; 3rd active create -> 422 `ALTERNATIVE_ROUTE_LIMIT_EXCEEDED` (fields discriminator on alternativeRoutes; dedicated code per Decision #1 RESOLVED) · (the separate AlternativeRouteStop duplicate-orderIndex stays generic 422 VALIDATION_ERROR — only the max-2 cap gets a dedicated code) · DELETE = deactivate (is_active=false) via IActivatable, NOT a hard row delete (alternative_routes has no deleted_at); deactivating frees a slot toward the max-2 cap · destinationStationId validated present (reuse internal Station lookup -> 404 STATION_NOT_FOUND if missing) · AlternativeRouteStop has its OWN stop sequence with unique orderIndex (NOT reused RouteStop) -> conflict 422 · operator write-eligibility guard |
| acceptance | POST /v1/operator/routes/id/alternative-routes creates an AlternativeRoute with its own stop sequence; 3rd ACTIVE create on same route -> 422 `ALTERNATIVE_ROUTE_LIMIT_EXCEEDED` (error.fields); after deactivating one, a new create succeeds; GET lists; PATCH tenant-scoped (cross-operator -> 404); DELETE /v1/operator/alternative-routes/altId sets is_active=false (row still present in DB) and returns success; duplicate orderIndex within an alternative route -> 422; build/format/test green incl. max-2-active enforcement test and DELETE-deactivates (not hard-delete) test |
| source citations | `db-schema/trip-route-vehicle/schema.sql:227-258` (AlternativeRoute 227-238 — is_active line 235, no deleted_at; AlternativeRouteStop 245-258); `db-schema/trip-route-vehicle/README.md:24-25`; `SU26SE101_VIETRIDE_technical_context_v7.md:2358-2364`; `BE_TIMELINE_VU.md:103` |

### Task 8.5 — Postman collection: Day-8 Route/RouteStop/AlternativeRoute flow
| Field | Value |
|---|---|
| stack/owner | cross-cutting |
| implement agent | worker |
| review agent | reviewer (or /code-review) |
| skill | (none) |
| owned files (write set) | `docs/api/postman/vietride.postman_collection.json` (append Day-8 folder), `docs/api/postman/README.md`, `scripts/run-day8-newman-local.js` (mirror `scripts/run-day7-newman-local.js`), `package.json` (add postman:day8:local script only) |
| forbidden scope | code under `apps/**`/`libs/**`, `.env`/secrets, `db-schema/**`, contract/BSOT docs, git ops. Do NOT modify the Day-7 Postman folder or its runner. |
| depends on | 8.G, 8.1, 8.2, 8.3, 8.4 |
| invariant flags | LF/.json/.js/.md · tokens redacted/minted in-process (never recorded) · all calls go through Gateway port 3000 (FE never calls Trip directly) — exercises the new /v1/operator/routes + /v1/operator/alternative-routes Gateway entries from 8.G |
| acceptance | Newman folder covers: route create (origin==destination -> 422, non-existent originStationId/destinationStationId -> 404 STATION_NOT_FOUND), route list/get, RouteStop add + order-conflict 422 + both-flags-false 422, fare-template create + bad effective window 422 + overlapping-window 422 + stopId-not-on-route 422, alternative-route create + 3rd-active -> 422 + DELETE deactivates (subsequent create succeeds), cross-operator route -> 404, non-APPROVED operator write -> 403; node scripts/run-day8-newman-local.js runs green against the local stack; JSON parses |
| source citations | `scripts/run-day7-newman-local.js`; `docs/api/postman/README.md`; `BE_TIMELINE_VU.md:105`; package.json postman:day7:local |

## Dispatch order
1. **Task 8.0a** (worker) — contract/BSOT sync; fixes the field/error surface. Parallel-safe: yes (docs only).
2. **Task 8.G** (worker) — Gateway routes. Depends on 8.0a. Parallel-safe: yes (only edits `apps/gateway/src/config/routes.ts`, disjoint from all Trip code and from the 8.0a docs once 8.0a lands).
3. **Task 8.0** (dotnet-worker) — entities + EF config + migration. Depends on 8.0a. Parallel-safe with 8.G: yes (disjoint write sets). Not parallel-safe with later Trip tasks (touches TripDbContext + migration snapshot).
4. **Task 8.1** (dotnet-worker) — Route CRUD. Depends on 8.0, 8.G. Parallel-safe: no.
5. **Tasks 8.2, 8.3, 8.4** — depend on 8.1. NOT parallel-safe with each other: all three append to OperatorRoutesController.cs and InfrastructureServiceCollectionExtensions.cs (overlapping write set). Dispatch serially in the current tree (8.2 -> 8.3 -> 8.4); do not run concurrently. (If parallelism is wanted, split the controller into per-feature controllers first — flag before dispatch.)
6. **Task 8.5** (worker) — Postman. Depends on 8.G + 8.1-8.4. Parallel-safe: yes (disjoint from code once endpoints land).

## Progress tracker
> Orchestrator bookkeeping — informational only, NOT audit evidence. `/audit-day` re-verifies every task independently.

| Task | Status | Review verdict | Date | Notes |
|---|---|---|---|---|
| 8.0a | done | APPROVE | 2026-06-10 | Reviewer approved; human verify pending; 1 patch round. |
| 8.G | done | APPROVE | 2026-06-10 | Reviewer approved; human verify pending. |
| 8.0 | done | APPROVE | 2026-06-10 | Reviewer approved; human verify pending; 1 patch round. |
| 8.1 | todo | — | — | — |
| 8.2 | todo | — | — | — |
| 8.3 | todo | — | — | — |
| 8.4 | todo | — | — | — |
| 8.5 | todo | — | — | — |

Legend: todo / in progress / done (reviewer APPROVED + human /verify) / done-with-carryover / blocked

## Decisions to confirm with human
> Baked-in verified defaults the plan now runs on. Override any of these before dispatch and the affected task(s) must be re-issued.

1. **Error codes (was OQ1) — RESOLVED (human, 2026-06-10) = DEDICATED machine-readable codes.** The three Day-8 validation failures get their own UPPER_SNAKE_CASE codes (all HTTP 422), added as new BSOT §5.9 rows by Task 8.0a:
   - `ROUTE_STOP_ORDER_CONFLICT` (422) — RouteStop order-index conflict on the same route (Task 8.2).
   - `ROUTE_STOP_FLAGS_INVALID` (422) — RouteStop added with allowPickup=false AND allowDropoff=false (Task 8.2).
   - `ALTERNATIVE_ROUTE_LIMIT_EXCEEDED` (422) — 3rd ACTIVE AlternativeRoute on the same main route (Task 8.4).
   These three replace the generic `VALIDATION_ERROR` for those specific cases (8.2/8.4 emit `error.code` = the dedicated code; `error.fields` may still carry the field discriminator). **Scope-affecting:** Task 8.0a now ADDS three §5.9 registry rows (not just the §13 changelog row) + bumps the BSOT version. NOTE — the OTHER Day-8 validations (OperatorStation-not-active on route create, fare-template effective-window overlap, fare-template stopId-not-on-route, effectiveUntil<=effectiveFrom) STAY generic `VALIDATION_ERROR` + `error.fields`; only these three get dedicated codes.
2. **Fare-template floor-to-1000 (was OQ2) — DEFAULT: yes.** fareFromThisStop is Money VND, floored to 1000 via Money.FromRaw like all money per the AGENTS.md Money rule. Schema only checks >= 0. Override: store raw value (not recommended — breaks the VND invariant).
3. **RouteStop DELETE (was OQ3) — DEFAULT: hard-delete the junction row.** route_stops has no deleted_at and there is no Trip/Booking yet, so removal is an unconditional junction-row delete (no booking-impact check). The STOP_DISABLED replacement flow is explicitly Day-24 (`BE_TIMELINE_VU.md:253-258`). Override: if a soft-delete column is wanted, that requires a db-schema change first (out of Day-8 scope).
4. **AlternativeRoute DELETE — DEFAULT: deactivate (is_active=false), not hard-delete; max-2 cap counts only active rows.** alternative_routes has is_active (schema line 235) and NO deleted_at. Override: hard-delete (would need confirmation no Day-9+ entity references AlternativeRoute).
5. **Fare-template stopId must be a RouteStop on the same route (was NIT) — DEFAULT: enforce -> else 422.** Not explicitly stated in db-schema README; set as the safe default and flagged for dotnet-reviewer to confirm against `SU26SE101_VIETRIDE_technical_context_v7.md`. Override: allow fare templates for stops not on the route (looser; unlikely correct since a fare template overrides Route.baseFare at a stop).
6. **returnRouteId validation (Task 8.1) — DEFAULT: target must EXIST and belong to the SAME operator as the caller, else 404 ROUTE_NOT_FOUND; the link is ONE-WAY (no auto-reciprocal).** The DB FK `return_route_id REFERENCES routes(id) ON DELETE SET NULL` (schema:155) guarantees only existence within the Trip DB, not operator scope — so the same-operator check is enforced app-layer reusing the tenant-isolation lookup. Override: auto-reciprocal (setting A.returnRouteId=B also sets B.returnRouteId=A) — NOT done by default; would need confirmation it matches the DriverSchedule round-trip pairing UX in `SU26SE101_VIETRIDE_technical_context_v7.md`.
7. **RouteStopFareTemplate effective-window overlap (Task 8.3) — CORRECTED: REJECT overlap (was wrongly "allow/latest-wins").** A new template whose `[effectiveFrom, effectiveUntil)` window intersects an existing (non-deleted) template for the same `(route_id, stop_id)` MUST be rejected -> 422 VALIDATION_ERROR (open-ended `effectiveUntil=NULL` = +infinity). RATIONALE: `db-schema/trip-route-vehicle/README.md:48` is CANONICAL and explicitly requires "Validation overlap chỗ effective windows enforced app-layer" — the earlier "allow overlap" default misread the non-unique index (the index being non-unique is precisely WHY the app must enforce it, not a license to allow overlap). Fare lookup (greatest effectiveFrom <= query instant) remains Day-11+ scope. No override needed — this matches the SOT; only revisit if the human explicitly relaxes README:48.
