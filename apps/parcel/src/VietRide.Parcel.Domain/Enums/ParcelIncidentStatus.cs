namespace VietRide.Parcel.Domain.Enums;

public enum ParcelIncidentStatus
{
    OPEN,
    SEARCHING,
    FOUND,
    FORWARDING,
    RESOLVED,
    CLOSED,
    ESCALATED,
    SEARCH_EXPIRED,
    LOST_CONFIRMED,
}
